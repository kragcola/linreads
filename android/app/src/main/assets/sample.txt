第一章　OTA 验证版本 v2（签名修复）

本文件用于验证 OTA 覆盖安装修复是否生效。如果你在平板上看到这段文字，说明：
① 签名已一致（debug.keystore 已从 GitHub Secret 还原）
② DownloadManager 下载并安装成功
③ 覆盖安装正常，无需卸载重装

恭喜，OTA 流程完整闭环 ✅

第二章　TXT 渲染验证

这一段用来确认 TxtVirtualPager 正常工作。段落以空行分隔，TxtVirtualPagerEngine 会将整个文件按空行切成段落，逐段渲染到 RecyclerView 里，支持连续向下滚动。

短段落。

这是一个稍长的段落，包含多个句子。LinReads 的 TXT 引擎设计目标是处理超大文本文件时不 OOM，通过虚拟分页实现。每个段落作为一个 item，只有可见的 item 会被真正渲染。

第三章　完

以上即为全部测试内容。

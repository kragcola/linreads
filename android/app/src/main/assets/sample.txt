第一章 测试样本

这是 LinReads TXT 最小阅读链路的内置样本文件。它用来验证从解析、引擎、视图到 reader 屏幕的端到端通路是否成立。

段落之间以空行分隔。TxtVirtualPagerEngine 会把整个文件按空行切成段落，逐段渲染到 RecyclerView 里，支持连续向下滚动。

正文字号默认 18sp，行高约 1.6 倍，左右留出舒适的边距，符合长文阅读的排版基线。

这是第四段。滚动时引擎会上报 progression（书内进度比例），作为后续进度同步与书签定位的依据。

第二章 继续滚动

为了能真正看到滚动效果，这里多放几段文字。阅读器的核心是把文本以舒适的方式呈现，让读者专注于内容本身。

继续往下，验证 RecyclerView 的滚动与进度回报是否正常工作。每一段都是一个独立的列表项。

最后一段。如果你能滚动到这里并看到完整文字，说明 TXT 最小阅读链路已经打通。

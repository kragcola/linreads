# Active Work

_最后更新：2026-06-27_

Mode: `task`
Objective: v4lite 本地阅读闭环（用户验收 Phase A）— **L1–L5 全部 ✅**，Phase A 功能条件已满足
Active tracker: [linreads-architecture-docs-2026-06-18.md](linreads-architecture-docs-2026-06-18.md)
v4lite 执行文档: [../android-v4lite-plan.md](../android-v4lite-plan.md)
纯阅读缺口验收表: [android-v4-pure-reading-gap-checklist.md](android-v4-pure-reading-gap-checklist.md)
未完成项回填总表: [android-v4-pure-reading-unfinished-backfill.md](android-v4-pure-reading-unfinished-backfill.md)
Test ledger: N/A

> ⛔ **IMPLEMENTATION GATE**：已于 2026-06-19 获用户放行。2026-06-20 `38367f5` 将 v4lite L1–L5 全部落地。其后持续进行体验打磨。

## 当前状态

- Phase 1 地基框架 ✅、v4lite L1–L5 全部完成 ✅
- `-Preadflow.phase=2 :app:assembleDebug` SUCCESSFUL
- 真机通过 OTA 安装验证 ✅
- EPUB/PDF/TXT/MD 四引擎就位
- Settings（URL/字号/主题/更新）可用
- OTA 更新系统完整（检查→下载→进度→安装）
- ACTION_VIEW/ACTION_SEND 系统入口已接入现有本地导入链路（resolver 单测 + phase2 构建通过；2026-06-26 repo-owned AVD ContentProvider smoke 覆盖 TXT/MD/PDF/EPUB 的 VIEW/SEND 入口；2026-06-27 发现 #109 清数据首读会被通知权限弹窗阻断，已移除启动期通知/安装权限请求并用 A01 回归锁住；#110 release AVD 黑盒复验清数据 ACTION_VIEW EPUB 已无权限弹窗；物理真机文件管理器/分享面板 smoke 待补）
- 非 v4lite 原计划附加：Dwell 建组、拖拽排序、文件夹批量导入、Bundle 详情页

## v4lite 阶段状态

| Phase | 目标 | 状态 |
|-------|------|------|
| L1 书库接入 | SAF 导入 + 书架真实数据 | ✅ 完成（`38367f5`） |
| L2 Reader 完善 | TXT 进度/字号/主题/chrome | ✅ 完成（`38367f5`） |
| L3 EPUB 原生重排 | jsoup→AnnotatedString，连续滚动 | ✅ 完成（`38367f5`，基础连续滚动） |
| L4 PDF 引擎 | PdfRenderer + 分页 | ✅ 完成（`38367f5`） |
| L5 Phase A 验收门 | ACTION_VIEW / 性能 / TalkBack | ✅ 功能完成；性能测量和 TalkBack 测试待独立验证 |

## Phase A 验收检查清单

- [x] 安装后不需要账号/Calibre/网络/选引擎
- [x] SAF 导入 TXT/EPUB/PDF 后书架刷新显示
- [x] ACTION_VIEW/ACTION_SEND 代码入口已接入，可导入后直接进入 reader（repo-owned AVD ContentProvider 8-case smoke 通过；物理真机/系统文件管理器 smoke 待补）
- [x] 最近阅读列表正确（lastReadAt 降序）
- [x] 关闭再开书进度恢复（TXT/EPUB/PDF 各有 locator）
- [x] 字号调整 + 主题切换（白/暗/护眼/系统）生效
- [x] 进度/书架数据离线本地存储（Room，无网络可用）
- [ ] APK < 25MB 和冷启动 < 2s 性能测量（2026-06-22 OTA size/cold-start/PSS/small-sample open 已录入；预算语料与稳定手势帧已补测；2026-06-26 新增 repo-owned `A02PerformanceRuntimeSmokeTest` 作为 AVD instrumentation 性能代理 smoke，数据明确受 test runner/模拟器噪声污染；仍需真机/平板/真实语料与 TXT/PDF 预算调优）
- [ ] TalkBack smoke 测试（2026-06-22 已启用 TalkBack 做 AVD partial；2026-06-26 新增 repo-owned `A03AccessibilityRuntimeSmokeTest`，覆盖书库书卡/菜单、reader surface action、chrome、TOC/search/bookmark/annotation/font/theme 面板并拉取 XML/截图/DB；2026-06-26 staged rerun 再次在 `emulator-5554` 串行跑通并拉取 `/tmp/readflow-stage-verify-20260626/a03-accessibility-runtime-smoke`，该轮抓取记录 TalkBack/touch exploration 开启；2026-06-27 又补 TalkBack settings-on 复跑并恢复关闭；仍缺物理手机/平板与真人完整焦点顺序/语音遍历）

## 剩余差距

完整验收回填以 [android-v4-pure-reading-unfinished-backfill.md](android-v4-pure-reading-unfinished-backfill.md) 为准；原始缺口与来源详表见 [android-v4-pure-reading-gap-checklist.md](android-v4-pure-reading-gap-checklist.md)。

| 项目 | 说明 |
|------|------|
| L3 EPUB 分页模式 | 已从 ViewPager2/PagedReaderEngine 段落页推进到 StaticLayout/measured-line 辅助的段内页切片，并补入 cached 图片块独立页 slice、图片页独立 locator 锚点、cached 文本块样式元数据、样式感知测量、默认 runtime Compose `TextMeasurer.measure()` / `TextLayoutResult` line ranges、paged 文本页 `ComposeView` 根节点、可见 Compose `BasicText` 文本层、Compose 高亮层、Compose 可观察 selection range、Compose selection 高亮状态、Compose selection container 状态、Compose selection callback bridge、Compose gesture selection wiring、Compose long-press ASCII/typographic apostrophe/Unicode hyphen 词边界、standalone connector punctuation 清理、selection selectedText 边缘空白/连接符清理、Compose callback trimmed range/highlight 同步、CJK 单字/text-element 初始 selection、emoji/ZWJ text-element 初始 selection、Compose drag selection emoji/text-element focus 边界规整、Compose drag selection standalone connector focus 清理、跨页导航 selection 清理、Compose outside-text collapsed 初始 selection、Compose drag selection 初始词保留、Compose callback selection code point/text-element 边界规整、selection overlay z-order 前景化、Compose 文本语义/透明 overlay accessibility 隐藏及重绑后持久隐藏、Compose 空白-only selection 清理、Compose 根页码 state 与正文语义分离、透明 overlay touch/focus bridge-only、透明 overlay native selection disabled、透明 overlay empty bridge、selection bridge detached from Compose tree、paged selection state 不再依赖 `SelectionAwareTextView` view bridge、重分页后活跃 Compose 页 slice rebind、page-local selection 清理与 progress/stateDescription 刷新、Compose 文本样式/布局状态，以及 ViewPager 宿主 pageCount 变化刷新、unbind/rebind 生命周期恢复、旧 page request callback 清理、跨书 openBook 旧 selection/Compose 页状态清理、mode switch 旧 Compose selection callback 失效、mode switch 旧 Compose 页 text/callback/semantics/root progress 退场、mode switch 旧 image page alt/可访问文案/drawable 退场、重分页后活跃 image page 页码文案刷新、page-local link metadata、Compose link ranges/callback、paged 内外链 selection 清理和 Compose URL annotation/underline tag/`LinkAnnotation.Url`/`LinkInteractionListener`；真机可交互 Compose selection/action-mode、TalkBack link traversal/speech、视觉/性能 gate 仍待完成 |
| L5 性能测量 | OTA size/cold-start/PSS/small-sample open 已正式记录；2026-06-22 预算语料补测覆盖 1MB TXT、1MB EPUB、5MB EPUB、10MB/50 页 PDF、稳定滚动/翻页 gfxinfo 与 Java heap PSS；补测中发现并修复 PDF recycled-bitmap 翻页崩溃。2026-06-25 follow-up 又收口了两条当前可做的 A-02 优化：TXT 打开 app-private `file://` 书籍时不再二次复制临时文件，PDF paged path 改为串行异步 bitmap store + 首屏预热/邻页预取；JVM/phase2/OTA 验证通过，AVD spot-check `/tmp/readflow-a02-stage-20260625` 显示 PDF aligned 两次翻页 `gfxinfo` 已降到 `69 frames / 28.99% jank / p90 61ms`，但仍未达 `<50ms` 预算。2026-06-26 follow-up 把 PDF page cache 从固定 `±1` / 3 张推进为 128MB bitmap 预算内优先 `±2` / 5 张、超预算自动降级，并通过 PDF 单测、phase2 构建与 UX-05 AVD reader-route 回归；同日新增 repo-owned `A02PerformanceRuntimeSmokeTest`，在 `emulator-5554` 上生成 1MB TXT、1MB EPUB、20 页 PDF 并记录首屏代理、PSS 与 gfxinfo，证据 `/tmp/readflow-a02-performance-runtime-20260626/a02-performance-runtime-smoke` 显示 TXT/EPUB/PDF 最终首屏代理分别 `8124ms/10788ms/6783ms`、PDF `p90=150ms`，且 summary 明确 `AVD instrumentation proxy` 边界；A-02 继续保持 `PARTIAL`。仍缺真实手机/平板、真实出版语料复测，以及 TXT/PDF 进一步预算调优 |
| L5 TalkBack | TalkBack partial 已执行：Reader 内容/页码和书库节点有 label；第一张书卡标题焦点裁切已修复并在 AVD TalkBack/D-pad 焦点下复测；reader chrome 与 TOC/search/bookmark/annotation/font/theme 面板 XML+截图已补。2026-06-26 进一步新增 repo-owned `A03AccessibilityRuntimeSmokeTest`，在 `emulator-5554` 上走 ACTION_VIEW 导入、返回书库、书卡打开、reader chrome 与各面板检查，证据 `/tmp/readflow-a03-accessibility-runtime-20260626` 记录 TalkBack/touch exploration 开启、22 个截图/XML/DB 和 clean logcat；同日 staged rerun 串行复跑 A-03 并拉取 `/tmp/readflow-stage-verify-20260626/a03-accessibility-runtime-smoke`，摘要仍记录书卡、reader surface、chrome、TOC/search/bookmark/annotation/font/theme 标签，该轮 AVD settings 记录 `enabled_accessibility_services=TalkBack`、`accessibility_enabled=1`、`touch_exploration_enabled=1`，logcat 关键 crash/ANR grep 为空；仍缺物理手机/平板和真人完整焦点顺序/语音遍历，A-03 保持 `PARTIAL` |
| L5 staged rerun 2026-06-27 | 本轮只发现 `emulator-5554`，继续不声明真实设备。A-02 复跑 `A02PerformanceRuntimeSmokeTest` = `OK (1 test)`，证据 `/tmp/readflow-stage-verify-20260627/a02/a02-performance-runtime-smoke` 显示 TXT/EPUB/PDF 首屏代理 `7839ms/10256ms/7131ms`、total PSS `222554/245131/302291KB`、PDF `gfx_p90_ms=200`，继续保持 `PARTIAL`。A-03 先在 TalkBack 关闭态复跑，再临时开启 AVD TalkBack settings（`enabled_accessibility_services=com.google.android.marvin.talkback/...`、`accessibility_enabled=1`、`touch_exploration_enabled=1`）复跑，均为 `OK (1 test)`；证据 `/tmp/readflow-stage-verify-20260627/a03-talkback-on/a03-accessibility-runtime-smoke`，测试后已恢复 settings 为 `null/0/0`。这仍不是真人 TalkBack speech 或完整焦点顺序遍历 |
| L5 late staged rerun 2026-06-27 | #110 release 黑盒后又串行补跑当前可模拟项，证据 `/tmp/readflow-stage-verify-20260627-late/staged-verification-summary.md`。A-01 `OK (1 test)`，50 个入口证据文件，logcat critical grep 0；A-03 `OK (1 test)`，标签证据完整但本轮 `touch_exploration_enabled` 未保持为 1，因此不声明真实 TalkBack speech/触摸探索；A-02 `OK (1 test)`，TXT/EPUB/PDF 首屏代理 `7940/10040/6722ms`、PDF `gfx_p90_ms=150`，继续 `PARTIAL`；SAF `OK (1 test)`，CREATE/OPEN monitor 和备份恢复回灌通过。Calibre grouped 本轮因 Docker `qq-bot` 占用宿主 8081 未复跑成功，18081 failure smoke 也在连接成功文案前超时，本轮不新增 Calibre 通过证据 |
| L5 文件入口 smoke | ACTION_VIEW/ACTION_SEND 已有代码入口；repo-owned phase2 AndroidTest external ContentProvider 已验证 TXT/MD/PDF/EPUB 的 VIEW/SEND 导入、upsert 和 reader 打开。2026-06-27 #109 release 黑盒发现清数据首读 EPUB 被 Android 通知权限弹窗阻断；当前修复已移除启动期通知/安装权限请求，A01 回归新增无阻断弹窗断言并通过；#110 release AVD 黑盒用同一 `content://media/external/file/306` 清数据 ACTION_VIEW 复验，首采样 `blocked_dialog=false` 且 reader marker 出现；仍需物理真机从系统文件管理器/分享面板验证 |
| Reader 捏合/字号验收 | 2026-06-25 UX-02 follow-up：上一轮“`adb shell input` 只能单指”的边界已被 phase2 AndroidTest 真双指 runtime smoke 推进。新增 `android/app/src/phase2AndroidTest/java/dev/readflow/ux02/ReaderPinchRuntimeSmokeTest.kt`，通过直接向 reader 容器注入双指 `MotionEvent`，在 `emulator-5554` 上跑通 `adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux02.ReaderPinchRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。证据 `/tmp/readflow-ux02-stage-20260625/ux02-runtime-smoke` 显示 TXT 从 `baseline_font_label=16sp` / `baseline_text_size_px=42.0` 捏合到 `after_font_label=22sp` / `after_text_size_px=59.294098`，且 DataStore `persisted_font_size_sp=22`；PDF 从 `baseline_scale_type=FIT_CENTER` 进入 `after_scale_type=MATRIX`，矩阵值 `matrix_scale_x=1.4117643`、`matrix_translate_x=-222.35272`、`matrix_translate_y=56.47101`，证明当前 AVD 已不只是“文案说支持 pinch”，而是真正跑到了 TXT 字号 preview+持久化与 PDF matrix zoom 路径。边界：仍是 AVD instrumentation 注入，不是真实手指；缺物理手机/平板 pinch ergonomics、PDF 手势后视觉观感/边界拖拽、以及 EPUB/MD 真机多尺寸调校，所以 UX-02 继续保持 `VERIFY`，但“当前设备无法合成真实双指 pinch”这条旧阻塞已不再成立 |
| Reader 模式切换/阅读控制验收 | 2026-06-25 UX-03/UX-04 follow-up：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/ux03/ReaderModeControlsRuntimeSmokeTest.kt`，并在 `emulator-5554` 上跑通 `adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux03.ReaderModeControlsRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。证据 `/tmp/readflow-ux03-ux04-stage-20260625/ux03-ux04-runtime-smoke` 显示 TXT 在 scroll 模式滚到 paragraph `018..031` 可见区后切到 paged，当前页 `tagIndex=19` 落在原可见范围内；切回 scroll 后同一段仍在视口里，且 Room `reading_progress.locatorJson` 前后保持同一 `ByteOffset offset=1406 length=73`、`totalProgression=0.296875`。同组证据还确认 `排版` 面板真实暴露 `16sp`、`1.75x`、`滚动`、`分页`，`主题` 面板暴露 `跟随系统` / `日间` / `夜间` / `护眼`；切到夜间后 DataStore `persisted_theme=DARK`，TXT runtime palette 从 `background=0xFFEDE6D6 text=0xFF2A2620` 切到 `background=0xFF2A2620 text=0xFFD8CFBC`。边界：仍是 AVD instrumentation + XML/Room/截图证据，不是真实手机/平板 UI、TalkBack 焦点/语音遍历或更广尺寸视觉验收，因此 UX-03/UX-04 继续保持 `VERIFY`，但旧的“只证明控件存在/模式可切”弱证据已推进到真实运行态位置保持与主题应用路径。 |
| Reader 模式切换 tablet-like 锚点回归 | 2026-06-27 UX-03/UX-04 follow-up：在 `1600x2560` / density `320` 的 tablet-like AVD override 下，复现旧风险：TXT scroll 模式可见 `paragraph 000..030` 后切到 paged 会因 stale `_currentLocator` 回落到 `paragraph 000` / `第 1 页`。修复后 `TxtVirtualPagerEngine.setMode()` 在连续滚动切分页前采样 viewport 中心最近的可见 paragraph 并触发对应 page request。证据 `/tmp/readflow-ux03-fixed-20260627/ux03-ux04-runtime-smoke/ux03-mode-switch-summary.txt` 显示切换前可见 `0..30`，切到分页后 `paged_tag_index=15` / `paged_visible_text=UX03 anchor paragraph 015...`，`ux03-paged-mode.xml` 显示 `第 16 页，共 64 页`，切回滚动后可见 `14..45` 且 Room `persisted_after_return_total=0.234375`。Validation: TXT engine 单测、UX03 targeted/full runtime、PAGE-05 EPUB runtime 回归和 `:app:assembleDebug` 通过。边界：仍是 AVD instrumentation，不是真实物理平板、TalkBack speech、性能 benchmark、真实 Calibre 或 SAF；UX-03/UX-04 继续保持 `VERIFY`。 |
| Reader release 黑盒锚点复验 | 2026-06-27 staged follow-up：在已安装 `dev-latest` / `Dev build #105` OTA 包的 AVD 上，用 MediaStore `content://` 授权打开长 TXT；黑盒手势切到 scroll 并滚到 `paragraph 056..067` 后，再从 `排版` 切回 paged。证据 `/tmp/readflow-release-blackbox-20260627/release-blackbox-summary.txt` 显示 paged 后可见 `paragraph 062`，没有回到 `paragraph 000` / 第 1 页；logcat 关键 grep 未命中 Readflow crash/ANR/security/recycled-bitmap。边界：这是 release 包 AVD 黑盒，不是真实平板文件管理器/SAF DocumentsUI 或人手验收 |
| Reader 行距控制验收 | 2026-06-26 UX-04/PAGE-05 follow-up：同一个 packed EPUB runtime smoke 现在会在真实 reader route 里通过 `排版` 面板拖动 `行距` slider。证据 `/tmp/readflow-page05-packed-line-spacing-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-micro-summary.txt` 显示 `after_line_spacing_persisted=2.2`，EPUB paged Compose 从 4 页重分页到 5 页，当前页仍有 8 个短段而不是一段一页。边界：仍是 AVD UIAutomator slider，不是真实手机/平板触摸调参、TalkBack 焦点/语音遍历或多尺寸视觉验收，因此 UX-04 继续保持 `VERIFY`、PAGE-05 继续保持 `PARTIAL`。 |
| Reader 键盘/翻页动效验收 | 2026-06-26 UX-05/PAGE-01~04 follow-up：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/ux05/ReaderPagedKeyboardRuntimeSmokeTest.kt`，并在 `emulator-5554` 上跑通 `adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux05.ReaderPagedKeyboardRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (3 tests)`。证据 `/tmp/readflow-ux05-page-stage-20260626/ux05-page-runtime-smoke` 显示 reader surface 基线暴露 `上一页|下一页|显示或隐藏阅读工具栏`，默认 paged host transformer 为 `CurlPageTransformer`，`DPAD_RIGHT/LEFT`、`PAGE_DOWN/UP`、`SPACE`、`ACTION_SCROLL_FORWARD/BACKWARD` 都能在 4 页 PDF 上驱动 `第 1 页，共 4 页` ↔ `第 2 页，共 4 页`，`DPAD_CENTER` 会拉起含 `排版` / `主题` 的 chrome；同组证据还确认 `TransitionType.FADE` 运行态映射到 `FadePageTransformer`、`TransitionType.SLIDE/NONE` 回到 `ResetPageTransformer`，以及 PDF 页面在 `zoomScale=1.5`、`scaleType=MATRIX` 后翻到下一页会重置为 `zoomScale=1.0`、`scaleType=FIT_CENTER`。边界：仍是 AVD instrumentation + XML/截图证据，不是真实硬件键盘/D-pad、TalkBack action traversal、真机/平板视觉动效、真实 swipe/pinch ergonomics 或帧率测量，因此 UX-05 继续保持 `PARTIAL`、PAGE-01~04 继续保持 `VERIFY`。 |
| EPUB-04~06 连续阅读验收 | 2026-06-26 grouped runtime smoke：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/epub04/EpubContinuousRuntimeSmokeTest.kt`，并在 `emulator-5554` 上跑通 `adb -s emulator-5554 shell am instrument -w -e class dev.readflow.epub04.EpubContinuousRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (3 tests)`。证据 `/tmp/readflow-epub04-06-stage-20260626/epub04-06-runtime-smoke` 显示连续模式 image-only cover 首屏直接落到 `Cover art` 图片块、后续正文与 inline `Scene art` 图片连续渲染；同组外链/内链 smoke 记录 `clickable_span_count=2`、`external_monitor_hits=1`、`internal_tap_consumed=true`，并把 Room locator 推进到 `spineIndex=1` 的目标段落；fixed-layout fallback smoke 还确认 heading 加粗放大、table/pre 等宽、`pre_can_scroll_horizontally=true`、blockquote/list 缩进和 unknown block 文本降级都跑在真实 reader route 上。边界：仍是 AVD runtime + XML/DB/screenshot 证据，不是真机/平板视觉、TalkBack link traversal/speech、外链 chooser/browser、pre 横向滚动手感或性能验收，因此 `EPUB-04/05/06` 继续保持 `VERIFY`。 |

| PAGE-05 | 2026-06-26 grouped runtime smoke：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/page05/EpubPagedRuntimeSmokeTest.kt`，并在 `emulator-5554` 上跑通 `adb -s emulator-5554 shell am instrument -w -e class dev.readflow.page05.EpubPagedRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (3 tests)`。证据 `/tmp/readflow-page05-stage-20260626/page05-epub-runtime-smoke` 显示 paged EPUB 文本页在 selection 后切回 scroll 时会退场并清空 `pageProgress/selection/semantics`，paged 内链可跳到 image fragment 且旧 selection 归零，image-only cover 首屏直接落在图片页并在下一页进入正文。边界：仍是 AVD runtime + XML/DB/screenshot 证据，不是真机图文分页、硬件键盘、TalkBack、视觉或性能验收，因此 PAGE-05 仍保持 `PARTIAL`。 |
| PAGE-05 packed typography | 2026-06-26 follow-up：36 个微短段 EPUB 已覆盖 baseline packed、pinch 字号重分页、accessibility scroll forward、以及行距 slider 重分页。最终完整 class 证据显示 baseline `page_count=3`、pinch 后 `after_pinch_page_count=4`、行距 2.2x 后 `after_line_spacing_page_count=5` 且当前页 `Line 017..024` 8 段；`EpubPagedRuntimeSmokeTest` 完整跑通 `OK (4 tests)`，logcat crash/ANR/recycled-bitmap grep 为空。边界：这仍不等价于真机平板手指、TalkBack speech/action-mode、混合 EPUB 视觉或帧率/PSS。 |
| PAGE-05 packed rotation | 2026-06-26 follow-up：新增横竖屏/viewport 改变 smoke，48 个微短段 EPUB 竖屏分页后旋转到横屏仍保持 packed。证据 `/tmp/readflow-page05-packed-rotation-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-rotation-summary.txt` 显示 portrait `1080x2400` / `page_count=4` / 第 1 页 14 段，landscape `2400x1080` / `page_count=8` / 第 1 页 6 段，landscape 下一页仍有 6 段；完整 `EpubPagedRuntimeSmokeTest` = `OK (5 tests)`。边界：仍是 AVD orientation，不是真实平板旋转、折叠屏/分屏、TalkBack 或性能验收，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 packed max typography | 2026-06-26 follow-up：新增最大排版横屏 smoke，72 个微短段 EPUB 在 32sp 字号、2.2x 行距、landscape `2400x1080` 下仍保持 packed。证据 `/tmp/readflow-page05-packed-max-typography-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-max-typography-summary.txt` 显示 `page_count=24`，第 1 页 `A11y line 001..003` 3 段，accessibility scroll forward 后第 2 页 `A11y line 004..006` 3 段；完整 `EpubPagedRuntimeSmokeTest` = `OK (6 tests)`，logcat crash/ANR/recycled-bitmap/`Unable to find reader surface` grep 为空。边界：仍是 AVD 横屏与 instrumentation 证据，不是真实平板低视力排版、TalkBack speech/action-mode、混合 EPUB 视觉或帧率/PSS，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 packed CJK mixed content | 2026-06-26 follow-up：新增中文混合内容 smoke，包含章节标题、60 个中文短文本块、中文对话标点、blockquote、list item 和 5 个空段。证据 `/tmp/readflow-page05-cjk-dialogue-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-cjk-dialogue-summary.txt` 显示 `page_count=31`，第一页为非空章节标题 `第一章 雨声`，accessibility scroll forward 后第 2 页 packed 了 `对话001`、`对话002`、`旁白003`、`旁白004` 4 个中文短块；完整 `EpubPagedRuntimeSmokeTest` = `OK (7 tests)`，logcat crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError grep 为空。边界：仍是 AVD reader-route 证据，不是真实平板中文书籍视觉、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 packed long CJK paragraph | 2026-06-26 follow-up：新增长无空格中文段 smoke，96 个无 ASCII 空格中文长段片段 + 18 个尾部短句在真实 reader route 中分页为 5 页。证据 `/tmp/readflow-page05-cjk-long-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-cjk-long-summary.txt` 显示第 1 页/第 2 页各 packed 27 个长段片段，accessibility scroll 到第 4 页时同页包含 `长段082..096` 和 `尾句001..006` 6 个尾句；完整 `EpubPagedRuntimeSmokeTest` = `OK (8 tests)`，阶段构建通过，logcat crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError grep 为空。边界：仍是 AVD reader-route 证据，不是真实平板中文书籍视觉、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 packed CJK br poem lines | 2026-06-26 follow-up：新增 `<br/>` 分隔中文诗行 smoke，单个 `<p>` 内 84 行 `诗行001..084` 在真实 reader route 中分页为 4 页。证据 `/tmp/readflow-page05-cjk-br-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-cjk-br-summary.txt` 显示 `max_packed_page_budget=28`、`page_count=4`，第 1 页 packed 27 行，accessibility scroll forward 后第 2 页 packed 27 行；完整 `EpubPagedRuntimeSmokeTest` = `OK (9 tests)`，阶段构建通过，logcat crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError/FAILURES grep 为空。边界：仍是 AVD reader-route 证据，不是真实平板中文诗歌视觉、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 packed decorative separators | 2026-06-26 follow-up：新增装饰分隔/空白段/超短中文对话 smoke，64 个 `碎句` 中穿插 14 个空白段、9 个 `<hr/>`、em/strong/span/ruby 内联包裹，在真实 reader route 中分页为 8 页。证据 `/tmp/readflow-page05-decorative-runtime-20260626/page05-epub-runtime-smoke/packed-decorative-summary.txt` 显示采样前 4 页各 packed 8 个真实碎句，且没有落到空白/装饰页；targeted `EpubPagedRuntimeSmokeTest#epubPagedSkipsDecorativeSeparatorsAndPacksTinyDialogueRuntime` = `OK (1 test)`，阶段构建和 OTA assembly 通过，logcat crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError/FAILURES grep 为空。边界：仍是 AVD reader-route 证据，不是真实平板短句小说视觉、TalkBack speech/action-mode、真实翻页手感或帧率/PSS，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 mixed safe-style packed fix | 2026-06-27 follow-up：`Dev build #107` release 黑盒极端 EPUB `/tmp/readflow-dev107-extreme-smoke/dev107-extreme-pack.epub` 发现 micro paragraph 已 packed，但 Body/ListItem/Blockquote 混排短块仍在 `page-12.xml` 起出现 `DEV107 对话001`、`DEV107 清单002`、`DEV107 旁白003` 一页一个短块。修复后 `packAdjacentShortTextPages()` 对无链接、非 heading/pre/table/image 的 safe styles 只要求同 spine，不再要求完全同 `textStyle`；新增 `EpubPageMappingTest.block paged layout packs adjacent safe mixed text styles`。Validation: `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubPageMappingTest"`、`git diff --check`、`:app:assembleOta` 通过，本地 OTA SHA-256 `d44afde08dd10c387654a1e69abcda16267d29b57127c8c602401be2cf886512`。边界：新 release 黑盒需 push 后复验；当前仍非真实平板/TalkBack/性能证据，PAGE-05 保持 `PARTIAL`。 |
| PAGE-05 mixed safe-style release #108复验 | 2026-06-27 follow-up：GitHub Actions 发布 `Dev build #108` / commit `cf861e3` 后，下载远端 `app-ota.apk`（SHA-256 `08a4e521a39ae83e8bb37d1db4966207fcf145dc3542e3247a8cc250e238cb60`）安装到 `emulator-5554`，用同一 MediaStore EPUB `content://media/external/file/306` 做 release 黑盒。冷启动 `TotalTime=924ms`；证据 `/tmp/readflow-dev108-extreme-smoke/epub-release-blackbox/page-sampling.txt` 显示 page 11 packed `DEV107 对话001`、`DEV107 清单002`、`DEV107 旁白003` ... `DEV107 清单020` 共 20 个混合 safe-style markers，page 14/15 也各 packed 20 个 markers；#107 的一页一个短块未复现。边界：仍是 AVD release 黑盒，不是真实物理平板、人手翻页、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。 |
| TXT 格式质量回归 | 2026-06-23 回填 TXT-01/02/03/04：`TxtDocumentTest` 增加大于 `3 * 64 KiB` 的 UTF-8 语料、跨 block ranges、search ByteOffset 字符起点、engine-state cache round-trip、GB18030 四字节边界 normalization，以及 UTF-8/GBK/Shift_JIS search/selection ByteOffset 字符起点断言；TXT targeted/all tests 与 phase2 组合构建通过。仍缺真实导入 TXT 的物理手机/平板 PSS、首开、滚动、恢复、搜索/选择 UI smoke |
| PDF 目录/outline | 2026-06-23/24 回填 PDF-02：`PdfOutlineParser` 在常见 `/Outlines` 树基础上补入 indirect `/Title` object ref、indirect `/A` / `/D` action-destination ref、catalog `/Names /Dests` name tree 与旧式 catalog `/Dests` dictionary named destination 解析；`PdfOutlineParserTest` 覆盖 outline `/Dest (chapter-two)`、GoTo `/D /chapter-one`、direct destination array、indirect `/D [...]` destination object，以及 indirect outline title/action/name-tree destination ref。PDF targeted/all tests 与 phase2 组合构建通过；AVD real-outline runtime 证据 `/tmp/readflow-toc-batch10-pdf-real` 与 `/tmp/readflow-toc-batch11-pdf-real-late` 证明 `react_2210_03629_outline|PDF` 的 TOC 面板出现真实条目 `1 Introduction` / `2 ReAct: Synergizing Reasoning + Acting` / `3.3 Results and Observations`，不再回退到 `第 N 页（页列表）`，且点击 `3.3 Results and Observations` 后 reader 从 `第 1 页，共 33 页` 跳到 `第 5 页，共 33 页`，Room 写入 `Page index=4/33`、`totalProgression=0.121212124824524`。仍缺物理手机/平板 TOC 跳转、xref/object streams、大 PDF scan cap 与更广 named destination 形态 |
| MD locator/渲染 offset | 2026-06-23/24 回填 MD-01：`MarkdownDocument` 增加源码 offset ↔ Markwon rendered text offset 轻量映射；滚动、goTo、选择和高亮不再直接混用源码 offset 与渲染文本 offset。MD targeted/all tests 与 phase2 组合构建通过；AVD runtime TOC 证据 `/tmp/readflow-toc-batch8-md` 显示 `readflow-search-long.md` 从文首打开后，TOC 面板可见 `Search Target`，点击后正文落到 near-tail `MDSEARCH042` / `Search Target` 区域，Room 写入 `Section(spineIndex=0, elementIndex=362, charOffset=21820)`、`totalProgression=0.917192101478577`。2026-06-24 晚补 MD READ-04/05 grouped runtime closure：`SelectionAwareTextView.clearNativeTextSelection()` 会移除原生 `Spannable` selection 并保留祖先 scroll，`MarkdownEngine.clearTextSelection()` 会在清空 engine/native selection 时抑制内部 locator 覆写并按当前 near-tail 视口重算 locator；新增 RED/GREEN `SelectionAwareTextViewTest.clear native text selection preserves scroll and removes native selection` 与 `MarkdownEngineTest.clearTextSelection preserves markdown scroll and locator near current section`，配合 `./gradlew -Preadflow.phase=2 :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.SelectionAwareTextViewTest" :render:md:testDebugUnitTest --tests "dev.readflow.render.md.MarkdownEngineTest" :render:api:compileDebugKotlin :app:assembleDebug` GREEN。运行时证据 `/tmp/readflow-md-runtime-fix4-20260624-205642`、`/tmp/readflow-md-runtime-fix4b-20260624-210955`、`/tmp/readflow-md-runtime-fix5-20260624-2115` 证明 `TOC -> Search Target -> 长按 -> 保存` 后视口不再回到文首，save 后 `reading_progress` 保持 near-tail `Section(spineIndex=0, elementIndex=362, charOffset=21823)`、`totalProgression≈0.9173182`；再移走到 `Section(spineIndex=0, elementIndex=304, charOffset=18311)`、`totalProgression≈0.76969314` 后，从标注列表跳回会把 `reading_progress` 恢复到选区锚 `Section(spineIndex=0, elementIndex=366, charOffset=21936)`、`totalProgression≈0.9220681`。仍缺真实设备/平板 MD save-restore、TOC jump、长文滚动、复杂 Markdown inline/table/link 选择和布局变化恢复 smoke |
| 进度同步/replica 元数据 | LWW 骨架已就位（NoOpSyncBackend），真实远程同步后端在当前纯阅读回填中明确延期；Settings 已显示 `远程同步未启用`，不再把 no-op 伪装成已同步。2026-06-22 AVD 回填 STATE-04：DataStore `device_id` 与 Room `reading_progress.deviceId` 在首次生成、保留数据重装、AVD reboot 后保持一致，证据在 `/tmp/readflow-state04-deviceid-smoke`；仍缺物理手机/平板首次安装、应用升级和系统重启 smoke |
| 进程死亡恢复 | 2026-06-22 AVD 回填 STATE-01/02/03：发现并修复 TXT 硬换行长文滚动后 locator 仍为 0，以及 `goTo()` 早于 `createView()` 时重开未滚到恢复位置的问题；final evidence 在 `/tmp/readflow-state-recovery-smoke-final`，Room locator 写入 `ByteOffset offset=520`，engine-state cache `129110 bytes`，后台杀进程后从书架重开同一本可恢复到 line `000003/000004` 附近；仍缺物理手机/平板系统级 process-death/task restore smoke |
| Calibre 离线缓存 LRU | 2026-06-25 grouped AVD/runtime 回填 SRC-05：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src05/CalibreCacheLruRuntimeSmokeTest.kt`，复用扩展后的 `android/test-tools/fake_calibre_server.py` 下载 6 本远程 EPUB，并通过打开前 4 本写入 `lastReadAt`，验证第 6 本下载触发 trim 时会保留刚下载的 protected book `calibre-106`、淘汰唯一未读旧书 `calibre-105`。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 8081`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src05.CalibreCacheLruRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。证据 `/tmp/readflow-src05-lru-runtime-20260625-final/calibre-lru-runtime-smoke` 证明 `calibre-105` 被清成 `NOT_DOWNLOADED + localUri=NULL` 且文件删除，`calibre-101/102/103/104/106` 保留本地文件，离线筛选为 `离线可读 5`，关闭 fake server 后仍能离线打开 `LRU Remote 06`。边界：仍是 AVD + fake host server，不是真实 Calibre Content Server、认证服务器、真实 LAN/低存储、物理手机/平板或容量策略文案验收。 |
| Calibre 封面/OTA 构建 | 2026-06-26 follow-up：为平板 OTA 构建接入 Coil 3 HTTP network fetcher，`ReadflowApplication` 通过 `SingletonImageLoader.Factory` 注册 `OkHttpNetworkFetcherFactory()`，app/core-ui 依赖补齐 `coil-network-okhttp`。随后修复 fake Calibre server 路由顺序：`/get/cover/<id>/calibre-library` 先于泛化 `/get/<FMT>/<id>/...` 匹配，避免 cover 被 download handler 当成 unsupported format 404。Validation: `python3 -m unittest android/test-tools/test_fake_calibre_server.py` = `OK`；`./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest` 通过；`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src01.CalibreGroupedRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。同日 staged rerun 再次启动 fake server 并串行跑通 `CalibreGroupedRuntimeSmokeTest`，证据拉取到 `/tmp/readflow-stage-verify-20260626/calibre-runtime-smoke`；`settings-probe-summary.txt` 记录 bad port 失败提示和 host-only probe 成功，`download-offline-remove-summary.txt` 记录 cover event、download event、离线打开正文和移除下载后 `NOT_DOWNLOADED/localUri=null`。边界：当前仍只有 `emulator-5554`，没有真实平板证据；SRC-01~04/SRC-06/07 保持 `VERIFY`。 |
| Calibre staged rerun 2026-06-27 | 再次启动 `python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 8081` 并串行跑通 `CalibreGroupedRuntimeSmokeTest` = `OK (2 tests)`；证据 `/tmp/readflow-stage-verify-20260627/calibre/calibre-runtime-smoke` 记录 bad port 失败提示、host-only probe 成功到 `10.0.2.2:8081`、cover/download events、离线打开正文和移除下载后 `NOT_DOWNLOADED/localUri=null`。边界仍是 AVD + fake server，不是真实 Calibre Content Server、真实 LAN、认证服务器或真实平板封面视觉 |
| Calibre 手动下载/移除 | 2026-06-25 grouped AVD/runtime 回填 SRC-06：phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src01/CalibreGroupedRuntimeSmokeTest.kt` 在 `emulator-5554` 上串联 `设置失败提示 -> 探测 10.0.2.2:8081 -> 搜索/下载 -> 断服务后离线打开 -> 移除下载`，`adb ... -e class dev.readflow.src01.CalibreGroupedRuntimeSmokeTest ...` = `OK (2 tests)`。证据 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke` 显示 `calibre-42` 下载后 `DOWNLOADED + file:/data/user/0/dev.readflow/files/books/calibre-42.epub`，移除后 DB 回到 `NOT_DOWNLOADED + localUri=NULL`，磁盘文件删除且离线空状态出现。边界：仍是 AVD + 本机 fake Calibre server，不是真实 Calibre Content Server、认证服务器、物理手机/平板或大文件进度/失败态 smoke。 |
| Calibre 下载失败态 | 2026-06-26 follow-up 回填 SRC-06：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src06/CalibreDownloadFailureRuntimeSmokeTest.kt`，通过完整 URL `http://10.0.2.2:18081` 连接 fake Calibre，搜索到 `Remote EPUB Smoke` 后主动关闭 server，再点击下载，模拟 LAN/Calibre 睡眠或断源。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 18081`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src06.CalibreDownloadFailureRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。证据 `/tmp/readflow-src06-download-failure-runtime-20260626-pass/calibre-download-failure-runtime-smoke/download-failure-summary.txt` 显示 `downloadError=Calibre: Failed to connect to /10.0.2.2:18081`、`bookRowAfterFailure=null`、`orphanFilesAfterFailure=`，DB `books` count 为 0，书架仍显示 `还没有书`。边界：仍是 AVD + fake server failure-path，不是真实 Calibre Content Server、认证服务器、真实 LAN、物理手机/平板、大文件进度/取消或失败重试 UX；`SRC-06` 保持 `VERIFY`。 |
| Calibre 离线模式视图 | 2026-06-25 grouped AVD/runtime 回填 SRC-07：同一 `CalibreGroupedRuntimeSmokeTest` 在关闭 fake server 后切到 `离线可读`，仍可见并打开 `Remote EPUB Smoke`，reader 首屏出现 `Calibre smoke paragraph proves offline reader opening after download.`；随后移除下载后离线筛选落到 `没有离线可读的书`。证据同 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke`。边界：仍是 AVD + fake host server，不是真实 LAN/source unavailable、物理手机/平板或 TalkBack/UI 验收。 |
| 数据导出/恢复 | 2026-06-23 回填 SRC-08/SRC-09：Settings 已提供 SAF `导出备份` 与 `恢复备份` 入口；`LinReadsBackupExporter` 导出 ZIP `manifest.json`，`LinReadsBackupRestorer` 只恢复进度、书签、标注，按 `updatedAt` LWW 合并并保留 tombstone，不覆盖 `books` 本地书库。2026-06-24 先补入恢复 schema hardening，只接受显式 `schema_version=1`，缺失或非法旧版本不会写入本地数据；随后新增 phase2 AndroidTest `BackupExportRestoreRuntimeSmokeTest`，在 AVD 上验证真实 ZIP 导出、manifest 计数、第二个 Room DB 的 LWW 恢复、bookmark tombstone 保留、`books` 表不被覆盖，以及缺失 `schema_version` 时 restore 直接拒绝且本地 DB 不变。2026-06-26 staged rerun 串行复跑 `BackupSafUiRuntimeSmokeTest`，证据 `/tmp/readflow-stage-verify-20260626/backup-saf-ui-runtime-smoke/backup-saf-ui-summary.txt` 记录 `CREATE_DOCUMENT` / `OPEN_DOCUMENT` monitor 各命中 1 次、ZIP `format=linreads-backup` / `schema_version=1`、恢复后进度 `{"type":"byte","offset":128} updatedAt=300`、删除态书签和标注回灌；并确认此前并行跑 A-03 + SAF 的 `Process crashed` 是 UI instrumentation 抢同一 AVD 的测试调度干扰，SAF 串行重跑通过。仍缺真实手机/平板 SAF 导出/恢复、手动 ZIP 核验、大数据量/异常写入、真实跨设备 round-trip，以及真实系统文档选择器 UI smoke |
| 数据导出/恢复 staged rerun 2026-06-27 | 串行复跑 `BackupSafUiRuntimeSmokeTest` = `OK (1 test)`；证据 `/tmp/readflow-stage-verify-20260627/saf/backup-saf-ui-runtime-smoke/backup-saf-ui-summary.txt` 记录 `CREATE_DOCUMENT` / `OPEN_DOCUMENT` monitor 均命中、ZIP `format=linreads-backup` / `schema_version=1`、恢复后进度 `{"type":"byte","offset":128} updatedAt=300`、删除态书签和标注回灌。仍是 AVD ActivityMonitor + test ContentProvider Uri，不是真实 DocumentsUI/OEM 文件管理器/真人选择 |
| Optional DOCX/CBZ | 2026-06-23 回填 OPT-01/02/03：新增 `ADR-MuPDF-License`，裁决 base APK 保持 MuPDF-free，不在当前里程碑启用 AGPL MuPDF；DOCX 因官方资料未显示为 MuPDF Core 标准输入而延期，CBZ 虽可由 MuPDF 支持但也因许可/包体积成本延期，未来优先评估无 AGPL 的 first-party ZIP image pager |

## 下一步

- Phase A 性能测量 + TalkBack + 文件入口 smoke 的真机/平板补测；2026-06-27 当前 `adb devices -l` 仍仅有 `emulator-5554`，不能把 staged AVD rerun、TalkBack settings-on rerun、release 黑盒或 fake Calibre/SAF 模拟冒认为实机证据；#110 已补 release 清数据 ACTION_VIEW 首读无通知权限弹窗 AVD 黑盒复验，下一步仍是真实平板/手机文件入口
- 继续纯阅读回填：当前未完成队列已无 TODO；优先补 VERIFY/PARTIAL 的真机/平板、TalkBack、性能、真实语料与真实 Calibre/SAF smoke，涉及真机门的条目保持 VERIFY/PARTIAL，不升 DONE
- 2026-06-27 已补 TXT scroll→paged tablet-like 锚点修复与回归；平板 OTA 更新后优先用真实长 TXT 复测“滚到中段→切分页→切回滚动”是否仍锚在阅读焦点附近
- 平板 OTA 更新后优先复测 SRC-03：真实 Calibre 搜索结果封面是否发起 HTTP 请求并显示；当前 AVD fake server cover request 已观察到，但仍不能替代真实 Calibre Content Server / 真实 LAN / 真实平板 smoke
- Calibre grouped fake-server smoke 若要本机复跑，需要先释放/改避宿主 8081；当前 8081 被 Docker `qq-bot` 容器占用，而测试硬编码 `10.0.2.2:8081`
- 继续 staged backfill：`SRC-01~09` 当前已全部从 JVM/compile 推进到 AVD/runtime，并在 2026-06-26 staged rerun 中串行复验 Calibre/SAF；A-02 已补 repo-owned AVD instrumentation 性能代理 smoke，但结果仍只支持 `PARTIAL`。下一组转向真实 Calibre/SAF、真机/平板、TalkBack speech、真实性能与真实语料补测，涉及真机门的条目暂不升 DONE
- EPUB 分页模式 gate（v4 最高技术风险）
- Calibre 书源接入（Phase B）
- 进度同步后端选型与实现（Phase D）

## 恢复备注

- v4lite L1–L5 已全部完成，当前在体验打磨阶段
- 6/20 之后的增量提交主要是 OTA 更新系统、书架交互、reader 菜单/触摸和体验打磨
- 2026-06-26 A-01 已从临时 `/tmp/readflow-share-smoke` synthetic sender 推进到 repo-owned phase2 AndroidTest：测试 APK 暴露 `dev.readflow.test.a01bookprovider` 外部 ContentProvider，`A01IncomingBookRuntimeSmokeTest` 在 `emulator-5554` 上跑通 `ACTION_VIEW` / `ACTION_SEND` × TXT/MD/EPUB/PDF 共 8 个导入入口。证据 `/tmp/readflow-a01-incoming-runtime-smoke-20260626` 记录 8 个 `books` row 均落到 app-private `file://`，reader probe 命中 TXT/MD/EPUB 正文与 PDF `第 1 页，共 1 页`。当前仍只能记 AVD/provider 模拟，`A-01` 保持 `VERIFY`，不可替代真实平板系统文件管理器、Android sharesheet、OEM DocumentsUI 或第三方文件 App
- 2026-06-25 UX-02 已从“`adb shell input` 无法双指”推进到 phase2 AndroidTest 真双指 runtime smoke；`ReaderPinchRuntimeSmokeTest` 在 `emulator-5554` 上通过 direct `MotionEvent` 注入拿到 `/tmp/readflow-ux02-stage-20260625/ux02-runtime-smoke`，确认 TXT 字号 preview+持久化与 PDF matrix zoom 都真正生效。当前仍只能记 AVD/`VERIFY`，不可替代真实手机/平板 pinch ergonomics 与视觉验收
- 2026-06-25 UX-03/UX-04 已从“AVD 点点看得到控件/能切模式”推进到 phase2 AndroidTest grouped runtime smoke；`ReaderModeControlsRuntimeSmokeTest` 在 `emulator-5554` 上拿到 `/tmp/readflow-ux03-ux04-stage-20260625/ux03-ux04-runtime-smoke`，确认 TXT scroll/paged 往返时可见段落范围与 Room `ByteOffset` locator 保持稳定，且 `排版` / `主题` 控制会真实驱动 `16sp`、`1.75x`、`ThemeMode.DARK` 与夜间 palette 切换。当前仍只能记 AVD/`VERIFY`，不可替代真实手机/平板 UI、TalkBack 焦点/语音遍历和多尺寸视觉验收
- 2026-06-27 UX-03 tablet-like follow-up 已补：在 `1600x2560` / density `320` AVD override 下，旧实现会因 stale `_currentLocator` 导致 scroll 可见中段后切 paged 回到 `paragraph 000`；修复后 TXT `setMode()` 采样 viewport 中心可见 paragraph 并请求对应分页。验证包括 TXT engine 单测、UX03 targeted/full runtime、PAGE-05 EPUB runtime 回归和 `:app:assembleDebug`；证据在 `/tmp/readflow-ux03-fixed-20260627/ux03-ux04-runtime-smoke`。边界仍是 AVD，不是真实平板或 TalkBack speech
- 2026-06-26 UX-05/PAGE-01~04 已从旧的 `adb input keyevent`/坐标 smoke 推进到 phase2 AndroidTest grouped runtime smoke；`ReaderPagedKeyboardRuntimeSmokeTest` 在 `emulator-5554` 上拿到 `/tmp/readflow-ux05-page-stage-20260626/ux05-page-runtime-smoke`，确认 paged reader 无障碍 action、键盘翻页、默认 CURL host、FADE/SLIDE transformer runtime 映射，以及 PDF 翻页 zoom reset 都在真实运行态生效。当前仍只能记 AVD：UX-05 保持 `PARTIAL`，PAGE-01~04 保持 `VERIFY`，不可替代真实硬件键盘/D-pad、TalkBack、真机/平板视觉动效与帧率验收
- 2026-06-26 EPUB-04/05/06 已从 corpus/JVM gate 推进到 phase2 AndroidTest grouped runtime smoke；`EpubContinuousRuntimeSmokeTest` 在 `emulator-5554` 上拿到 `/tmp/readflow-epub04-06-stage-20260626/epub04-06-runtime-smoke`，确认连续模式 image-only cover 与 inline image 渲染、外链 `ACTION_VIEW` 拦截 + 内链定位持久化，以及 fixed-layout fallback 的 heading/table/pre/blockquote/list/unknown block 降级都在真实 reader route 上成立。当前仍只能记 AVD：`EPUB-04/05/06` 保持 `VERIFY`，不可替代真机/平板视觉、TalkBack link traversal/speech、外链 chooser/browser、pre 横向滚动手感和性能验收
- 2026-06-22 STATE-04 已补 AVD evidence；`device_id` 稳定性只能记 `VERIFY`，不可替代物理设备首次安装/升级/重启验收
- 2026-06-22 STATE-05 已明确 `DEFERRED`；当前纯阅读回填不实现 KorroSync/KoSync/WebDAV 真实后端，Settings 通过 `SyncBackend.isAvailable` 显示 `远程同步未启用` 和本机保存说明，避免 no-op 被误认为已远程同步
- 2026-06-24 READ-03 已从 TXT-only 扩展到 MD/EPUB DB-backed bookmark lifecycle evidence；旧 TXT `/tmp/readflow-read03-bookmark-smoke` 之外，`/tmp/readflow-read-interaction-batch2/md_bookmark_db` 与 `/tmp/readflow-read-interaction-batch2/epub_bookmark_db` 覆盖 active `书签 0%`、progress `0.0 -> 0.3169819116592407 -> 0.0`（MD）/ `0.0 -> 0.2119981050491333 -> 0.0`（EPUB）回跳，以及删除后 Room tombstone `isDeleted=1`；仍只能记 `VERIFY`，不可替代真机/平板、TalkBack 和真实 sync backend 验收
- 2026-06-24 READ-04 已补 EPUB + MD 非 TXT selection evidence；`/tmp/readflow-read-interaction-batch2/epub_annotation_db` 覆盖 `Chapter 1 paragraph 077` 附近长按后浮层出现 `笔记` / `取消` / `高亮` / `保存`，并把选区锚到 `Section(spineIndex=0, elementIndex=78, charOffset=8094..8101)`；MD `/tmp/readflow-md-runtime-fix4-20260624-205642` 与 `/tmp/readflow-md-runtime-fix5-20260624-2115` 证明 `readflow-search-long.md` 经 `TOC -> Search Target -> 长按 -> 保存` 后，新标注 anchor 稳定落在 near-tail `Section(spineIndex=0, elementIndex=366, charOffset=21936..21945)`，不再漂回文首。仍只能记 `VERIFY`，不可替代真机/平板、TalkBack/action-mode 和 EPUB link coexistence 验收
- 2026-06-24 TXT READ-05 已补 AVD instrumentation closure；手动 `ACTION_VIEW` `/tmp/readflow-read05-manual.xml` 已先证明 FileProvider TXT 可直达 reader，随后 phase2 `TxtRead05NoteSaveSmokeTest` 修正为按空行分段、等待 reader 根节点和 `EditText` 自身值，并在严格串行 `adb shell am instrument -w -e class dev.readflow.read05.TxtRead05NoteSaveSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` 下通过。日志见 `/tmp/readflow-read05-note-save-instrument-pass.txt`；DB 快照 `/tmp/readflow-read05-note-save-pass-db/readflow.db` 可查询最新 active `TXT-NOTE-*` rows，`selectedText=anchor`、`anchorType=text-selection-range`、`totalProgression=0.0843881890177727`。该 smoke 同时校验了保存后 selection actions dismiss、Room `text_annotations.note` 非空，以及 reader chrome -> `标注` 面板可见 note；仍只能记 `VERIFY`，不可替代真机/平板、TalkBack/action-mode 和导出/同步验收
- 2026-06-24 READ-05 已补 EPUB + MD 非 TXT highlight/jump evidence；`/tmp/readflow-read-interaction-batch2/epub_annotation_db` 覆盖高亮后 `text_annotations.selectedText=Chapter`、`totalProgression=0.24002134799957275`、移走到 `0.3243579864501953` 后再从标注列表跳回相同 locator/progression；MD `/tmp/readflow-md-runtime-fix4-20260624-205642`、`/tmp/readflow-md-runtime-fix4b-20260624-210955`、`/tmp/readflow-md-runtime-fix5-20260624-2115` 证明 save 后 `reading_progress` 保持 near-tail `Section(spineIndex=0, elementIndex=362, charOffset=21823)`、`totalProgression≈0.9173182`，移走到 `Section(spineIndex=0, elementIndex=304, charOffset=18311)`、`0.76969314` 后再从标注列表跳回选区锚 `Section(spineIndex=0, elementIndex=366, charOffset=21936)`、`0.9220681`。仍只能记 `VERIFY`，不可替代真机/平板、TalkBack/action-mode、MD/EPUB 和备份/同步导出验收
- 2026-06-24 晚 MD READ-04/05 closure：root cause 已从旧的 offset 混用与 long-press refresh 问题继续收敛到“保存后清理 native selection 时不能覆写视口/locator”。实现上新增 `SelectionAwareTextView.clearNativeTextSelection()`，在移除原生 `Spannable` selection 时保留祖先 `ScrollView` scroll；`MarkdownEngine.clearTextSelection()` 则在清空 engine/native selection 时抑制内部 locator overwrite，并按当前 near-tail 视口重算 locator。Validation: RED/GREEN `SelectionAwareTextViewTest.clear native text selection preserves scroll and removes native selection`、`MarkdownEngineTest.clearTextSelection preserves markdown scroll and locator near current section`，以及 `./gradlew -Preadflow.phase=2 :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.SelectionAwareTextViewTest" :render:md:testDebugUnitTest --tests "dev.readflow.render.md.MarkdownEngineTest" :render:api:compileDebugKotlin :app:assembleDebug` GREEN。AVD 证据 `/tmp/readflow-md-runtime-fix4-20260624-205642`、`/tmp/readflow-md-runtime-fix4b-20260624-210955`、`/tmp/readflow-md-runtime-fix5-20260624-2115` 现已证明 `TOC -> Search Target -> 长按 -> 保存 -> 移走 -> 标注列表跳回` 全链路稳定，不再回到文首；仍只能记 grouped runtime `VERIFY`，不可替代真机/平板与 TalkBack/action-mode 验收
- 2026-06-24 READ-01/02 已补 AVD 多格式搜索 evidence；`/tmp/readflow-read-multiformat-smoke` 覆盖 PDF unsupported UI（`当前格式暂不支持搜索` + `第 1 页，共 50 页`）、EPUB `EPUBSEARCH042 -> 结果 1 · 94%` 并跳到 chapter 4 token、MD `MDSEARCH042 -> 结果 1 · 92%` 并跳到 near-tail token。当前 AVD 的 MediaStore `ACTION_VIEW` smoke 需显式 `--grant-read-uri-permission`，否则会出现 `dev.readflow has no access to content://media/...` 假阴性。三次运行 pid 存活且 crash/ANR grep 为空；仍只能记 `VERIFY`，不可替代真机/平板、TalkBack speech 和真实语料延迟验收
- 2026-06-25 A-02 performance follow-up 已补阶段性 closure：TXT reader 现在在打开 app-private `file://` 已导入书籍时直接复用原文件，不再额外复制到 `readflow-txt-*` temp file；新增 `TxtVirtualPagerEngineTest.openBook reuses app private file uri without creating a temp txt copy` 守住这条路径。PDF paged path 则新增 `PdfPageBitmapStore`，把 `createPageView()` 的同步整页 render 改成串行异步 load + 首屏预热 + 邻页预取，并补 `PdfPageBitmapStoreTest` 守住 coalescing/cancel/release 语义。Validation: `./gradlew :render:txt:testDebugUnitTest --tests "dev.readflow.render.txt.TxtVirtualPagerEngineTest" --rerun-tasks`、`./gradlew :render:pdf:testDebugUnitTest --tests "dev.readflow.render.pdf.PdfPageBitmapCacheTest" --tests "dev.readflow.render.pdf.PdfPageBitmapStoreTest" --tests "dev.readflow.render.pdf.PdfBitmapAttachmentRegistryTest" --rerun-tasks`、`./gradlew -Preadflow.phase=2 :render:txt:testDebugUnitTest :render:pdf:testDebugUnitTest :features:reader:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:pdf:compileDebugKotlin :render:txt:compileDebugKotlin :app:assembleDebug`、`./gradlew -Preadflow.phase=2 :app:assembleOta`、`git diff --check -- android/render/pdf/build.gradle.kts android/render/pdf/src/main/kotlin/dev/readflow/render/pdf/PdfPageBitmapCache.kt android/render/pdf/src/main/kotlin/dev/readflow/render/pdf/PdfPageBitmapStore.kt android/render/pdf/src/main/kotlin/dev/readflow/render/pdf/PdfRendererEngine.kt android/render/pdf/src/test/kotlin/dev/readflow/render/pdf/PdfPageBitmapStoreTest.kt android/render/txt/src/main/kotlin/dev/readflow/render/txt/TxtDocument.kt android/render/txt/src/main/kotlin/dev/readflow/render/txt/TxtVirtualPagerEngine.kt android/render/txt/src/test/kotlin/dev/readflow/render/txt/TxtVirtualPagerEngineTest.kt` 全绿。AVD spot-check 证据在 `/tmp/readflow-a02-stage-20260625`：TXT `ACTION_VIEW` 冷启代理首轮 `2089ms`、后续 `1047ms/1353ms`，说明 duplicate-copy removal 还没把 A-02 拉进预算；PDF 若在 reader 稳定进入 `第 1 页，共 50 页` 后再 reset `gfxinfo` 并做两次翻页，结果为 `69 frames / 28.99% jank / p90 61ms / p95 73ms / p99 97ms`，较旧的 `87 frames / 64.37% jank / p90 65ms` 有阶段改善，但仍未过 `<50ms` frame-paced gate，因此 A-02 继续保持 `PARTIAL`
- 2026-06-25 A-03 accessibility follow-up 已补阶段性 closure：reader/library 的 TalkBack 语义这轮又收了一次。新增 `ReaderAccessibilityLabels.kt` 与 `ReaderAccessibilityLabelsTest`，把 reader 进度/章节/搜索结果/书签/标注/TOC 文案收口为稳定可测试标签；`ReaderScreen` 现在给全书进度条补 `contentDescription="全书进度"` + percent `stateDescription`，章节中区改为 `mergeDescendants` 的整块朗读（如 `正文，第 1 / 1 章，本章进度 0%`），排版面板 slider 补 `字号` / `行距` 语义，底部快捷按钮改为容器合并语义并移除 icon 层重复 label；`BookGrid.kt` 菜单按钮则细化为 `<书名> 的菜单` / `书组 <名称> 的菜单`。Validation: `./gradlew :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.ReaderAccessibilityLabelsTest" --tests "dev.readflow.features.reader.ReaderBookmarkStateTest" --tests "dev.readflow.features.reader.ReaderAnnotationStateTest" --tests "dev.readflow.features.reader.ReaderSearchStateTest"`、`./gradlew :core:ui:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug -Preadflow.phase=2`、`./gradlew -Preadflow.phase=2 :app:installDebug` 全绿；新 AVD 证据 `/tmp/readflow-a03-stage-20260625/library.xml`、`reader-chrome.xml`、`panel-font.xml` 显示书卡已读作 `打开 readflow-perf-1mb` / `readflow-perf-1mb 的菜单`，reader chrome 已暴露 `全书进度`、整块章节进度文案，以及底部 `目录/搜索/书签/标注/排版/主题` 容器语义，排版 slider 也已出现 `字号` / `行距`。边界：新一轮 `accessibility-enabled.txt` 抓取时机偏早，只证明服务已绑定但没有再次抓到 `touchExplorationEnabled=true`；因此仍以旧 `/tmp/readflow-a03-full-traversal/accessibility-enabled.txt` 作为 TalkBack touch exploration 已开启的基线证明。本项依旧只有 AVD XML/screenshot 证据，没有物理手机/平板与真人语音遍历，所以 A-03 继续保持 `PARTIAL`
- 2026-06-25 UX-01 tap-zone follow-up 已补实现收口：这轮发现 UX-01 不只是“差真机”，连续滚动宿主 `NoTransitionHost.next()/previous()` 实际会按 locator 推进，因此旧的 reader 左右 tap-zone 在 TXT/MD/EPUB scroll 模式里也可能偷偷触发段落跳转，不符合来源表“scroll formats do not lose natural scrolling”。修复后，`readerTapZoneForTap()` 新增 `pagedTapZonesEnabled` 边界，连续滚动格式只保留中间区 `ToggleChrome`，左右区不再触发 `PreviousPage/NextPage`；`ReaderTapContainer` 会按 `pagingKind == PAGED` 才开启左右 tap-zone 和无障碍 `上一页/下一页` action，连续滚动只保留点击显示/隐藏工具栏。Validation: `./gradlew :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.ReaderTapZoneTest"`、`./gradlew :features:reader:compileDebugKotlin -Preadflow.phase=2`、`./gradlew -Preadflow.phase=2 :app:installDebug` 全绿；新增 AVD smoke `/tmp/readflow-ux01-stage-20260625` 先把 1MB TXT 滚到中段 `baseline_first_line=000045`，随后左点、右点、中点连续验证，`summary.txt` 记录 `left_first_line=000045`、`right_first_line=000045`、`center_first_line=000045`、`center_has_chrome=True`，说明连续滚动 TXT 已收口为“左右点不跳段，中间点开 chrome”。边界：当前仍只有 AVD synthetic tap 证据，分页格式的人手 ergonomics、平板布局和真实手指容错仍缺，因此 UX-01 继续保持 `PARTIAL`，但代码层缺口已收掉，后续主要剩真机/平板验收而不是再改 tap-zone 逻辑
- 2026-06-24 TOC 阶段已补 grouped AVD runtime evidence：MD `/tmp/readflow-toc-batch8-md` 证明 `readflow-search-long.md` 从 TOC `Search Target` 可跳到 near-tail `MDSEARCH042` 区域，Room 写入 `Section(spineIndex=0, elementIndex=362, charOffset=21820)`、`totalProgression=0.917192101478577`；PDF `/tmp/readflow-toc-batch10-pdf-real` 与 `/tmp/readflow-toc-batch11-pdf-real-late` 证明 `react_2210_03629_outline|PDF` 的 TOC 不再回退到页列表，且点击 `3.3 Results and Observations` 会从 `第 1 页，共 33 页` 跳到 `第 5 页，共 33 页`，Room 写入 `Page index=4/33`、`totalProgression=0.121212124824524`；EPUB `/tmp/readflow-toc-batch12-epub-real` 证明 `readflow-search|EPUB` 从 baseline `Chapter 1` 进入 TOC 并点击 `Chapter 4` 后，正文切到 `Chapter 4 paragraph 000..007`，Room 写入 `Section(spineIndex=3, elementIndex=243, charOffset=0)`、`totalProgression=0.748087286949158`。当前仍只能记 AVD synthetic runtime `VERIFY`，不可替代真机/平板、TalkBack traversal 或真人 clean tap 验收
- 2026-06-24 EPUB-05/06 已补 AVD runtime link smoke；外链方面，`/tmp/readflow-epub-runtime-batch6/external-firstline-scan-20260624-135713` 证明旧历史探针 `x=430,y=250` 在当前布局已不再命中链接带，但 `/tmp/readflow-epub-runtime-batch6/external-y-scan-20260624-140352/x_430_y_270` 成功离开 `dev.readflow` 并进入 `com.android.chrome/org.chromium.chrome.browser.firstrun.FirstRunActivity`，说明外链 runtime 已通，只是验收点需改到当前有效 hit band。内链方面，`/tmp/readflow-epub-runtime-batch7/ch4-open-centered-20260624-142754` 证明 TOC 可进入 `Chapter 4 / Index Style and Format`，随后 `/tmp/readflow-epub-runtime-batch7/internal-link-tap-20260624-143027` 从 chapter 4 首屏 note 中点 `Chapter 3` 后仍停留 `dev.readflow` 且标题变为 `Chapter 3 / Characteristics of a Good Index`；同次 injected tap 还带出 `笔记` / `取消` / `高亮` / `保存` 浮层，因此这组证据只能记 AVD runtime navigation `VERIFY`，不可替代真机 clean tap、TalkBack link traversal 或 selection/link coexistence UX 验收
- 2026-06-23 TXT-01/02/03/04 已补 JVM/local corpus evidence；`TxtDocumentTest` 覆盖大 UTF-8 语料跨 block indexing/search/cache round-trip、GB18030 四字节字符边界 normalization、UTF-8/GBK/Shift_JIS search 与 selection ByteOffset 字符起点。TXT targeted/all tests 与 phase2 组合构建通过；四项仍保持 `VERIFY`，不可替代真实手机/平板导入、PSS/首开/滚动、恢复和搜索/选择 UI smoke
- 2026-06-23 PDF-02 已补 JVM synthetic PDF evidence；`PdfOutlineParser` 支持 catalog `/Names /Dests` name tree 与旧式 catalog `/Dests` dictionary named destination，`PdfOutlineParserTest` 覆盖 outline `/Dest (chapter-two)`、GoTo `/D /chapter-one`、direct destination array 与 indirect `/D [...]` destination object 解析到预期页码。PDF targeted/all tests 与 phase2 组合构建通过；`PDF-02` 仍保持 `VERIFY`，不可替代真实 outline PDF corpus、物理手机/平板 TOC 跳转、xref/object streams、大 PDF scan cap 和更广 named destination 形态验收
- 2026-06-23 MD-01 已补 JVM/compile evidence；`MarkdownDocument` 现在提供源码 offset ↔ rendered text offset 映射，`MarkdownEngine` 在滚动 locator、goTo、selection 和 highlight range 中使用同一映射。`MarkdownDocumentTest` 覆盖 `**bold**` 与 `[link](url)` 渲染剥离语法后的选择/高亮锚点；MD targeted/all tests 与 phase2 组合构建通过；`MD-01` 仍保持 `VERIFY`，不可替代真实设备/平板 MD save-restore、TOC jump、长文滚动和复杂 Markdown 渲染选择验收
- 2026-06-22 SRC-05 已补 JVM evidence；`DownloadedBookCachePlannerTest` / `DownloadedBookCacheTest` 覆盖 LRU、未读最旧、protected book、本地导入排除、文件删除与 row 清理；`SRC-05` 从 `TODO` 推进到 `VERIFY`，仍不可替代真实 Calibre + 真机/平板离线缓存 smoke
- 2026-06-22 SRC-06 已补 JVM/compile evidence；`DownloadedBookCacheTest` 覆盖手动移除远程下载资产、保留书架 row、本地导入排除和 Repository 委托，`LibraryViewModelTest` 覆盖移除动作接线；`SRC-06` 从 `TODO` 推进到 `VERIFY`，仍不可替代真实 Calibre + 真机/平板下载/移除 smoke
- 2026-06-22 SRC-07 已补 JVM/compile evidence；`LibraryViewModelTest` 覆盖离线筛选只显示本地可读书，以及 Bundle 只保留离线成员；`LibraryScreen` 暴露 `全部` / `离线可读` chips 和离线空状态；`SRC-07` 从 `TODO` 推进到 `VERIFY`，仍不可替代真实 Calibre source unavailable + 真机/平板离线打开 smoke
- 2026-06-25 SRC-01~04/06/07 已补 grouped AVD/runtime evidence；新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src01/CalibreGroupedRuntimeSmokeTest.kt`，通过 `@FixMethodOrder(MethodSorters.NAME_ASCENDING)` 和同进程一次性 reset 稳定串起 `step01_settingsShowsFailureGuidanceThenProbeFindsHostCalibre` / `step02_downloadsRemoteEpubThenOpensOfflineAndRemovesDownloadedAsset`。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 8081`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src01.CalibreGroupedRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。运行时证据 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke` 证明：坏端口 `http://10.0.2.2:8090` 会显示 `无法连接到服务器` / `确认手机和 Calibre 在同一局域网，并检查端口是否为 8080`；host hint `10.0.2.2` 可探测到 `10.0.2.2:8081`；Calibre 搜索/下载会把 `calibre-42` 写成 `DOWNLOADED + file:/data/user/0/dev.readflow/files/books/calibre-42.epub`；关闭 fake server 后 `离线可读` 仍可打开该书；移除下载后 DB 回到 `NOT_DOWNLOADED + localUri=NULL`，磁盘文件删除且离线空状态出现。边界：仍只能记 AVD + fake host server runtime `VERIFY`，不可替代真实 Calibre Content Server、认证服务器、广域 LAN IP 组合、物理手机/平板和 TalkBack 验收
- 2026-06-25 SRC-05 已补 grouped AVD/runtime evidence；新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src05/CalibreCacheLruRuntimeSmokeTest.kt`，复用扩展后的 fake Calibre server 下载 `calibre-101..106` 共 6 本远程 EPUB，并在前 4 本打开后等待 `reading_progress` / `books.lastReadAt` 落库，再由第 6 本下载触发 trim。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 8081`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src05.CalibreCacheLruRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。运行时证据 `/tmp/readflow-src05-lru-runtime-20260625-final/calibre-lru-runtime-smoke` 显示：`lru-summary.txt` 记录 `evictedBookId=calibre-105`、`evictedStatus=NOT_DOWNLOADED`、`evictedFileExistsAfterTrim=false`、`protectedBookId=calibre-106`、`keptBooks=calibre-101,calibre-102,calibre-103,calibre-104,calibre-106`、`offlineFilterLabel=离线可读 5`；`after-lru-trim-readflow.db` 可查询 `calibre-105|LRU Remote 05|NOT_DOWNLOADED||` 与 `calibre-106|LRU Remote 06|DOWNLOADED|file:/data/user/0/dev.readflow/files/books/calibre-106.epub|`；`reading_progress` 中 `calibre-101..104` 的 `updatedAt` 单调递增，证明当前 AVD/runtime 下最近阅读顺序确实驱动了淘汰。关闭 fake server 后仍可在离线筛选里滚动找到并打开 `LRU Remote 06`，命中段落 `LRU runtime paragraph 06 proves cached remote reading after download.`。边界：仍只能记 AVD + fake host server runtime `VERIFY`，不可替代真实 Calibre Content Server、认证服务器、真实 LAN/低存储、大文件、多格式混合或物理手机/平板验收
- 2026-06-23 SRC-08 已补 JVM/compile evidence；`LinReadsBackupExporterTest` 覆盖 ZIP `manifest.json` 包含 `reading_progress` / `bookmarks` / `text_annotations`，`SettingsViewModelTest` 覆盖导出成功计数和失败文案，phase2/phase1 构建通过；`SRC-08` 从 `TODO` 推进到 `VERIFY`，仍不可替代真实手机/平板 SAF 导出、手动 ZIP 核验和 SRC-09 恢复验收
- 2026-06-24 SRC-09 已补 JVM/schema evidence；`LinReadsBackupRestorer` 从 ZIP `manifest.json` 恢复 `reading_progress` / `bookmarks` / `text_annotations`，按 `updatedAt` LWW 合并、保留 tombstone 且不触碰 `books` 表；恢复 schema 现在只接受显式 `schema_version=1`，`LinReadsBackupExporterTest.restoreRejectsSchemaVersionsBeforeOneWithoutMutatingLocalData` / `restoreRejectsMissingSchemaVersionWithoutMutatingLocalData` 覆盖非法旧版本和缺失 schema 拒绝且不写入本地数据；`LinReadsBackupExporterTest`、`SettingsViewModelTest`、phase2/phase1 构建通过；`SRC-09` 保持 `VERIFY`，仍不可替代真实手机/平板 SAF 恢复、实际导出文件手动核验和跨设备 round-trip
- 2026-06-24 晚 SRC-08/SRC-09 已补 AVD/runtime evidence；新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src08/BackupExportRestoreRuntimeSmokeTest.kt`，在 `emulator-5554` 上完成真实 ZIP 导出与 restore round-trip。导出 ZIP `/tmp/readflow-src08-src09-backup-runtime/linreads-backup-runtime.zip` 的 `manifest.json` 计数为 `reading_progress=2`、`bookmarks=2`、`text_annotations=1`、`assets=0`；恢复后目标 DB `/tmp/readflow-src08-src09-backup-runtime/backup-runtime-target.db` 保留 `books.local-shelf-book`，同时写入 `new-book` 进度和 `new-annotation`，并让较新的本地 `existing-book` / `existing-bookmark` 赢过旧备份；远端删除书签 `new-bookmark` tombstone 也被保留。缺失 `schema_version` 的坏 ZIP `/tmp/readflow-src08-src09-backup-runtime/linreads-backup-invalid-missing-schema.zip` 会被直接拒绝且不污染本地 DB。该组现在可记 grouped AVD/runtime `VERIFY`，但仍不可替代真实手机/平板 SAF 文档选择器、手动 ZIP 核验、跨真机 round-trip 和大数据量/损坏文件验收
- 2026-06-26 SRC-08/SRC-09 SAF UI runtime follow-up：新增测试 APK provider `android/app/src/androidTest/java/dev/readflow/SafDocumentProvider.java` 与 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src08/BackupSafUiRuntimeSmokeTest.kt`，从 Settings 可见入口点击 `导出备份` / `恢复备份`，拦截 `ACTION_CREATE_DOCUMENT` / `ACTION_OPEN_DOCUMENT` 并返回测试 SAF Uri。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest`；`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src08.BackupSafUiRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。证据 `/tmp/readflow-src08-src09-saf-ui-runtime-20260626/backup-saf-ui-runtime-smoke/backup-saf-ui-summary.txt` 记录 export/restore monitor 均命中 1 次，导出 ZIP `manifest_format=linreads-backup`、`manifest_schema=1`、`manifest_counts=progress:1,bookmarks:1,annotations:1`，恢复后 Room 回到较新的进度、删除态书签和标注。边界：这是 AVD instrumentation + test ContentProvider SAF Uri，不是真实平板 DocumentsUI 手动选择或跨设备恢复；`SRC-08/SRC-09` 保持 `VERIFY`
- 2026-06-23 OPT-01/02/03 已收口；`docs/audit/adr-mupdf-license-2026-06-23.md` 明确 MuPDF AGPL/商业授权 gate，base APK 不引入 MuPDF，DOCX/CBZ optional engines 在当前纯阅读回填中延期；`OPT-01` 标记 `DONE`，`OPT-02`/`OPT-03` 标记 `DEFERRED`
- 2026-06-23 PAGE-05 已从 EPUB 段落页推进到 StaticLayout/measured-line 辅助的段内 TextView 页切片；`EpubPageMetrics` / `epubViewportPagedLayout` 会按视口、字号、行高和平均字宽估算 page size，follow-up 新增 `EpubLazyBook.cachedParagraphAt()`，runtime paged slice builder 可使用已缓存 lazy spine 正文做词/空白边界切分且不为分页加载冷 spine；`EpubReflowEngine.buildPagedSlices()` 调用 `epubStaticLayoutPagedLayout()`，在有缓存正文时用 Android `StaticLayout` 测量真实 visual line ranges，再按 viewport-derived `linesPerPage` 合并为 page slices，空/冷文本仍回退到 indexed/viewport slicing。图片 follow-up 增加 `EpubPageSliceKind.Image` 和 `EpubLazyBook.cachedBlocks()`，cached spine 内的图片块会作为独立 page slice 渲染为 paged `ImageView`，且不会为了分页加载冷 spine。图片 locator follow-up 将 image slice 锚到关联段落末尾，并让 `epubPageIndexFromLocator()` 优先匹配零宽图片页锚点，避免恢复图片页时落回前一个文本页。文本样式 follow-up 增加 `EpubPageTextStyle`，cached `EpubDisplayBlock.Text` 的 heading/pre/table/list/blockquote/indent 元数据会透传到 paged text slice，并在 `EpubReflowEngine.createPageView()` 中复用标题加粗/字号、等宽、缩进和 blockquote padding。样式感知测量 follow-up 让 `epubMeasuredPagedLayout()` / `epubPagedLayoutWithBlocks()` 把 `EpubPageTextStyle` 传给 line breaker，runtime `StaticLayout` 会用标题字号/粗体和 pre/table 等宽字体对应的 `TextPaint` 测量 visual line ranges。最新 Compose/TextLayoutResult follow-up 新增 `epubComposeMeasuredPagedLayout()`、`EpubTextLayoutLineRange` 和 `EpubPageMeasurement.ComposeTextLayoutResult`，分页核心可消费 TextLayoutResult 风格 visual line ranges 并保留 measurement 来源；fallback follow-up 让 TextLayoutResult-style line ranges 为空时生成的完整文本页也保留 `ComposeTextLayoutResult` measurement 与 `EpubPageTextStyle`。`EpubPageMappingTest.static layout paged layout follows measured visual line breaks`、`paged layout inserts cached image blocks as standalone slices`、`image page slice maps to a distinct section offset after preceding text`、`paged layout preserves cached text block style metadata`、`paged layout measures cached text blocks with their style metadata`、`compose text layout result line ranges are preserved as compose measured page slices`、`compose measured pagination keeps measurement and style when line ranges are empty`、`EpubLazyBookTest.cached block lookup does not load cold spines for paged image slices`、EPUB 全量单测和 phase2 组合构建通过，但仍不是 runtime `ComposeView` 真分页，无真机视觉/性能证据，继续保持 `PARTIAL`
- 2026-06-25 PAGE-05 real-corpus paged gate 已补阶段性证据：`EpubCorpusSmokeTest` 新增 `real epub corpus preserves paged layout coverage and locator round trips`，直接对 `/tmp/readflow-epub-corpus` 5 本公开 EPUB 跑 `epubPagedLayoutWithBlocks()` 的真实分页契约，验证每个 page slice 都能 `epubLocatorForPageSlice()` -> `epubPageIndexFromLocator()` round-trip，text page 保持 `totalProgression` 单调，image page 保留 `href/alt`，page-local links 不越界且仍能命中 targeted internal links，styled text blocks 仍保留 `EpubPageTextStyle`，并要求 measurement 来源维持 `ComposeTextLayoutResult`。Validation: 先 RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubCorpusSmokeTest.real epub corpus preserves paged layout coverage and locator round trips" -Dreadflow.epubCorpusDir=/tmp/readflow-epub-corpus --rerun-tasks`，在 `idpf-childrens-literature.epub` 的 image-only `cover.xhtml` 上暴露“不能要求所有视觉页 `totalProgression` 全局单调”的假设错误；校正 gate 为“所有页 round-trip + text page progression 单调”后 GREEN，同一 targeted run 输出 `EPUB_PAGED_CORPUS|...`：`gutenberg-23950` `pages=9793 linkPages=6 styledPages=29`、`gutenberg-4094` `pages=5967 linkPages=6 styledPages=5720`、`idpf-childrens-literature` `pages=4329 imagePages=1 linkPages=119 targetedInternalLinkPages=92`、`idpf-cole-voyage` `pages=128 imagePages=9 styledPages=7`、`idpf-indexing` `pages=5311 imagePages=11 linkPages=862 targetedInternalLinkPages=959`。后续 `./gradlew :render:epub:testDebugUnitTest -Dreadflow.epubCorpusDir=/tmp/readflow-epub-corpus --rerun-tasks` 与 phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 均通过。边界：这仍是 JVM/real-corpus 契约证据，不是真机 mixed text/image 视觉分页、Compose selection action-mode、TalkBack speech/link traversal、帧率/内存预算或真实用户手势验收，PAGE-05 继续保持 `PARTIAL`
- 2026-06-25 PAGE-05 same-paragraph multi-image locator follow-up 已补入：在 real-corpus gate 之外新增 RED/GREEN `EpubPageMappingTest.multiple image page slices in one paragraph round trip to distinct pages`，暴露“同一段里两张以上图片页会共享段尾锚点，第二张图 `epubLocatorForPageSlice()` 回跳时落回前一张”的真实分页 locator 冲突。修复后，`epubPagedLayoutWithBlocks()` 会为同段 successive image slice 分配稳定且彼此不同的 paragraph-local image anchors（段尾、段尾+1、…），`epubLocatorForOffset()` 则允许图片页 locator 保留该超出正文长度的 synthetic char offset，同时继续把 `totalProgression` 钉在该段正文末尾，避免把图片页伪装成新的文本进度。Validation: RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubPageMappingTest.multiple image page slices in one paragraph round trip to distinct pages" --rerun-tasks` 失败于第二张图回到第一页图片；GREEN 后该 targeted test 与原有 `EpubPageMappingTest.image page slice maps to a distinct section offset after preceding text` 均通过，后续 `./gradlew :render:epub:testDebugUnitTest --rerun-tasks` 与 phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 JVM/code-contract 修复，不是真机 mixed image gallery、TalkBack speech、Compose selection action-mode 或视觉/性能验收，PAGE-05 继续保持 `PARTIAL`
- 2026-06-25 PAGE-05 image-fragment internal-link follow-up 已补入：EPUB fragment target 链路不再只记“段落索引”，而是改为保留 `paragraphIndex + paragraphOffset` 的 `EpubTargetPosition`，让图片 fragment 能复用与 paged image slice 相同的 synthetic image anchor 语义。修复后，`epubFragmentTargetIndexes()` 会为同段图片 fragment 分配段尾、段尾+1…的 paragraph-local image anchors；`EpubParser`/`EpubLazyBook` 会把该 target position 透传进 `fragmentTargetIndexes`；`buildEpubToc(...)` 与 `EpubReflowEngine.goToInternalLink()` 则改为用 `epubLocatorForTarget(...)` 生成带 offset 的 `Section` locator。这样 nav/ncx TOC 和 paged runtime internal link 在命中 `<img id="...">` 这类 fragment 时，不会再回落到前一个文本页，而是能落到真正的图片页。Validation: 新增 RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime internal links can target image fragments" --rerun-tasks`，先暴露 runtime 请求页落在错误文本页；补入低层守护 `EpubLinkTargetsTest.fragment targets preserve synthetic image anchors within one paragraph` 和 `EpubParserTocTest.parseBook and lazy parse map toc image fragments to synthetic image anchors` 后 GREEN。后续 `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubLinkTargetsTest" --tests "dev.readflow.render.epub.EpubParserTocTest" --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime internal links can target image fragments" --rerun-tasks`、`./gradlew :render:epub:testDebugUnitTest --rerun-tasks`、phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 与 `git diff --check -- android/render/epub/... docs/tracking/ACTIVE.md docs/tracking/linreads-architecture-docs-2026-06-18.md` 全部通过。边界：这仍是 JVM/Robolectric/code-contract 证据，不是真机 mixed text/image 内链点击、TalkBack link traversal/speech、Compose selection action-mode、视觉或性能验收，PAGE-05 继续保持 `PARTIAL`
- 2026-06-25 PAGE-05 image-only spine synthetic paragraph closure 已补入：`epubParagraphPlan()` 现在会为 image-only spine 保留稳定 synthetic paragraph slot，把 spine 起始 `Break` 延后绑定到该 spine 首个真实 text/image paragraph，只在纯 trailing-break spine 上落到 synthetic paragraph；`epubPagedLayoutWithBlocks()` 则不再让 `Break` 提前把 paragraph 标成 emitted，避免 image-only cover/leading-break contract 引入后，同段 `Break -> Text` 把正文页吞掉。这样 image-only cover spine 的 TOC / fragment target / runtime open-book / paged page mapping 能共用同一 synthetic paragraph 语义，同时不会在 paged 模式前插空白文本页，也不会因为 leading break 把首个正文页丢掉。Validation: focused RED/GREEN `EpubPageMappingTest.paged layout keeps text page when leading break shares the first paragraph`，并回归 `EpubLinkTargetsTest.fragment targets in image only spine use synthetic spine paragraph`、`EpubPageMappingTest.paged layout does not emit blank synthetic text page before image only spine`、`EpubParserTocTest.parseBook keeps toc entry for image only cover spine before later text spine`、`EpubReflowEngineTest.paged runtime starts on image only cover spine without blank text page`、`EpubReaderItemParserTest.text and heading reader items can feed existing paragraph renderer`；后续 `./gradlew :render:epub:testDebugUnitTest --rerun-tasks`、phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 与 `git diff --check -- android/render/epub/src/main/kotlin/dev/readflow/render/epub/EpubPageMapping.kt android/render/epub/src/test/kotlin/dev/readflow/render/epub/EpubPageMappingTest.kt docs/tracking/ACTIVE.md docs/tracking/linreads-architecture-docs-2026-06-18.md` 全部通过。边界：这仍是 JVM/Robolectric/code-contract 证据，不是真机 mixed cover/text 视觉分页、TalkBack speech/link traversal、Compose selection action-mode 或性能验收，因此 PAGE-05 继续保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose/block measurement follow-up 已补入：`epubPagedLayoutWithBlocks()` 新增 `measurement` 参数并传入 `epubMeasuredPagedLayout()`，图文/样式感知分页路径也可保留 `ComposeTextLayoutResult` 来源。RED/GREEN `EpubPageMappingTest.block paged layout can preserve compose measurement source`、EPUB 全量单测和 phase2 组合构建通过；边界不变，仍不是 runtime `ComposeView` 真分页，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose/block cold-cache fallback follow-up 已补入：当 block/style-aware 分页路径的 cached text 为空、回退 indexed/viewport slicing 时，fallback page slices 现在也保留调用方传入的 `ComposeTextLayoutResult` measurement 和 cached `EpubPageTextStyle`。RED/GREEN `EpubPageMappingTest.block paged layout preserves compose measurement and style when cached text is empty`、EPUB 全量单测和 phase2 组合构建通过；边界不变，仍不是 runtime `ComposeView` 真分页，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 runtime Compose measurement source follow-up 已补入：`EpubReflowEngine` 新增 internal `EpubPageLineMeasurer` 注入点，公开 `EpubReflowEngine(context)` 构造保持不变；runtime `buildPagedSlices()` 通过当前 measurer 把 `ComposeTextLayoutResult` measurement 传入 `epubPagedLayoutWithBlocks()`。Robolectric 覆盖 `openBook()` → `setMode(PAGED)` → `createPageView(0)`，并从 tagged `EpubPageSlice` 断言 measurement 仍为 `ComposeTextLayoutResult`。验证中修复测试自身 `runBlocking` 与 `Dispatchers.Main` 在 Robolectric SDK main thread 上自锁的问题，改为 `runTest` + `Dispatchers.setMain`，并给 `:render:epub` 测试补 `libs.coroutines.test`。targeted `EpubReflowEngineTest.paged runtime can use compose text layout measurement source`、EPUB 全量单测和 phase2 组合构建通过；边界不变，仍不是实际 `ComposeView` / 真实 `TextLayoutResult` 排版分页，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 default runtime Compose TextMeasurer follow-up 已补入：公开 `EpubReflowEngine(context)` 默认 paged measurement 不再停在注入点预接线，而是通过 Compose `TextMeasurer.measure()` 生成真实 `TextLayoutResult` line ranges，并把 `getLineStart()` / `getLineEnd(visibleEnd = true)` 转成 `EpubTextLayoutLineRange`；`TextStyle` 会按当前字号、行高、标题加粗/字号、Serif/Monospace 映射。Robolectric 覆盖 `openBook()` → `setMode(PAGED)` → `createPageView(0)`，断言默认构造产出的 tagged `EpubPageSlice.measurement == ComposeTextLayoutResult`。targeted `EpubReflowEngineTest.paged runtime uses real compose text layout measurement by default`、EPUB 全量单测和 phase2 组合构建通过；边界不变，仍不是 per-page `ComposeView` 视觉分页，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 ComposeView host follow-up 已补入：`:render:epub` 启用 Compose 编译，EPUB paged 文本页的外层宿主从纯 `FrameLayout + SelectionAwareTextView` 推进为 `ComposeView`，并用 `AndroidView` 临时承载既有 `SelectionAwareTextView`，以保留已验证的 selection/highlight path。RED/GREEN `EpubReflowEngineTest.paged runtime renders text pages with compose view host`、EPUB 全量单测和 phase2 组合构建通过；边界不变，内部文本仍不是纯 Compose `Text` / selection 实现，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 ComposeView root follow-up 已补入：EPUB paged 文本页 `createPageView()` 现在直接返回 `ComposeView` 根节点，根节点持有 page slice tag、页码 `contentDescription` 和页面背景；`setContent { AndroidView(...) }` 仍临时承载既有 `SelectionAwareTextView`，保留 selection/highlight callback。RED `EpubReflowEngineTest.paged runtime returns compose view as text page root` 失败于根节点仍非 `ComposeView`，GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界不变，文本仍不是纯 Compose `Text` / selection，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose text surface follow-up 已补入：`:render:epub` 增加 `compose.foundation`，paged 文本页 `ComposeView.setContent` 现在包含透明 `BasicText` 文本面，并继续用 `AndroidView(SelectionAwareTextView)` 保留现有 selection/highlight path；`ComposeView` 通过 `R.id.epub_compose_text_surface` keyed tag 暴露 page text 供 Robolectric 验证。RED `EpubReflowEngineTest.paged runtime exposes compose text surface state` 失败于 tag 为空，GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界不变，Compose 文本面为非交互/透明层，真实可见文本、选择和高亮仍由 Android TextView 承担，无真机视觉/性能证据，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose visible text/highlight follow-up 已补入：paged 文本页从透明 Compose 文本面推进到可见 Compose `BasicText` 渲染层，分页高亮 ranges 迁到 Compose `AnnotatedString` 背景样式；`SelectionAwareTextView` 继续作为透明 `AndroidView` 覆盖层保留长按选择 callback；字号、行距、主题和标注刷新会重新绑定活跃分页页的 Compose 文本层。RED `EpubReflowEngineTest.paged runtime makes compose text visible while selection overlay stays non visual` 失败于可见状态 tag 为空，RED `EpubReflowEngineTest.paged runtime exposes compose text highlight ranges` 失败于高亮 ranges tag 为空；GREEN 后两个 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：可见文本和高亮已由 Compose 承担，但 selection 仍不是纯 Compose selection，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose selection state follow-up 已补入：paged 文本页会把透明 `SelectionAwareTextView` overlay 产生的 page-local selection range 同步到 `ComposeView` keyed tag，并在 `clearTextSelection()` 时清空活跃 Compose 页 selection range。RED `EpubReflowEngineTest.paged runtime mirrors overlay selection range into compose state` 失败于 overlay view tag 为空，RED `EpubReflowEngineTest.paged runtime clears compose selection range with engine selection` 失败于 stale selection range 未清理；GREEN 后两个 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：selection 状态已有 Compose 可观察落点，但手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose text style/layout follow-up 已补入：paged 文本页会把 `EpubPageTextStyle` 与 `EpubComposeTextLayout` 暴露到 `ComposeView` keyed tags，并让可见 Compose `BasicText` 使用正文 padding、indent、blockquote 额外缩进和 preformatted 横向滚动。RED `EpubReflowEngineTest.paged runtime exposes compose text style and layout state` 在补最小 layout 类型后失败于 style tag 为空；GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：可见文本/高亮/样式布局已有 Compose 落点，但手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose selection highlight follow-up 已补入：paged 文本页会把透明 `SelectionAwareTextView` overlay 产生的 page-local selection range 转成 `ReaderTextHighlightRange`，写入 `R.id.epub_compose_text_selection_highlight_range` 并同步到 Compose state；`EpubComposeTextPage` 会把该 selection highlight 追加到 Compose `AnnotatedString` 背景 ranges。RED `EpubReflowEngineTest.paged runtime exposes compose selection highlight range` 失败于 selection highlight tag 为空；GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：selection 高亮状态已有 Compose 渲染落点，但手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose selection container follow-up 已补入：paged 文本页会把可见 Compose `BasicText` 包入 `SelectionContainer`，并通过 `R.id.epub_compose_text_selection_enabled` 暴露 Compose selection container 已启用状态。RED `EpubReflowEngineTest.paged runtime exposes compose text selection container state` 失败于 selection-enabled tag 为空；GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：Compose 文本层已有 selection container 可测落点，但真实手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose selection callback bridge follow-up 已补入：paged `ComposeView` 通过 `R.id.epub_compose_text_selection_callback` 暴露 Compose-side selection 回调桥，回调会复用 overlay selection 更新逻辑，写入 engine `currentTextSelection`、Compose selection range tag 和 Compose selection highlight state；overlay 真实手势路径仍保留。RED `EpubReflowEngineTest.paged runtime exposes compose text selection callback bridge` 失败于 callback tag 为空；GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：这是未来纯 Compose selection 手势的回调落点，不是真实手势/action-mode 迁移，缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose gesture selection wiring follow-up 已补入：`EpubComposeTextPage` 现在保存 `BasicText(onTextLayout)` 的 `TextLayoutResult`，并通过 `pointerInput` / `detectDragGesturesAfterLongPress` 将长按拖选坐标映射为 page-local text offset 后调用 Compose selection callback bridge；`R.id.epub_compose_text_gesture_selection_wired` 暴露接线状态。RED `EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring` 失败于 gesture wiring tag 为空；GREEN 后 targeted、EPUB 全量单测和 phase2 组合构建通过；边界更新：透明 Android overlay 仍保留，真实手势/action-mode 迁移未做真机验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose selection overlay z-order follow-up 已补入：透明 `AndroidView(SelectionAwareTextView)` 仍保留为兼容/状态桥，但在 Compose `Box` 中移到可见 `BasicText`/gesture 层之后，`R.id.epub_selection_overlay_behind_compose_text` 暴露前景层级契约。RED/GREEN `EpubReflowEngineTest.paged runtime keeps selection overlay behind compose gesture text`、EPUB 全量单测和 phase2 组合构建通过；边界更新：这是避免 overlay 在代码层级挡住 Compose gesture 的契约，不是真机已验证的纯 Compose selection/action-mode，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 Compose text semantics / overlay accessibility follow-up 已补入：可见 Compose `BasicText` 不再清空语义，透明 `SelectionAwareTextView` overlay 标记为 `IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS`，`R.id.epub_compose_text_semantics_exposed` / `R.id.epub_selection_overlay_accessibility_hidden` 暴露契约。RED/GREEN `EpubReflowEngineTest.paged runtime exposes compose text semantics instead of transparent overlay accessibility`、EPUB 全量单测和 phase2 组合构建通过；边界更新：这是代码层语义职责切换，不是真机 TalkBack speech/action-mode 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 transparent overlay touch/focus bridge follow-up 已补入：`SelectionAwareTextView.touchSelectionEnabled` 默认 `true`，保持普通 TXT/MD 文本视图可点击、可长按、可聚焦选择；EPUB paged 透明 overlay 在创建和 Compose rebind 时设为 `false`，确保它只作为 selection 状态/兼容桥，不再抢触摸或焦点。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay out of touch and focus after compose text rebind`、TXT 默认行为守护、TXT/EPUB 单测和 phase2 组合构建通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 transparent overlay native selection off follow-up 已补入：`SelectionAwareTextView.nativeTextSelectionEnabled` 默认 `true`，普通 TXT/MD 文本视图仍保持原生选择；EPUB paged 透明 overlay 在创建和 Compose rebind 时设为 `false`，`isTextSelectable=false`，selection 更新继续由 Compose callback bridge 写入 engine/Compose state。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay out of native selection while compose callback selects text`、TXT 默认行为守护、TXT/EPUB 单测和 phase2 组合构建通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 transparent overlay empty bridge follow-up 已补入：EPUB paged 透明 overlay 不再复制整页正文/高亮 spans，创建与标注 rebind 后都保持空文本；可见正文与高亮由 Compose surface/tag 承担，Compose callback selection 仍可写入 engine/Compose state。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay text empty while compose surface owns content after rebind`、TXT/EPUB 单测和 phase2 组合构建通过。边界：这是 Robolectric/code-contract 证据，不是真机视觉、TalkBack speech/action-mode、Compose selection action-mode 或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 PAGE-05 detached selection bridge follow-up 已补入：EPUB paged `EpubComposeTextPage` 不再用 `AndroidView` 承载透明 overlay，Compose UI tree 只保留 `SelectionContainer` / `BasicText` 前景文本；`SelectionAwareTextView` 仅作为未挂载 state/callback bridge 保留在 tag/weak-map。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps selection bridge outside compose tree while compose gesture text owns foreground`、相邻 selection/empty bridge 回归、TXT/EPUB 单测和 phase2 组合构建通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 view-free selection state follow-up 已补入：EPUB paged 文本页不再创建或暴露 `SelectionAwareTextView` bridge；`R.id.epub_selection_overlay_view` 保持 `null`，active page selection/rebind/clear 状态改为 `ComposeView` + page slice state，Compose callback 直接更新 engine selection、Compose selection range 和 selection highlight。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime updates compose selection without selection aware text view bridge`、EPUB 全量单测、TXT 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 active Compose repagination follow-up 已补入：`setLineSpacing()` / `setFontSize()` 重建 `pagedSlices` 后，活跃 paged `ComposeView` 会按 page index 绑定新 `EpubPageSlice`，并清空旧 page-local Compose selection range/highlight 与 engine selection，避免重分页后沿用 stale slice 或 stale selection。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime rebinds active compose page to rebuilt line spacing slice`、`paged runtime clears page local compose selection when line spacing rebuilds slices`、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 active Compose progress semantics follow-up 已补入：重分页 rebind 现在会刷新 `R.id.epub_compose_page_progress_description` 与 API 30+ `stateDescription`，并在 page count 变化时 clamp active page index，避免 TalkBack/无障碍进度语义留在旧总页数。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime updates compose page progress after line spacing rebuilds page count`、相邻重分页 targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack speech/action-mode 或视觉/性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose long-press initial selection follow-up 已补入：paged Compose 文本层在长按起点会按词边界生成 page-local selection range，并立即走 Compose selection callback；空白 offset 不扩展为词选区，但会发 collapsed range 走同一 callback，从而清空旧 engine/Compose selection，避免 stale 高亮残留。Validation: RED/GREEN `EpubSelectionTest.compose initial selection expands long press offset to word range`、`EpubSelectionTest.compose initial selection ignores whitespace offsets`、`EpubSelectionTest.compose initial selection event reports word range with anchor`、`EpubSelectionTest.compose initial selection event reports collapsed range for whitespace`、`EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring`、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose CJK initial selection follow-up 已补入：`epubComposeInitialSelectionRange()` 改为 code point aware；中文/日文假名/韩文等无空格 CJK/Hangul 文本不再把连续字符整段扩成词选区，而是在长按起点返回单个 Unicode code point range，并覆盖 CJK Extension-B surrogate pair；ASCII/拉丁字母、数字、下划线、撇号和连字符仍按词边界扩展，空白 collapsed 清理行为不变。Validation: RED/GREEN `EpubSelectionTest.compose initial selection keeps cjk long press to one character`、RED/GREEN `EpubSelectionTest.compose initial selection keeps cjk surrogate pair to one code point`、相邻 initial selection targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机中日韩文本 selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose callback code point boundary follow-up 已补入：paged Compose selection callback 现在通过 `epubSelectionRangeOnCodePointBoundaries()` 规整 page-local start/end，再更新 engine selection、Compose selection range 和 selection highlight；拖选 focus 落在 CJK Extension-B surrogate pair 低代理项时不再写入半 surrogate range。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps compose callback selection on code point boundaries`、相邻 selection/callback targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 CJK Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose callback combining-mark boundary follow-up 已补入：paged Compose selection callback 在 code point 规整后继续用 `BreakIterator.getCharacterInstance(Locale.ROOT)` 扩到用户可见 text-element/grapheme 边界，decomposed accent / combining mark 不再被选成半个可见字形。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps compose callback selection on combining mark boundaries`、相邻 selection/callback targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose initial combining-mark boundary follow-up 已补入：`epubComposeInitialSelectionRange()` 现在先把长按 hit-test offset 扩到用户可见 text-element/grapheme，再基于 text-element range 做词边界扫描；decomposed accent / combining mark 位于 base letter 或 mark offset 时都会落入完整词选区。Validation: RED/GREEN `EpubSelectionTest.compose initial selection keeps combining mark with word range`、相邻 initial-selection / callback boundary targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose drag selection follow-up 已补入：`epubComposeDragSelectionRange()` 让长按拖选向左/向右扩展时保留初始词，focus 在初始词内时也维持完整初始词 range，空白/collapsed 初始 selection 不生成拖选 range。Validation: RED/GREEN `EpubSelectionTest.compose drag selection keeps initial word when dragging before it`、`EpubSelectionTest` class、相邻 gesture/callback targeted tests、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose outside-text initial selection follow-up 已补入：长按 hit-test offset 位于文本范围外时，`epubComposeInitialSelectionRange()` 不再把 offset clamp 到首/末字符并误选边缘词，而是返回 `null`；`epubComposeInitialSelectionAt()` 产出 collapsed range 与 `anchor=null` 以清理旧 selection。Validation: RED/GREEN `EpubSelectionTest.compose initial selection event reports collapsed range outside text`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose typographic apostrophe initial selection follow-up 已补入：`epubComposeInitialSelectionRange()` 现在把常见 EPUB 排版中的右单引号 `\u2019` 视为拉丁词连接符，`reader\u2019s` 这类单词长按不再被切成 `reader` / `s`。Validation: RED/GREEN `EpubSelectionTest.compose initial selection expands typographic apostrophe word`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose standalone connector punctuation follow-up 已补入：`epubComposeInitialSelectionRange()` 会保留词内部的 `'` / `\u2019` / `-` 连接符，但对独立或位于词边缘的连接符返回 `null`，避免长按分隔符生成无意义选区。Validation: RED/GREEN `EpubSelectionTest.compose initial selection ignores standalone word connector punctuation`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose Unicode hyphen initial selection follow-up 已补入：`epubComposeInitialSelectionRange()` 现在把 Unicode hyphen `\u2010` 与 non-breaking hyphen `\u2011` 视为词内部连接符，`well\u2011being` 这类正文长按不再被切成半个词；standalone/edge connector trim 语义保持不变，en/em dash 不纳入词连接。Validation: RED/GREEN `EpubSelectionTest.compose initial selection expands unicode hyphen word`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 selection edge trim follow-up 已补入：`epubTextSelection()` 现在会在 code point/text-element 边界规整后，清理 selection 两端的空白与 standalone word connector，避免 Compose 拖选/callback 落在分隔符边缘时产生带 `-` 或前后空白的 selectedText，并把 start/end locator 调整到 trim 后正文锚点。Validation: RED/GREEN `EpubSelectionTest.selection trims edge whitespace and standalone word connectors`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose callback trim mirror follow-up 已补入：paged Compose selection callback 现在复用 shared EPUB selection 的 trimmed 有效范围写入 `R.id.epub_compose_text_selection_range` 和 `R.id.epub_compose_text_selection_highlight_range`，避免 `selectedText` 已清理成 `beta` 时 Compose 高亮仍覆盖 `- beta`。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime mirrors trimmed compose callback selection range into compose state`、相邻 `EpubSelectionTest` / code point / combining mark / blank selection targeted 回归、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose emoji/ZWJ initial selection follow-up 已补入：`epubComposeInitialSelectionRange()` 现在对 emoji symbol 与 ZWJ 序列返回单个 text-element range，长按 `👩‍💻` 不再清空 selection 或只选半个 surrogate。Validation: RED/GREEN `EpubSelectionTest.compose initial selection keeps emoji zwj sequence to one text element`、`EpubSelectionTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 emoji Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose drag emoji/text-element focus boundary follow-up 已补入：`EpubComposeInitialSelection` 携带原文，`epubComposeDragSelectionRange()` 会先把 drag focus offset 规整到完整 code point/text-element/emoji ZWJ sequence，再扩展 selection range，避免 focus 落在 `👩‍💻` 内部时产生半个 surrogate 或半个序列。Validation: RED/GREEN `EpubSelectionTest.compose drag selection keeps emoji focus on text element boundary`、`EpubSelectionTest` class、相邻 gesture wiring targeted test、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose drag standalone connector focus cleanup follow-up 已补入：`epubComposeDragSelectionRange()` 现在复用 selection edge trim 清理拖选候选 range 两端的 standalone connector 与空白；`epubComposeDragFocusRange()` 只保留词内部连接符 focus，独立 `-` 不再把 `Alpha -` 这类分隔符 range 传给 callback。Validation: emoji modifier guard 直接通过；RED/GREEN `EpubSelectionTest.compose drag selection ignores standalone word connector focus`、`EpubSelectionTest` class、相邻 gesture wiring targeted test、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 paged navigation selection cleanup follow-up 已补入：`EpubReflowEngine.goTo()` 在 PAGED 模式检测当前 page index 与目标 page index，跨页导航会调用 `clearTextSelection()` 清空 engine selection、活跃 `ComposeView` page-local selection range 和 selection highlight，避免离开当前页后旧选区残留。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose selection when page navigation leaves active page`、`EpubSelectionTest` class、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 text-to-image repagination retirement follow-up 已补入：活跃 paged `ComposeView` 若在字号/行距重分页后同一 page index 映射为 `EpubPageSliceKind.Image`，会退出文本页状态并清空 text surface、selection callback、page-local selection/highlight 与 text semantics tags，避免旧文本页继续参与选择或语义曝光。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime retires active compose text page when repagination maps page to image slice`、相邻重分页/跨页 targeted tests、EPUB 全量单测、phase2 compile/build 组合和 `git diff --check` 通过。边界：这是 Robolectric/code-contract 证据，不是真机图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 ViewPager host pageCount refresh follow-up 已补入：`ViewPagerTransitionHost` 现在观察 `PagedReaderEngine.pageCount`，页数变化时刷新 adapter 并 clamp 当前页，避免 EPUB 字号/行距重分页或 text/image slice 变化后 ViewPager2 保留 stale 页面。Validation: RED/GREEN `ViewPagerTransitionHostTest.paged host refreshes adapter when engine page count changes`、`:render:animate:testDebugUnitTest --rerun-tasks`、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode 或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 ViewPager host rebind lifecycle follow-up 已补入：`ViewPagerTransitionHost` 在 `unbind()` 取消 scope 后，再次 `bind()` 会重建可用 scope 并重新启动 `PagedReaderEngine.pageCount` collection，避免同一 host 重绑后 adapter 刷新/页回调停摆。Validation: RED/GREEN `ViewPagerTransitionHostTest.paged host refreshes adapter after unbind and rebind`、`ViewPagerTransitionHostTest` class、`:render:animate:testDebugUnitTest --rerun-tasks`、phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 ViewPager stale page request callback follow-up 已补入：`ViewPagerTransitionHost.bind()` 会在绑定新 engine 前清理旧 `PagedReaderEngine` 的 page request callback，避免不经 `unbind()` 直接重绑时旧 engine 继续驱动新 ViewPager 翻页。Validation: RED/GREEN `ViewPagerTransitionHostTest.paged host clears old page request callback when rebound`、`ViewPagerTransitionHostTest` class、`:render:animate:testDebugUnitTest --rerun-tasks`、phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 cross-book openBook selection cleanup follow-up 已补入：同一 `EpubReflowEngine` 打开第二本 EPUB 前会清理旧 lazy cache、active paged `ComposeView` text/selection tags、active page maps、engine selection、目录/page count/current locator 等书籍态；旧 Compose selection callback 增加 active-page guard，过期 callback 即使晚到也不能写入新书 selection。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose selection when opening another book`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 close lifecycle Compose state cleanup follow-up 已补入：`EpubReflowEngine.close()` 在清空 active page maps 前会清理已创建 paged `ComposeView` 的 text surface、selection tags、selection callback、gesture wiring 和 text semantics tags，避免关闭 engine 后外部仍持有的旧页暴露 stale selection/语义状态；关闭后的旧 callback 因 active page state 已移除，晚到调用不会写入 `currentTextSelection`。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose text page state when closing book`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 mode switch selection cleanup follow-up 已补入：EPUB paged 文本页已有 selection 时切换到 scroll 模式会清理 engine selection 与旧 paged `ComposeView` page-local selection/highlight，避免模式切换后旧 ViewPager 页继续携带 stale selection；`setMode()` 只在 reading mode 真实变化时清理。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose selection when switching to scroll mode`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 mode switch stale Compose callback follow-up 已补入：EPUB paged 文本页切换到 scroll 模式后，会清空 active paged text/page container tracking；旧 `ComposeView` 捕获的 selection callback 即使晚到触发，也不再属于 active page state，不能重新写入 engine selection 或 page-local selection/highlight。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime ignores stale compose selection callback after switching to scroll mode`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 mode switch Compose page state cleanup follow-up 已补入：EPUB paged 文本页切换到 scroll 模式后，会在清空 active page tracking 前清理旧 `ComposeView` 的 text surface、selection tags、selection callback、gesture wiring 和 text semantics tags，避免旧 ViewPager 页继续暴露正文/选择/语义状态。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose text page state when switching to scroll mode`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 mode switch image page state cleanup follow-up 已补入：EPUB paged 图片页切换到 scroll 模式后，会递归清理旧 paged `ImageView.contentDescription` 与 drawable，避免旧 ViewPager 图片页继续暴露 stale alt/page 语义或视觉内容；同一清理路径也覆盖 `openBook()` reset 与 `close()`。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears image page accessibility state when switching to scroll mode`、RED/GREEN `EpubReflowEngineTest.paged runtime clears image page drawable state when switching to scroll mode`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 active image page progress rebind follow-up 已补入：EPUB paged 图片页如果在字号/行距重分页后仍映射到同一 image slice，会刷新旧 active `ImageView.contentDescription` 中的页码/总页数文案；若同一 page index 不再是 image slice，则清理旧图片页语义与 drawable。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime updates active image page progress after page count changes`、相邻 image page targeted tests、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合和空白检查通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 close book progress state cleanup follow-up 已补入：`EpubReflowEngine.close()` 现在会清零 `pageCount`、把 `currentLocator` 置为 `Unknown`，并将 `chapterInfo` 重置为 `0/1/""/0f`，避免关闭书后外部观察者继续看到 stale 页数、locator 或章节进度。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears book progress state when closing book`、相邻 close 生命周期测试、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 scroll goTo page request boundary follow-up 已补入：`EpubReflowEngine.goTo()` 与 internal-link post 路径现在只在 `PAGED` 模式向 `pageRequestCallback` 请求页码；scroll 模式 `goTo(Section...)` 只更新 locator/chapterInfo 和滚动位置，避免旧 paged host callback 误驱动 ViewPager。Validation: RED/GREEN `EpubReflowEngineTest.scroll runtime does not request paged navigation for section goTo`、相邻 paged navigation targeted test、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 page-local link metadata follow-up 已补入：`EpubPageSlice.links` 会保留 cached text block links，并按 page slice 裁剪为页内 offset，避免 paged text slice 丢失 inline link metadata。Validation: RED/GREEN `EpubPageMappingTest.paged layout preserves page local link metadata`、`EpubPageMappingTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 JVM/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose link ranges tag follow-up 已补入：paged `ComposeView` root 现在通过 `R.id.epub_compose_text_links` 暴露当前 page-local inline link ranges，并在退场/重置时清空。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime exposes compose text link ranges`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose link callback follow-up 已补入：paged `ComposeView` 文本页现在暴露 `R.id.epub_compose_text_link_callback` / `R.id.epub_compose_text_link_tap_wired`，可见 Compose 文本层用 `TextLayoutResult` tap hit-test 命中 page-local link；paged 内链不再依赖 `RecyclerView`，会更新 locator 并请求目标页。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime exposes compose link callback that navigates internal links` 通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、视觉或性能验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 paged link navigation selection cleanup follow-up 已补入：paged 内链跳转离开当前文本页时会调用 `clearTextSelection()` 清空 engine selection、活跃 `ComposeView` page-local selection range 和 selection highlight，避免点链接跳页后旧选区残留。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose selection when link navigation leaves active page`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 paged external link selection cleanup follow-up 已补入：paged 外链 callback 发出 `ACTION_VIEW` intent 前会清空 engine selection、活跃 `ComposeView` page-local selection range 和 selection highlight，避免跳到外部应用再返回 reader 时旧选区残留。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose selection when external link opens intent`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机外链 chooser/browser smoke、paged link tapping、TalkBack link traversal、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose LinkAnnotation follow-up 已补入：paged 可见 Compose `BasicText` 的 `AnnotatedString` 现在同时暴露 Compose 原生 `LinkAnnotation.Url` ranges，并保留 legacy `URL` string annotation 与 underline span。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime exposes compose link annotations for inline links`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合和 `git diff --check` 通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack link traversal/speech、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 Compose LinkAnnotation listener follow-up 已补入：paged `LinkAnnotation.Url` 现在带 `LinkInteractionListener`，Compose 原生 link activation 会复用既有 `handleLinkClick()`，内部链接可更新 locator 并请求目标页。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime exposes compose link annotation listener for internal links`、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack link traversal/speech、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 stale Compose link activation guard follow-up 已补入：退场后的 paged `ComposeView` 旧 link callback / `LinkAnnotation.Url` listener 现在带 active-page/slice guard，即使切到 scroll 模式后被调用，也不会再启动外链 intent 或驱动旧页链接动作。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime ignores stale compose link activation after switching to scroll mode`、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合、`git diff --check` 和 touched-file 空白扫描通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal/speech、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 mode switch Compose root progress cleanup follow-up 已补入：EPUB paged 文本页切到 scroll 模式后，旧 `ComposeView` root 会清空 page progress tag、正文语义委托标记和 API 30+ `stateDescription`，避免退场页面继续暴露 stale 页码/进度语义；同一 reset 路径也覆盖 close/openBook 清理。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime clears compose page progress semantics when switching to scroll mode`、相邻 lifecycle targeted tests、`EpubReflowEngineTest` class、EPUB 全量单测、phase2 compile/build 组合、`git diff --check` 和 touched-file 空白扫描通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack speech/action-mode、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-24 PAGE-05 text-to-image Compose root progress cleanup follow-up 已补入：EPUB paged 活跃文本 `ComposeView` 在重分页后映射为 image slice 时，会清空旧 root page progress tag、正文语义委托标记和 API 30+ `stateDescription`；真实图片页仍由新 `ImageView.contentDescription` 暴露 alt/page 文案。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime retires active compose text page when repagination maps page to image slice`、相邻 progress/image targeted tests、`EpubReflowEngineTest` class、EPUB 全量单测和 phase2 compile/build 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、paged link tapping、外链 chooser/browser 或真实 Calibre/SAF smoke 验收，PAGE-05 保持 `PARTIAL`
- 2026-06-23 EPUB-01/02/03 已收口：`EpubCorpusSmokeTest` 扩展为验证真实 EPUB 语料的字符级 `totalProgression`、typed ReaderItem locators、parsed TOC Section locators；`fragmentIds` / `fragmentTargetIndexes` / `epubResolveHref()` / `buildEpubToc(..., fragmentTargets)` 和 runtime internal-link key 保证 nav/ncx 与内链 fragment/id 落到匹配段落。5 本 `/tmp/readflow-epub-corpus` 语料、`EpubParserTocTest`、`EpubLinkTargetsTest`、EPUB 全量单测和 phase2 构建均通过；`EPUB-01/02/03` 标记 `DONE`，PAGE-05 仍 `PARTIAL`
- 2026-06-23 EPUB-04/05/06 已补强 VERIFY 证据：`EpubCorpusSmokeTest` 现在校验真实语料中的包内图片 href/zip entry/alt text、link range/分类/display-block 保留/内链 target 命中，以及 style span、table/list/blockquote/pre、fixed-layout fallback、lazy/full parity。5 本 `/tmp/readflow-epub-corpus` 通过，报告覆盖 `packageLocalImages=21`、`altImages=21`、`links=1208`、`targetedInternalLinks=1048`、`styles=787`、`tables=6`、`lists=1092`、`blockquotes=2`、`pre=2`、`idpf-cole-voyage.epub fixed=true`。EPUB 全量单测和 phase2 构建通过；三项仍保持 `VERIFY`，不可替代真机/平板视觉、TalkBack link traversal、外链 intent、pre 横向滚动和 paged-mode image-aware pagination 验收
- 2026-06-23 EPUB-07/08 已补强 VERIFY 证据：`EpubLazyBookTest` 覆盖 lazy prefetch 与 close cache cleanup；`EpubCorpusSmokeTest.validateLazyBookIndex()` 在 5 本语料上校验 lazy/full paragraph、spine、display-block、fixed-layout、char-offset parity、sampled `paragraphAt()` 正文加载、block index range、cache warm/close。`EpubParserHardeningTest` 补 XHTML entity rejection、lazy oversized spine skip、zip entry-count cap、corrupt ZIP fallback 和 safe zip path rejection；`epubParserGuard()` 已捕获 `Exception` 防止 `ZipException` 越过 fallback。EPUB targeted corpus/hardening、全量单测和 phase2 构建通过；两项仍保持 `VERIFY`，不可替代真实大 EPUB PSS/首开/滚动性能、恶意 corpus/fuzz、encrypted ZIP 和真机内存压力验收
- 详细证据、风险、回滚见 active tracker

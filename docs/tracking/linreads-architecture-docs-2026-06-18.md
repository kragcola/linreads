# LinReads Architecture Docs Tracker

_最后更新：2026-07-04_

Mode: `task`
Status: `active`
Owner: Codex

## Objective

把 LinReads 当前架构设计、审计结论、待办、Android Phase 1/2 实装状态和恢复路径整理成可跨会话接续的 tracker。2026-06-19 已获用户放行 Android Phase 1 地基与 TXT 最小阅读链路实装；跨平台 Web/HarmonyOS 实现仍需另行许可。

## Recovery Summary

- `AGENTS.md`: missing.
- `.workspace/agent-session-state.md`: missing.
- `docs/tracking/ACTIVE.md`: 已规范为短入口，并指向本 tracker；2026-06-19 更新为 Android TXT 最小阅读链路 objective。
- `docs/tracking/BACKLOG.md`: 记录 41 项历史发现与处理状态，是待办来源。
- 当前 `git status --short docs/tracking maintenance-log.md CLAUDE.md` 显示 `docs/tracking/`、`CLAUDE.md`、`maintenance-log.md` 尚未纳入 git 跟踪。
- 当前 wiki `docs/wiki/Active-Work.md` 仍停在 2026-06-18/HEAD `44f0a64`，没有反映 Phase 1 地基和 TXT slice，需要后续刷新。

## 2026-07-02 MoonReader Handfeel Note

- 已把 MoonReader EPUB/PAGED 手感可借鉴项整理到 `docs/research/moonreader-handfeel-borrowing-backlog-2026-07-02.md`，并落地 Android EPUB/PAGED P0/P1：中心 `1/3 x 1/3` dead zone 消费不翻页、中心 `1/5 x 1/5` 临时滚动、翻页前 canonical anchor snap、page-top keyed texture cache。
- 2026-07-02 center-boundary safety follow-up：静读天下 `isMiddleTap(...)` / `isFlipSmallMiddleTap(...)` 源码边界为严格 `>` / `<`；LinReads 选择更保守的安全区边界，把精确落在 `1/5` 边框的坐标仍归为 `TemporaryScroll`，精确落在 `1/3` 边框的坐标仍归为 `CenterDead`，避免边界像素误翻页。后续 AVD targeted test 先 RED：非整除 viewport 下 `width / 3f` 由于浮点边界舍入被错分为翻页/滚动，实际 `scrollY=2315`；修复为 `0.5px` 边界容差后 targeted JVM/AVD 和完整回归通过。Validation: `EpubPagedTouchZonesTest.center box boundaries remain safe with non divisible viewport sizes` RED then SUCCESS；full `EpubPagedTouchZonesTest` SUCCESS；full `:render:epub:testDebugUnitTest` SUCCESS；`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS；targeted AVD `epubFlowMoonReaderCenterZonesRuntime` RED then `OK (1 test)`；full AVD `EpubFlowAnchorRuntimeSmokeTest` = `OK (10 tests)` / `116.058s`；logcat critical grep empty；`:app:assembleDebug` SUCCESS。
- AVD `EpubFlowAnchorRuntimeSmokeTest` 现覆盖 anchor normalization、`ACTION_DOWN` alone no content movement、center zones、inner-center horizontal no-curl、MoonReader-style 20dp/40dp 阈值矩阵、edge swipe、right-side clean tap exactly one page、center clean tap toggles chrome exactly once、long-press drag no page flip（含 center dead-zone long-press no scroll/flip）、right-side inline link tap no page flip、rapid real-overlay GL no double-advance、reader surface accessibility/keyboard actions、真实 `EpubCurlOverlay` 创建和 SIMULATION 离散 GL commit，结果 `OK (10 tests)`；logcat critical grep 为 `NO_CRASH_SIGNATURES`。JVM `EpubFlowViewTest` 覆盖 slide、GL host-path cancel/commit normalized-anchor、`ACTION_DOWN` alone no content movement、long-press drag no paged flip、center dead-zone long-press no scroll/flip、rapid slide/GL no double-advance、temporary-scroll/center-dead drag release no tap-zone、inner-center horizontal drag no page turn，以及 inline link tap 不触发 paged tap-zone 回归。
- 本轮 RED/GREEN 还修了首次 lazy GL overlay 尚未 layout/GL page rect ready 时 discrete turn 被吞的竞态：`CurlView.animatePageTurn(...)` 返回是否真正启动，`EpubCurlOverlay` 会逐帧重试 pending discrete turn 直到启动或 safety cancel。
- MoonReader 手感 Round 1 源码审计已补到 backlog：值得继续借的是输入仲裁与状态不变量，包括 long-press/selection 优先于翻页已由 JVM + AVD smoke 锁住、inline link tap 优先于翻页 tap-zone 已由 JVM + AVD smoke 锁住、clean tap 单 owner 已由 AVD smoke 锁住、rapid active-turn gate 已由 JVM + AVD smoke 锁住且暂不加独立 `500ms` cooldown、drag-release no-tap 已由 JVM smoke 锁住、tap-map 配置作为后置设计选项；明确不盲抄 `8dp` curl 阈值、密集设置面和 ClickTip。
- 2026-07-02 link-priority follow-up：`EpubFlowViewTest.link tap inside text wins over paged tap zone` 先 RED 于父容器 tap-zone 抢在 child `ClickableSpan` 后同次触发；GREEN 后 targeted link test、`EpubFlowViewTest` class、完整 `:render:epub:testDebugUnitTest`、`:app:installDebug :app:installDebugAndroidTest` 通过。随后新增 AVD reader-route `epubFlowLinkTapInPageTurnZoneDoesNotTriggerPagedFlipRuntime`，在右侧 NEXT 区点击可见内链并验证 child consumed marker 为 true、页码不前进；targeted AVD = `OK (1 test)`，full `EpubFlowAnchorRuntimeSmokeTest` = `OK (7 tests)`，logcat critical grep 无崩溃签名。
- 2026-07-02 stale link-marker guard：继续审计 link-priority 的跨手势状态，新增 `EpubFlowViewTest.stale link tap marker does not suppress next clean tap outside text`，验证上一手 `ClickableSpan` consumed marker 即使仍留在 child tag 上，下一手落在正文外的右侧 clean tap 仍会触发 `NEXT`，不会被 stale marker 吞掉。该测试直接 GREEN，作为父/子 tap 仲裁回归护栏保留。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS。
- 2026-07-02 inner-center horizontal no-curl guard：继续审计 MoonReader 中心 `1/3 x 1/3` 禁 curl 不变量，新增 `EpubFlowViewTest.inner center horizontal drag is consumed without page turn`，验证手势从中心 `1/5 x 1/5` 起笔但横向拖拽时也不会触发 `PREV/NEXT/MENU`、不会翻页、不会移动 `scrollY`；同时扩展 AVD `EpubFlowAnchorRuntimeSmokeTest.epubFlowMoonReaderCenterZonesRuntime` 在真实 reader route 覆盖该分支。该 JVM 测试直接 GREEN，说明实现已具备该行为；本轮只补回归护栏和记录。Validation: targeted JVM SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `:render:epub:testDebugUnitTest` SUCCESS、`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS、targeted AVD `epubFlowMoonReaderCenterZonesRuntime` = `OK (1 test)` / `13.019s`、logcat critical grep empty、`git diff --check` clean、touched-file whitespace scan clean。
- 2026-07-02 ACTION_DOWN no-move guard：继续执行原始状态机验收，新增 `EpubFlowViewTest.action down alone does not move paged content`，验证右侧翻页区单独 `ACTION_DOWN` 不触发 tap-zone、不翻页、不移动 `scrollY`；同时扩展 AVD `EpubFlowAnchorRuntimeSmokeTest.epubFlowMoonReaderCenterZonesRuntime` 覆盖真实 reader route。该 JVM 测试直接 GREEN，说明实现已具备该行为；本轮只补回归护栏和记录。Validation: targeted JVM SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `:render:epub:testDebugUnitTest` SUCCESS、`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS、targeted AVD `epubFlowMoonReaderCenterZonesRuntime` = `OK (1 test)` / `12.809s`。
- 2026-07-02 center dead-zone long-press guard：继续执行“center ring 不破坏 long-press/selection ownership”验收，新增 `EpubFlowViewTest.long press drag in center dead zone does not trigger paged flip or scroll`，验证中心环长按拖拽不触发 tap-zone、不翻页、不移动 `scrollY`；同时扩展 AVD `EpubFlowAnchorRuntimeSmokeTest.epubFlowLongPressDragDoesNotTriggerPagedFlipRuntime` 覆盖真实 reader route。该 JVM 测试直接 GREEN，说明实现已具备该行为；本轮只补回归护栏和记录。Validation: targeted JVM SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `:render:epub:testDebugUnitTest` SUCCESS、`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS、targeted AVD `epubFlowLongPressDragDoesNotTriggerPagedFlipRuntime` = `OK (1 test)` / `15.666s`。
- 2026-07-02 temporary-scroll cancel guard：新增 RED/GREEN `EpubFlowViewTest.temporary scroll cancel re-arms paged clip without tap zone`，修复 EPUB/PAGED 临时滚动收到 `ACTION_CANCEL` 后仍保持 `pageClipActive=false` 的状态泄漏。GREEN 后取消路径会 snap 到最近 line top、`reportTopOffset()`、重开 paged clip、清理 `classified/freeScrolling/centerDeadGesture/inSelectionMode/stealing`，并且不会触发 tap-zone。Validation: targeted JVM SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `:render:epub:testDebugUnitTest` SUCCESS、`:app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest` SUCCESS、targeted AVD `epubFlowMoonReaderCenterZonesRuntime` = `OK (1 test)` / `12.454s`、logcat critical grep empty、`git diff --check` clean。仍不把 AVD 运行态证据宣称为真实手感。
- 2026-07-02 clean-tap ownership follow-up：新增 AVD reader-route `epubFlowRightTapTurnsExactlyOnePageRuntime`，验证 EPUB/PAGED 右侧 clean tap 从第一页只前进到第 1 页 canonical top，不会因 `EpubFlowView` 与外层 `ReaderTapContainer` 双 owner 跳到第 2 页；随后新增 `epubFlowCenterTapTogglesChromeExactlyOnceRuntime`，用真实 reader surface 中心点击验证底部 chrome 打开且 page anchor 不动。两个 targeted AVD 均为 `OK (1 test)`，then-current full `EpubFlowAnchorRuntimeSmokeTest` = `OK (9 tests)`，logcat critical grep 无崩溃签名。中心 tap 这项无需实现改动。
- 2026-07-02 accessibility/keyboard preservation follow-up：新增 AVD reader-route `epubFlowReaderSurfaceAccessibilityAndKeyboardActionsRuntime`，在同一 EPUB/PAGED full reader route 验证 reader surface 暴露 `上一页`、`下一页`、`显示或隐藏阅读工具栏`；`ACTION_SCROLL_FORWARD` 前进到第 1 页 canonical top，`ACTION_SCROLL_BACKWARD` 回第 0 页，`DPAD_RIGHT` 前进一页，`DPAD_CENTER` 打开含 `排版` / `主题` 的 chrome。Validation: `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS；full `adb -s emulator-5554 shell am instrument -w -e class 'dev.readflow.page05.EpubFlowAnchorRuntimeSmokeTest' dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (10 tests)`；logcat critical grep 无崩溃签名。
- 2026-07-02 page-turn image/cache lifecycle Round 2：复查 MoonReader `createCachePageShots(...)` / `createCachePageShots2(...)` / `saveFlipShot(...)` / `getPageShot(...)` / `waitingForCahingShots(...)` 后，backlog 记录为“借不变量，不借全局阻塞”。LinReads 已通过翻页前 canonical snap、page-top keyed texture cache、pre-cache post 锚点复查、`turnInFlight` reflow defer、GL pending retry 和 bitmap recycle 覆盖主要风险；暂不加 MoonReader 式 `waitingForCahingShots()` 用户可感阻塞或 line-text sampling，后续 image-heavy EPUB/真机如果复现 same-top/different-content cache alias 再补测试和实现。
- 2026-07-02 reflow texture-cache follow-up：继续执行 Round 2 的 cache lifecycle 不变量，新增 RED/GREEN `EpubFlowViewTest.async reflow recycles stale turn texture cache before rebuilding pagination`。RED 证明 `reflowRunnable` 在重分页/重锚点时旧 `cachedFrontBitmap` / `cachedRevealedBitmap` 和旧 top keys 仍留存；GREEN 后 `EpubFlowView` 在 `turnInFlight` 已清、重分页前调用 `recycleCachedTextures()`，避免 reflow 后保留 stale turn texture cache。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 GL cache-key mismatch follow-up：继续执行 Round 2 的 cache identity 不变量，新增 RED/GREEN `EpubFlowViewTest.discrete gl turn recycles stale cached textures when top keys mismatch`。RED 证明 GL path 在 cached page index 匹配但 top key 不匹配时会 fallback live snapshot，却不会立即回收旧 `cachedFrontBitmap` / `cachedRevealedBitmap`；GREEN 后 `EpubFlowView.recycleCachedTexturesIfStaleForTurn(...)` 会在离散 GL 和跟手 GL snapshot 前检查 from/target page+top tuple，不匹配就回收旧缓存再 live snapshot。Fake overlay 断言收到 viewport-sized live textures，而不是 1x1 stale cache。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 clamped final-page texture follow-up：继续执行 Round 2 的 cache identity 不变量，新增 RED/GREEN `EpubFlowViewTest.discrete gl turn snapshots clamped final page at canonical visual top`。RED 证明 GL/cache snapshot 使用 raw `pageTopPxAt(target)`，而短 final page 实际只能显示在 `ScrollView` clamped `maxScroll`，revealed 纹理可能和 commit 后 live page 不一致；GREEN 后 `EpubFlowView` 的 pre-cache、离散 GL、跟手 GL 都用与 `scrollToPage(...)` 一致的 canonical/clamped visual top 作为 texture top/key。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS、`:app:assembleDebug` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 clamped final-page boundary guard：审计 `nearestCanonicalPageIndexForScrollY(...)` / `scrollToPage(...)` 后新增 `EpubFlowViewTest.next page from clamped final page reports chapter boundary`，验证 raw `topPx > maxScroll` 的短 final page 会保留 final page index、视觉上 clamp 到 `maxScroll`，并且 `goToAdjacentPage(1)` 返回 `false` 交给外层跨 spine，而不是在同章重复翻最后页。该测试直接 GREEN，作为回归护栏保留。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS。
- 2026-07-02 recycled-cache follow-up：继续执行 Round 2 的 bitmap-liveness 不变量，新增 RED/GREEN `EpubFlowViewTest.discrete gl turn ignores recycled cached textures and snapshots live pages`。RED 证明 cached page/top tuple 匹配但 `cachedFrontBitmap` / `cachedRevealedBitmap` 已 recycled 时，GL 离散翻页会尝试复用坏缓存并无法启动；GREEN 后 `recycleCachedTexturesIfStaleForTurn(...)` 同时检查 bitmap liveness，发现已 recycled 就清空 cache state，让 GL 改用 live viewport-sized snapshots。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS、`:app:assembleDebug` SUCCESS、`git diff --check` 和 touched untracked docs/tests no-index whitespace checks 通过。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 pre-cache liveness follow-up：继续执行 Round 2 的 bitmap-liveness 不变量，新增 RED/GREEN `EpubFlowViewTest.precache refreshes recycled cached textures even when top keys match`。RED 证明 `preCachePageTextures()` 只看 page/top key，导致 same-key 但已 recycled 的 `cachedFrontBitmap` / `cachedRevealedBitmap` 被当作命中并长期留在 cache state；GREEN 后预缓存命中条件要求两张 cached bitmap 都非空且未 recycled，否则先清空 stale state 再 post fresh live snapshots。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS、`:app:assembleDebug` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 partial-cache follow-up：继续执行 Round 2 的 cache-readiness 不变量，新增 RED/GREEN `EpubFlowViewTest.discrete gl turn treats partial cached textures as stale and snapshots live pages`。RED 证明 cached page/top tuple 匹配但只有 `cachedRevealedBitmap` 存在时，GL 离散翻页会把 live front texture 和旧 1x1 revealed texture 混用；GREEN 后 `recycleCachedTexturesIfStaleForTurn(...)` 要求 front/revealed bitmap pair 同时存在且未 recycled，任一缺失即回收 partial cache state，再 live snapshot 两页。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS、`:app:assembleDebug` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 mode-switch texture-cache follow-up：继续执行 Round 2 的 cache lifecycle 不变量，新增 RED/GREEN `EpubFlowViewTest.switching out of paged mode recycles cached turn textures`。RED 证明 `mode = SCROLL` 会触发 repaginate，但旧 `cachedFrontBitmap` / `cachedRevealedBitmap` 和 cached page/top keys 仍保留；GREEN 后 `EpubFlowView.mode` 在模式真实变化时、重分页前调用 `recycleCachedTextures()`，避免 PAGED 翻页纹理跨模式/布局复用。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS、`:app:assembleDebug` SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 pre-cache atomic-pair follow-up：继续执行 Round 2 的 complete-pair 不变量，新增 RED/GREEN `EpubFlowViewTest.precache discards partial texture pair when revealed snapshot fails`。RED 证明 `preCachePageTextures()` 在 front snapshot 成功、revealed snapshot 失败时，会留下半套 `cachedFrontBitmap` 与 page/top keys；GREEN 后预缓存先用局部变量完成 front/revealed 两张快照，只有两张都非空才提交 cache state，任一失败都会回收 partial bitmap 并清空 cache。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS。仍不引入 MoonReader 式全局等待或 line-text sampling。
- 2026-07-02 active-turn pre-cache follow-up：继续执行 Round 2 的“cache preparation must never race active turn”不变量，新增 RED/GREEN `EpubFlowViewTest.pending precache does not commit while gl turn is active`。RED 证明已 post 但尚未执行的 `preCachePageTextures()` runnable 可在 GL overlay active 后提交新的 front/revealed cache pair；GREEN 后 `preCachePageTextures()` 入口和 posted runnable 都检查 `turnInFlight`，active GL/slide turn 期间不做 snapshot、不提交 cache state。Validation: targeted JVM test SUCCESS、`EpubFlowViewTest` class SUCCESS、完整 `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :render:epub:testDebugUnitTest` SUCCESS。仍不引入 MoonReader 式全局 `waitingForCahingShots()` 阻塞。
- 2026-07-02 continuation audit rerun after clamped final-page texture follow-up：当前 `adb devices -l` 仍只有 `emulator-5554`；`git diff --check` clean；test-tools py_compile 通过；`python3 -m unittest discover android/test-tools` = `OK (4 tests)`；targeted JVM `EpubPagedTouchZonesTest` + `EpubFlowViewTest` 通过；完整 `:render:epub:testDebugUnitTest :app:assembleDebug` 通过；`:app:installDebug :app:installDebugAndroidTest` SUCCESS；`/Volumes/OmubotDisk/Applications/sdk/platform-tools/adb -s emulator-5554 shell am instrument -w -e class dev.readflow.page05.EpubFlowAnchorRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (10 tests)` / `125.309s`；post-run logcat grep for crash/ANR/OOM/recycled-bitmap/reader-surface/assertion/failure signatures returned no matches. 仍不把 AVD 运行态证据宣称为真实手感。
- 2026-07-02 page-turn snapshot padding-clip self-audit：对齐 `snapshotViewport()` / `snapshotPageAt(...)` 与 live `dispatchDraw()` 的 `TextView.paddingTop`-aware top/bottom clip，避免 slide/GL/pre-cache page-turn bitmap 在有 `TextView` padding 时和 live paged render 露出不同半行。新增 `EpubFlowViewTest.page turn snapshots use the same padding clip as paged rendering`。Validation: `:render:epub:testDebugUnitTest` SUCCESS；targeted JVM `--rerun-tasks --tests "dev.readflow.render.epub.EpubFlowViewTest.page turn snapshots use the same padding clip as paged rendering"` = SUCCESS；`:app:assembleDebug` SUCCESS；`python3 -m unittest discover android/test-tools` = `OK (4 tests)`；`:app:installDebug :app:installDebugAndroidTest` SUCCESS；AVD `EpubFlowAnchorRuntimeSmokeTest` on `emulator-5554` = `OK (10 tests)` / `124.508s`；narrow logcat critical grep = `NO_CRITICAL_LOGCAT_MATCHES`；`git diff --check` clean。仍不把 AVD/JVM 证据宣称为真实手机/平板手感。
- 2026-07-02 tap-map/settings surface Round 3：复查 MoonReader `A.clickArea(...)`、`A.set_default_Do_Events()`、`A.set_default_flip()`、`PrefControl`、`PrefVisual`、`PrefMisc` 后，backlog 记录为“借 escape hatch，不借设置密度”。MoonReader 支持 tapMode/toggleTapMode、可选九宫格 `do91..do99`、四边动作、ClickTip 区域预览、edge/multitouch/tilt/disable-move 以及 flip animation/speed 设置；但默认九宫格 disabled，默认主要仍是上一页/下一页/菜单/长按等基础动作。LinReads 决策：当前不实现九宫格、edge toggles、flip-speed slider 或 first-run ClickTip；若真机/低视力证据要求，再做 2-3 个 tap-layout presets，并用测试锁住 link、selection、TalkBack、keyboard 和 EPUB center temporary-scroll 的优先级。
- 2026-07-02 physical/A-B audit protocol：`adb devices -l` 当前只有 `emulator-5554` / `sdk_gphone64_arm64`，所以不把任何 AVD 结果升格为物理手感。backlog 已补具体协议：手机/平板手感、release-time no-snap A/B、post-settle rapid input、image-heavy EPUB GL cache、tap-layout presets 都需要设备信息、视频、filtered logcat 和 `keep current` / `run another A/B` / `implement narrow change` 决策后才能改实现。
- 2026-07-02 Current Completion Ledger：backlog 已把 MoonReader 借鉴项分成四类：已执行并 test-locked、仅作为未来设计选项、明确不抄、必须等物理/A-B 证据。当前代码继续保持已执行 P0/P1 不变量，不新增 speculative hand-feel 参数。
- 2026-07-02 release-time no-snap decision：执行顺序第 20 项已收口为 deferred。MoonReader 的可借鉴核心继续定义为“临时滚动后，翻页取图前必须 normalize 到 canonical visual anchor”，LinReads 已实现；松手后是否 no-snap 是真实手感实验，不是当前代码层安全修复。当前仅有 `emulator-5554`，因此不实现、不 A/B-enable no-snap，保持 release-time line snap，等待手机/平板 A/B 视频、logcat 和明确决策。
- 2026-07-03 duplicate-anchor feedback correction：继续执行 EPUB flow 手感收口时发现同一可视锚点重复 `goToOffset(...)` 仍会重复 `reportTopOffset()`，把相同 locator 重新送回 progress/sync/reader-state 链路。旧 `HEAD` 在临时 worktree 仅加入 `EpubFlowViewTest.reapplying the current flow anchor does not report duplicate progress` 即 RED；GREEN 后 `EpubFlowView` 记录 `lastReportedTopOffset`，普通同锚点 `goToOffset` 不再重复回报，首开 settle/reflow/尺寸变化/anchored mode switch 仍通过 `forceReport` 保留必要刷新。证据仍是 JVM/build 级，不是真机/平板物理手感。
- 2026-07-03 GL/AVD settle recovery：full AVD 复跑前先修正 stale smoke 断言，`epubFlowMoonReaderCenterZonesRuntime` 不再要求临时滚动 `ACTION_UP` 后停在滚动中间，而是验证拖动中可移动且 `pageClipActive=false`，松手后 normalize 回 canonical page anchor。真实 `EpubCurlOverlay` 在 AVD 上仍可能出现 harism settle callback 不到达；LinReads 现在由 host 安排离散 GL fallback，在预期动画窗口后提交目标页、dismiss overlay、预缓存，并在正常 settle/dismiss 时取消 fallback/late callback。新增 `EpubFlowViewTest.discrete gl turn falls back to target page if harism never settles`。Validation: targeted JVM fallback test SUCCESS；`:app:installDebug :app:installDebugAndroidTest` SUCCESS；targeted AVD `epubFlowMoonReaderCenterZonesRuntime`、`epubFlowSimulationCurlCreatesRealOverlayAndCommitsRuntime`、`epubFlowSimulationCurlIgnoresRapidSecondTurnRuntime` 通过；final full AVD `EpubFlowAnchorRuntimeSmokeTest` = `OK (10 tests)` / `129.886s`；`./gradlew --no-daemon -Dkotlin.incremental=false -Preadflow.phase=2 :render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean；narrow logcat critical grep 无匹配。仍不得把这些 AVD/JVM 证据宣称为物理手机/平板手感。
- 2026-07-03 paper/first-turn/SCROLL->PAGED final correction：最新用户复查后，EPUB flow 修复三项剩余手感问题。live draw 现在在 `ScrollView` 子绘制坐标下用 `scrollX/scrollY` 抵消父级平移后画 viewport paper，snapshot 仍在 bitmap 原点画同源 paper，避免手指按下进入 turn overlay 时底纹相位改变；首翻语义更新为“ready paged surface 立即结束 initial reveal 并走正常动画，只有零高度或未分页才排队”，替代前一轮“等 revealed frame 后再动”的保守策略；`SCROLL -> PAGED` anchored switch 仅在该方向隐藏内容，先停到 nearest canonical page anchor 再 reveal，避免用户看到滚动几何转换成分页几何。新增/更新 `EpubFlowViewTest.first slide frame preserves scrolled paper texture under the finger`、`first turn during initial reveal finishes reveal and animates immediately`、`first turn requested after zero-height initial settle replays when measured`、`queued first turn at chapter boundary is forwarded after measured viewport`、`scroll to paged anchored switch hides conversion until canonical page is parked`。Validation: targeted tests、`EpubFlowViewTest` class、完整 `:render:epub:testDebugUnitTest`、`:features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug`、`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest`、AVD `EpubFlowAnchorRuntimeSmokeTest` on `emulator-5554` = `OK (10 tests)` / `128.12s`、logcat narrow grep `NO_CRASH_SIGNATURES` 和 `git diff --check` 均通过。仍是 JVM/AVD 证据，不是真机/平板物理手感。
- 2026-07-04 paper/tap ownership continuation：继续修用户复查后两个更细的手感边缘。`readerPaperBackground(...)` 现在把 grain 预染色成 `ARGB_8888` bitmap 后再平铺，避免 live draw 和 page-turn snapshot/GL/slide 路径分别套 shader colorFilter/alpha 时出现细微色差；这仍只是 JVM/AVD 级验证，不声明物理手感完成。self-paging EPUB 的 clean tap ownership 也收窄为：右/左 PREV/NEXT 会在 `EpubFlowView` 自身设置 consumed tag，阻止外层同一 UP 二次处理；中心 MENU 不设置 consumed，继续让外层 reader host 打开 chrome。full AVD 先暴露 consumed 过宽导致 `epubFlowCenterTapTogglesChromeExactlyOnceRuntime` 找不到 `排版`，修正后新增 `EpubFlowViewTest.self paging center tap leaves outer reader host available for chrome toggle`，targeted AVD center/right tap 与 full `EpubFlowAnchorRuntimeSmokeTest` = `OK (10 tests)` / `171.076s`，最终 phase2 EPUB/reader/animate/app build combo 通过。
- 2026-07-04 page-shot strip / reveal reflow continuation：继续把静读天下 page-shot 不变量收紧到 slide/software-curl。旧 slide 只移动 outgoing snapshot，incoming 页由 live content + 静态背景滑入，纸纹在按下/拖动时不像属于页面本身；现在 `PageSlideDrawable` 和 lightweight `PageCurlDrawable` 都持有 front/revealed 两张 snapshot，slide 按两页截图 strip 移动，software-curl 也先铺 revealed shot 再折 front shot。若 revealed snapshot 失败，会回收 outgoing 并回退普通切页，不展示半套纹理。复杂 `SCROLL -> PAGED` 方面，reveal fade 期间如果 async image/layout reflow 到达，`EpubFlowView` 会取消 fade、重新隐藏、按 conversion anchor 重分页归位后再 reveal，避免半透明阶段露出滚动几何到分页几何的归位。新增 RED/GREEN `EpubFlowViewTest.interactive slide moves incoming paper as a page shot`、`action down alone preserves real paper texture`、`scroll to paged reflow during reveal fade re-hides conversion until reparked`；`EpubFlowViewTest` class 和完整 `:render:epub:testDebugUnitTest` 通过。最终 phase2 组合 `:render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean；`:app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest` SUCCESS；AVD `EpubFlowAnchorRuntimeSmokeTest` on `emulator-5554` = `OK (10 tests)` / `118.27s`；最窄 logcat crash/OOM/recycled-bitmap grep 无输出。仍是 JVM/AVD 证据，不是真机/平板物理手感。
- 2026-07-03 restored-open transactional locator follow-up：继续收紧已读书恢复入口，避免 `EpubReflowEngine.openBook(...)` 先把 `_currentLocator` 发布为书首再由 ViewModel `goTo(savedLocator)` 修正。新增 render-api 可选能力 `InitialLocatorAwareReaderEngine`；`ReaderViewModel` 在 open 前先解析 restored/persisted/remote winner locator 并预置给支持引擎；EPUB reflow open 使用目标 paragraph 预取 lazy spine、再建 paged slices，并把目标 locator 作为第一 resolved position。新增 ReaderViewModel/EPUB regression tests 锁住 open 前预置、同 Section payload normalization 不重复 `goTo`、以及 `currentLocator` emission 不出现 book-start Section。Validation: targeted tests SUCCESS；完整 `:render:epub:testDebugUnitTest` SUCCESS；`:features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean。仍是 JVM/build 证据，不是真机/平板物理手感。
- 2026-07-03 restored-open/SCROLL->PAGED host final follow-up：继续收紧宿主和同步反馈入口。自分页 EPUB flow 现在在 `ReaderScreen` 中按 `SelfPagingReaderEngine.selfPagingActive` 稳定选择 paged/self-paging host，哪怕当前 `pagingKind=CONTINUOUS`，也不再因 SCROLL/PAGED 模式切换拆 continuous host 再重挂 paged host；普通连续引擎仍走 continuous host。`ReaderViewModel.applyRemoteProgress(...)` 同步回放也复用 same-display-position 判断，同一 `Section` payload 仅 progression 被规范化时只更新 Room/ReaderState，不再触发后置 `goTo`。新增 `ReaderScreenHostKindTest` 与 `ReaderSavedStateHandleTest.remote sync replay does not goTo again for same display section`。Validation: targeted tests SUCCESS；完整 `:features:reader:testDebugUnitTest`、`:render:animate:testDebugUnitTest`、`:render:epub:testDebugUnitTest` SUCCESS；最终组合 `:render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean。仍是 JVM/build 证据，不是真机/平板物理手感。
- 2026-07-04 SCROLL->PAGED hidden-reflow anchor follow-up：继续查复杂模式切换时的隐藏窗口，发现 `SCROLL -> PAGED` 虽已 hidden 并用 nearest canonical anchor 停到目标页，但 reveal 窗口内若 async image/layout reflow 到达，`reflowRunnable` 会优先使用旧 `pendingRestoreOffset`；这个值可能还是进书时的恢复锚点，且 reflow path 默认 floor-to-containing-page，导致转换后从目标页跳回旧页。新增 RED/GREEN `EpubFlowViewTest.scroll to paged reflow during hidden conversion keeps the conversion anchor`，RED 结果 expected `210` but was `105`。GREEN 后 `setModeAnchored(...)` 在隐藏转换开始时把本次转换 offset 登记为 pending anchor、清掉旧 `pendingLandOnLast`，并在 nearest canonical snap 后把 pending anchor 更新为已停好的 page-top line offset；后续 reflow 继续停在用户刚切到的分页页。Validation: targeted RED/GREEN；`EpubFlowViewTest` class SUCCESS；完整 `:render:epub:testDebugUnitTest` SUCCESS；最终组合 `./gradlew --no-daemon -Preadflow.phase=2 :render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean。仍是 JVM/build 证据，不是真机/平板物理手感。
- 2026-07-04 restored legacy Page locator follow-up：继续收紧已读书打开入口里旧 paged locator 的身份翻译。根因：`LocatorStrategy.Page.index` 曾在 EPUB flow restore/goTo 中经 `epubIndexFromLocator(...)` 被当成 paragraph index；旧 paged/remote progress 的 page index 与 paragraph index 并不等价，尤其长段落、多 spine、字号/屏幕变化后会先落到错段/旧区域。实现：`EpubReflowEngine.openBook(...)` 先用 legacy Page 的 `totalProgression` / `progression` 估算文档 offset 并预热目标 lazy spine，建 `pagedSlices` 后将 Page 翻译为当前分页 slice 对应的 canonical `Section`；无 progression 时回退 raw page index；`goToFlow(...)` / `flowAnchorFromLocator(...)` 也先走同一翻译。分页构建继续只读 cached text，锁住首开不扫冷 spine；`ReaderViewModel` 使用 `openBook(...)` 返回的 normalized locator，避免 post-open 再把原始 Page replay 成第二次可见 `goTo`。新增/更新 `EpubReflowEngineTest.open book translates restored page locator through pagination instead of paragraph index`、`open book translates restored page locator by progression across spines`、`flow goTo translates page locator through pagination instead of paragraph index`、`open book builds page slices from cached text without loading cold spines`、`ReaderSavedStateHandleTest.initial locator aware engine does not receive duplicate goTo when it normalizes page locator`。Validation: targeted tests SUCCESS；最终组合 `./gradlew --no-daemon -Preadflow.phase=2 :render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean。仍是 JVM/build 级证据，不是真机/平板物理手感。
- 2026-07-04 restored-open layout transaction follow-up：继续查已读书首开仍可能“先露默认/旧几何再落到保存位置”的最后一段链路。根因：`ReaderViewModel` 已在 open 前预置最终 locator，但字号、行距、主题、字体、翻页样式和阅读模式仍在 `openBook(...)` 后应用；EPUB flow 这些 setter 会触发重建/重分页，导致第一 resolved position 可能先按默认几何发布再按用户配置重排。实现：`ReaderViewModel` 新增 `ReaderOpenSettings`，对 `InitialLocatorAwareReaderEngine` 在 `openBook(...)` 前应用最终布局/模式/翻页配置，再预置最终 locator；非 aware engine 保留 open 后配置路径，避免 TXT/MD/PDF 兼容风险。新增 `ReaderSavedStateHandleTest.initial locator aware engine receives layout settings before opening book`（旧顺序 RED）和 `EpubReflowEngineTest.open book honors pre-applied paged typography before publishing restored position`，后者锁住 EPUB legacy path 的 pre-open PAGED/排版配置不被 open 覆盖。Validation: targeted RED/GREEN；完整 `:features:reader:testDebugUnitTest` SUCCESS；完整 `:render:epub:testDebugUnitTest` SUCCESS；最终组合 `./gradlew --no-daemon -Preadflow.phase=2 :render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :render:animate:testDebugUnitTest :app:assembleDebug` SUCCESS；`git diff --check` clean。仍是 JVM/build 证据，不是真机/平板物理手感。
- 2026-07-02 physical handfeel capture helper：新增 `android/test-tools/moonreader_handfeel_capture.py`，把 Physical / A-B Audit Protocol 的证据包固定为 timestamped run dir：`manifest.json`、`NOTES.md`、before/after screenshot、短 screenrecord、`adb devices -l`、设备属性和 filtered critical logcat。新增 `android/test-tools/test_moonreader_handfeel_capture.py` 用 `--dry-run` 锁住 bundle shape；Validation: `python3 -m unittest android/test-tools/test_moonreader_handfeel_capture.py` = `OK`，`python3 -m unittest discover android/test-tools` = `OK (4 tests)`。该工具不改变产品行为，只降低后续真机 gate 采集成本。
- 2026-07-02 capture helper preset follow-up：`moonreader_handfeel_capture.py` 新增 `--preset phone|tablet|ab`，自动填常见 `device-label` / `gate` / 录屏时长，显式 CLI 参数可覆盖；新增 `phone` preset dry-run 单测锁住参数展开。Validation: `python3 -m unittest android/test-tools/test_moonreader_handfeel_capture.py` = `OK (2 tests)`。这只是在降低物理证据采集门槛，不改变物理/A-B 的判定标准。
- 2026-07-02 capture helper ADB smoke：当前仅有 `emulator-5554`，用 `moonreader_handfeel_capture.py --record-seconds 2` 实跑采集路径，生成 `/private/tmp/readflow-handfeel-capture-avd/20260702-085400-emulator-portrait-helper-smoke`，产物含 `manifest.json`、`NOTES.md`、`adb-devices.txt`、before/after screenshots、`screenrecord.mp4`，`logcat-critical.txt` 为空。该 smoke 只证明工具链可用，不升格为物理手感证据。
- 仍不得把 AVD 证据宣称为物理设备手感。后续真实手机/平板 harism GL 跟手、cancel、commit、release-time no-snap A/B 仍是独立审计项。

## 2026-06-22 Continuation Note

- 当前细粒度验收入口已转到 `docs/tracking/android-v4-pure-reading-unfinished-backfill.md` 和 `docs/tracking/android-v4-pure-reading-gap-checklist.md`；`ACTIVE.md` 保留短入口和高层剩余差距。
- 2026-06-22 已补 AVD-only evidence：A-01/A-02/A-03、UX/PAGE 若干项、READ-01/02、STATE-01/02/03、STATE-04。凡缺物理手机/平板、真实 TalkBack speech traversal、硬件键盘/D-pad 或真实 pinch 的条目不得升 DONE。
- 2026-06-26 A-01 follow-up：上一轮临时 `/tmp/readflow-share-smoke` synthetic sender 证据已固化为 repo-owned phase2 AndroidTest。新增测试 APK 侧 `A01ExternalBookProvider` 与 `A01IncomingBookRuntimeSmokeTest`，在 `emulator-5554` 上通过 external `content://dev.readflow.test.a01bookprovider/...` 分别发送 `ACTION_VIEW` / `ACTION_SEND` 的 TXT/MD/EPUB/PDF，共 8 个 grouped cases。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest`；`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.a01.A01IncomingBookRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。证据 `/tmp/readflow-a01-incoming-runtime-smoke-20260626` 记录 `case_count=8`，各 case 均写入 app-private `file://` 书籍并进入 reader；成功窗口 logcat crash/ANR/OOM/recycled-bitmap/FileNotFound/SecurityException grep 为空。边界：仍是 AVD/provider 模拟，不是真实平板系统文件管理器、Android sharesheet、OEM DocumentsUI 或第三方文件 App；`A-01` 保持 `VERIFY`。
- 2026-06-25 UX-02 follow-up：上一轮把捏合卡点记成“当前 AVD `adb shell input` 只有单指，所以只能做文案/XML 级证明”；这轮已用 phase2 AndroidTest 直接突破这条验证边界。新增 `android/app/src/phase2AndroidTest/java/dev/readflow/ux02/ReaderPinchRuntimeSmokeTest.kt`，不再依赖 shell input，而是直接向 `ReaderTapContainer` 注入双指 `MotionEvent`。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux02.ReaderPinchRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。运行时证据 `/tmp/readflow-ux02-stage-20260625/ux02-runtime-smoke` 记录：TXT `baseline_font_label=16sp` -> `after_font_label=22sp`，`baseline_text_size_px=42.0` -> `after_text_size_px=59.294098`，并且 DataStore `persisted_font_size_sp=22`；PDF `baseline_scale_type=FIT_CENTER` -> `after_scale_type=MATRIX`，矩阵 `matrix_scale_x=1.4117643`、`matrix_translate_x=-222.35272`、`matrix_translate_y=56.47101`。结论：当前 AVD 已不再只是“无法合成真实双指 pinch”，而是能通过 instrumentation 真正跑到 TXT 字号 preview/commit 与 PDF matrix zoom 路径。边界：依然是 AVD instrumentation，不是真实手指 pinch；缺物理手机/平板、PDF 手势后视觉观感/边界拖拽、EPUB/MD 多尺寸调校，因此 UX-02 继续保持 `VERIFY`，但后续重点应转向真机/平板 UX，而不是再卡在“AVD 无法双指”。
- 2026-06-25 UX-03/UX-04 follow-up：上一轮这两项还停在“AVD XML/截图能看到控件、点坐标能切模式”的弱证据；这轮已补 phase2 AndroidTest grouped runtime smoke `android/app/src/phase2AndroidTest/java/dev/readflow/ux03/ReaderModeControlsRuntimeSmokeTest.kt`，不再只依赖 shell tap 坐标，而是直接在运行态读取 reader view/Room/DataStore 状态。Validation: `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux03.ReaderModeControlsRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (2 tests)`。运行时证据已从设备拉到 `/tmp/readflow-ux03-ux04-stage-20260625/ux03-ux04-runtime-smoke`：`ux03-mode-switch-summary.txt` 记录 TXT 在 scroll 模式滚到 paragraph `018..031` 可见区后切到 paged，paged `tagIndex=19` 落在原可见区间 `18..31` 内；切回 scroll 后同一 paragraph `019` 仍在可见列表中，且 Room `reading_progress.locatorJson` 前后保持同一 `ByteOffset offset=1406 length=73`、`totalProgression=0.296875`。`ux03-paged-mode.xml` 还能看到 paged host 下仍暴露 `16sp`、`阅读正文预览`、`1.75x`、`滚动`、`分页`。`ux04-controls-theme-summary.txt` 记录 `font_label=16sp`、`line_spacing_label=1.75x`、`persisted_theme=DARK`，以及 TXT runtime palette 从 `baseline_background=0xFFEDE6D6` / `baseline_text=0xFF2A2620` 切到 `night_background=0xFF2A2620` / `night_text=0xFFD8CFBC`；`ux04-theme-panel-before-night.xml` / `ux04-theme-panel-after-night.xml` 则确认 `跟随系统` / `日间` / `夜间` / `护眼` 四个主题入口真实存在。结论：`UX-03` 现在不再只是证明“模式能切”，而是已经证明 TXT scroll/paged 往返时当前可见段落范围与持久化 locator 保持稳定；`UX-04` 也不再只是证明“控件文案在面板里”，而是证明了排版/主题控制会真实驱动运行态字号、行距和夜间 palette。边界：仍然只是 AVD instrumentation + XML/Room/截图证据，不是真实手机/平板 UI、TalkBack 焦点/语音遍历、多尺寸视觉验收或更广长文/多格式 smoke，因此 `UX-03/UX-04` 继续保持 `VERIFY`。
- 2026-06-26 A-02/PDF-01 page-cache policy follow-up：PDF paged path 和 legacy adapter 的 page cache 从固定 `±1` / 3 张推进到预算化策略：渲染页尺寸在 128MB bitmap 预算内时优先 `±2` / 5 张，连续翻两页可命中预取；高分屏或超大页会降级到 `±1` / 3 张，极端大页只保留当前页，避免为性能扩大 OOM 面。Validation: RED `./gradlew :render:pdf:testDebugUnitTest --tests "dev.readflow.render.pdf.PdfPageCachePolicyTest"` 先失败于缺少 policy；GREEN `./gradlew :render:pdf:testDebugUnitTest`；phase2 `:render:pdf:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:assembleDebug` 通过；安装后 `ReaderPagedKeyboardRuntimeSmokeTest` AVD reader-route `OK (3 tests)`，证据 `/tmp/readflow-a02-pdf-cache-policy-20260626/ux05-page-runtime-smoke`，logcat crash/ANR/recycled-bitmap/OOM grep 为空。边界：这是 cache/prefetch contract + AVD 稳定性，不是真实 PDF frame/PSS benchmark；A-02 保持 `PARTIAL`，PDF-01 保持 `VERIFY`。
- 2026-06-26 UX-05/PAGE-01~04 follow-up：上一轮这组还主要停在 `adb input keyevent` / 坐标 swipe 的弱 smoke；这轮已补 phase2 AndroidTest grouped runtime smoke `android/app/src/phase2AndroidTest/java/dev/readflow/ux05/ReaderPagedKeyboardRuntimeSmokeTest.kt`，直接在运行态读 paged host、accessibility action、page transformer 和 PDF zoom reset 状态。Validation: `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:installDebugAndroidTest`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.ux05.ReaderPagedKeyboardRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (3 tests)`。运行时证据已从设备拉到 `/tmp/readflow-ux05-page-stage-20260626/ux05-page-runtime-smoke`：`keyboard-runtime-summary.txt` 记录 baseline `第 1 页，共 4 页`、`baseline_current_item=0`、`baseline_item_count=4`、默认 transformer `dev.readflow.render.animate.ViewPagerTransitionHost$CurlPageTransformer`，以及 reader surface accessibility actions `上一页|下一页|显示或隐藏阅读工具栏`；同组测试确认 `ACTION_SCROLL_FORWARD/BACKWARD`、`DPAD_RIGHT/LEFT`、`PAGE_DOWN/UP`、`SPACE` 都会在同一 4 页 PDF 上把页码稳定推进到 `第 2 页，共 4 页` 再返回，`DPAD_CENTER` 会拉起含 `排版` / `主题` 的 chrome，`keyboard-after-center.xml` 可见当前 PDF `ImageView` 页码 `第 2 页，共 4 页` 与底部 chrome 节点。`host-transformer-summary.txt` 进一步记录 `fade_transformer=...FadePageTransformer`、`slide_transformer=...ResetPageTransformer`、`after_next=1`、`after_previous=0`，证明 FADE/SLIDE host runtime 切换和 host-driven `next()/previous()` 仍然工作。`zoom-reset-summary.txt` 则记录 PDF 当前页在 `after_zoom_preview_scale=1.5`、`after_zoom_preview_scale_type=MATRIX` 后翻到下一页会恢复 `after_page_change_scale_type=FIT_CENTER`、`after_page_change_zoom_scale=1.0`、`after_page_change_label=第 2 页，共 4 页`。结论：`UX-05` 现在不再只是证明“修掉了 D-pad 左右被吞”，而是已经证明 paged reader 的无障碍 action、键盘导航和 chrome toggle 都跑在真实 reader surface 上；`PAGE-01/02/03` 也不再只是“代码里有 ViewPager2/transformer”，而是已有 grouped runtime 证据证明默认 CURL host、FADE/SLIDE transformer runtime 切换、host-driven navigation 和真实 paged page label 同时成立；`PAGE-04` 则从“PDF 能翻页”推进到“PDF 翻页时 zoom preview 会按页重置回 FIT_CENTER/1x”。边界：仍然只是 AVD instrumentation + XML/截图证据，不是真实硬件键盘/D-pad、TalkBack action traversal、真机/平板 swipe/pinch ergonomics、真实视觉动效质量或帧率测量，因此 `UX-05` 继续保持 `PARTIAL`，`PAGE-01~04` 继续保持 `VERIFY`。
- 2026-06-26 PAGE-05 grouped runtime smoke：上一轮 PAGE-05 主要还是 JVM/Robolectric 契约和 real-corpus gate；这轮已补 phase2 AndroidTest grouped runtime smoke `android/app/src/phase2AndroidTest/java/dev/readflow/page05/EpubPagedRuntimeSmokeTest.kt`，直接在 phase2 app 的真实 reader route 上读 paged EPUB 的 `ComposeView` / image page runtime state。Validation: `./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew --no-daemon --no-parallel -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.page05.EpubPagedRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (3 tests)`。运行时证据已从设备拉到 `/tmp/readflow-page05-stage-20260626/page05-epub-runtime-smoke`：`mode-switch-summary.txt` 记录 paged 文本页 `page_text=Alpha selection sentinel keeps compose runtime visible while paged mode is active.`、`paged_progress=第 1 页，共 1 页`、`paged_selection_range=(6, 15)`，切回 scroll 后 `retired_progress=null`、`retired_surface_visible=false`、`retired_selection_enabled=false`、`retired_semantics_exposed=false`、`retired_selection_callback_present=false`，证明旧 paged `ComposeView` 的 text/selection/progress/semantics 状态会在真实 reader route 里退场；`link-navigation-summary.txt` 记录 paged 文本页 `page_text=Read the scene after selection.`、`links_count=1`、`selection_before_link=(0, 4)`，点击内链后落到 `image_description=Scene art，第 3 页，共 4 页`，旧文本页 `old_selection_after_link=null` / `old_selection_highlight_after_link=null`，并且 DB `locatorJson={"strategy":{"type":"dev.readflow.core.model.LocatorStrategy.Section","spineIndex":1,"elementIndex":1,"charOffset":1},"progression":0.969697,"totalProgression":0.969697}`，证明 image fragment internal-link 和 selection cleanup 不再只停在 Robolectric callback；`image-cover-summary.txt` 则记录 image-only cover spine baseline `baseline_current_item=0`、`baseline_item_count=2`、`baseline_image_description=Cover art，第 1 页，共 2 页`、`baseline_has_compose_page=false`，翻到下一页后 `after_next_current_item=1`、`after_next_page_text=Chapter one text starts immediately after the image-only cover.`、`after_next_page_progress=第 2 页，共 2 页`、`after_next_current_image_description=null`，证明 image-only cover 首屏不会插入空白文本页，且翻页后会进入真正正文页。结论：PAGE-05 这轮已经从“代码/契约上支持 paged EPUB”推进到“phase2 app 真 reader route 下，文本页退场、image fragment link、image-only cover 首屏”三组关键运行态都成立。边界：仍然只是 AVD instrumentation + XML/DB/screenshot 证据，不是真机图文分页视觉、真实 Compose selection action-mode、TalkBack link traversal/speech、帧率/内存预算或更大真实语料的人手交互验收，因此 PAGE-05 继续保持 `PARTIAL`，后续重点应转向真机/平板视觉、TalkBack 和性能 gate，而不是继续堆同类本地契约。
- 2026-06-26 PAGE-05 长无空格中文段 follow-up：在上一轮 CJK mixed runtime 后，继续补 `EpubPagedRuntimeSmokeTest.epubPagedSlicesLongUnspacedCjkParagraphAndPacksTailRuntime`，把 96 个无 ASCII 空格中文长段片段与 18 个尾部短句放进真实 reader route。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebug :app:installDebugAndroidTest`、targeted long-CJK instrumentation `OK (1 test)`、完整 `EpubPagedRuntimeSmokeTest` = `OK (8 tests)`、phase2 `:render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:assembleDebug` 通过。证据 `/tmp/readflow-page05-cjk-long-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-cjk-long-summary.txt` 显示 `long_paragraph_has_space=false`、`page_count=5`，第 1/2 页各 packed 27 个长段片段，第 4 页同时包含 `长段082..096` 与 `尾句001..006` 6 个尾句；logcat 精确 grep 未命中 crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError。边界：仍是 AVD reader-route 证据，不是真机平板视觉、TalkBack speech/action-mode、折叠屏/分屏或帧率/PSS，PAGE-05 保持 `PARTIAL`。
- 2026-06-26 PAGE-05 `<br/>` 中文诗行 follow-up：继续补 `EpubPagedRuntimeSmokeTest.epubPagedPacksCjkPoemLinesSeparatedByBreaksRuntime`，把 84 行中文诗句放在单个 `<p>` 内并用 `<br/>` 硬换行分隔，覆盖诗歌/网文短行 EPUB 不应“一行一页”的边界。Validation: targeted CJK `<br/>` instrumentation `OK (1 test)`、完整 `EpubPagedRuntimeSmokeTest` = `OK (9 tests)`、phase2 `:render:epub:testDebugUnitTest :features:reader:testDebugUnitTest :app:compileDebugAndroidTestKotlin :app:assembleDebug` 通过。证据 `/tmp/readflow-page05-cjk-br-runtime-20260626/full-class/page05-epub-runtime-smoke/packed-cjk-br-summary.txt` 显示 `poem_line_count=84`、`max_packed_page_budget=28`、`page_count=4`，第 1 页 packed `诗行001..027`，第 2 页 packed `诗行028..054`；logcat 精确 grep 未命中 crash/ANR/recycled-bitmap/`Unable to find reader surface`/AssertionError/FAILURES。边界：仍是 AVD reader-route 证据，不是真机平板诗歌视觉、TalkBack speech/action-mode、折叠屏/分屏或帧率/PSS，PAGE-05 保持 `PARTIAL`。
- 最新 STATE-01/02/03 证据保存在 `/tmp/readflow-state-recovery-smoke-final`：TXT `readflow-perf-1mb` 写入 Room `ByteOffset offset=520 length=129`，engine-state cache `129110 bytes`，进程 kill 后从书架重开同一本恢复到 line `000003/000004` 附近。边界：launcher cold start 返回书架，不代表系统自动恢复 reader route。
- 最新 STATE-04 证据保存在 `/tmp/readflow-state04-deviceid-smoke`：AVD 首次生成、保留数据重装、AVD reboot 后 DataStore `device_id` 与 Room `reading_progress.deviceId` 均保持 `a9bfdc2c-9f1a-46af-a94b-e46aaea40fd4`。边界：仍不是物理手机/平板升级/重启 smoke。
- 最新 STATE-05 决策：当前纯阅读回填明确延期真实远程同步后端；KorroSync/KoSync/WebDAV 保留到后续跨设备同步里程碑。Settings 已接入 `SyncBackend` 状态，`NoOpSyncBackend` / unavailable backend 会显示 `远程同步未启用` 和“本机保存进度、书签和标注”的说明，避免 no-op 被误认为已远程同步。Validation: `:features:settings:testDebugUnitTest` 红绿覆盖该状态，phase2 `:app:assembleDebug` 通过。
- 最新 READ-01/02 证据保存在 `/tmp/readflow-read-search-smoke`：TXT 搜索 `000800` 产生 `结果 1 · 10%`，点击后从 line `000000` 附近跳到 line `000799`-`000807`，Room locator 为 `ByteOffset offset=104000 length=129`。边界：仅 AVD/TXT UI smoke，MD/EPUB/PDF 与真机/TalkBack 仍待补。
- 2026-06-24 follow-up：READ-01/02 多格式证据已扩展到 `/tmp/readflow-read-multiformat-smoke`。本轮 AVD 的 MediaStore `ACTION_VIEW` 需要显式 `--grant-read-uri-permission`，否则会出现 `dev.readflow has no access to content://media/...` 假阴性。PDF `readflow-perf-10mb-50p|PDF` 搜索面板显示 `当前格式暂不支持搜索` 且页码保持 `第 1 页，共 50 页`；EPUB `readflow-search|EPUB` 查询 `EPUBSEARCH042` 返回 `结果 1 · 94%` 并跳到 chapter 4，Room locator 为 `Section(spineIndex=3, elementIndex=304, charOffset=6309)`、`totalProgression≈0.935`；MD `readflow-search-long|MD` 查询 `MDSEARCH042` 返回 `结果 1 · 92%`，`after-result.png` 可见 near-tail token，Room locator 为 `Section(spineIndex=0, elementIndex=364, charOffset=21855)`、`totalProgression≈0.919`。三次运行 pid 存活且 crash/ANR grep 为空。边界：仍是 AVD-only synthetic input，不是真机/平板或 TalkBack speech traversal。
- 2026-06-24 TOC stage follow-up：本轮 grouped TOC runtime evidence 仍只写入 `ACTIVE.md` 与本 tracker，不提前改大 checklist。MD 方面，`/tmp/readflow-toc-batch8-md` 证明 `readflow-search-long|MD` 从 baseline 文首打开后，reader chrome -> `目录` 面板可见 `Search Target`，点击后正文落到 near-tail `Search Target` / `MDSEARCH042` 区域；Room `reading_progress` 写入 `LocatorStrategy.Section(spineIndex=0, elementIndex=362, charOffset=21820)`、`totalProgression=0.917192101478577`。PDF 方面，先通过 RED/GREEN `PdfOutlineParserTest.resolves indirect outline title action and catalog names tree destination refs` 证明旧 parser 无法解析真实论文 PDF 常见的 indirect `/Title`、indirect `/A`、以及 catalog `/Names /Dests` value-ref 形态；GREEN 后 targeted `PdfOutlineParserTest` class、`:render:pdf:testDebugUnitTest` 与 phase2 `:render:pdf:testDebugUnitTest :render:pdf:compileDebugKotlin :features:reader:testDebugUnitTest :app:assembleDebug` 通过。对应 AVD runtime 证据 `/tmp/readflow-toc-batch10-pdf-real` 与 `/tmp/readflow-toc-batch11-pdf-real-late` 证明 `react_2210_03629_outline|PDF` 的 TOC 面板出现真实 outline 条目 `1 Introduction`、`2 ReAct: Synergizing Reasoning + Acting`、`3.3 Results and Observations`，不再回退为 `第 N 页（页列表）`；点击 `3.3 Results and Observations` 后，reader 从 `第 1 页，共 33 页` 跳到 `第 5 页，共 33 页`，Room 写入 `LocatorStrategy.Page(index=4,total=33)`、`totalProgression=0.121212124824524`。EPUB 方面，`/tmp/readflow-toc-batch12-epub-real` 证明 `readflow-search|EPUB` 从 baseline `Chapter 1` 进入 TOC 后点击 `Chapter 4`，正文切到 `Chapter 4 paragraph 000..007`，Room 写入 `LocatorStrategy.Section(spineIndex=3, elementIndex=243, charOffset=0)`、`totalProgression=0.748087286949158`。边界：三组都仍是 AVD synthetic tap/XML/screenshot/Room 证据，不是真机/平板、TalkBack speech traversal、或真人 clean tap 验收。
- 最新 READ-03 证据现包含 TXT `/tmp/readflow-read03-bookmark-smoke`，并新增 `/tmp/readflow-read-interaction-batch2/md_bookmark_db` 与 `/tmp/readflow-read-interaction-batch2/epub_bookmark_db` 的 DB-backed bookmark lifecycle：MD 从 `LocatorStrategy.Section(spineIndex=0, elementIndex=0, charOffset=0)` / `totalProgression=0.0` 起步，添加后写入 active `书签 0%`、`isDeleted=0`，滚动到 `Section(spineIndex=0, elementIndex=125, charOffset=7541)` / `0.3169819116592407` 后再点书签回到 `0.0`，删除后同 row tombstone 为 `isDeleted=1`；EPUB 走出同一闭环，progress `0.0 -> 0.2119981050491333 -> 0.0`，滚动 locator 为 `Section(spineIndex=0, elementIndex=69, charOffset=7149)`，删除后同 row `isDeleted=1`。边界：仍是 AVD synthetic tap/input + Room/XML/screenshot 证据，不是真机/平板/TalkBack/真实 sync backend。
- 最新 READ-04 证据现包含 TXT `/tmp/readflow-read04-selection-smoke`、EPUB 非 TXT `/tmp/readflow-read-interaction-batch2/epub_annotation_db`，以及 MD grouped runtime `/tmp/readflow-md-runtime-fix4-20260624-205642` / `/tmp/readflow-md-runtime-fix5-20260624-2115`：EPUB 在 `Chapter 1 paragraph 077` 附近长按后，`after_longpress_0.xml/png` 暴露 `笔记` / `取消` / `高亮` / `保存`，随后 Room 生成 `anchorType=text-selection-range`、`LocatorStrategy.Section(spineIndex=0, elementIndex=78, charOffset=8094..8101)` 的选区锚点。MD 方面，`TOC -> Search Target -> 长按 -> 保存` 后，新标注 anchor 稳定落在 near-tail `Section(spineIndex=0, elementIndex=366, charOffset=21936..21945)`，不再漂回文首。边界：仍是 AVD synthetic long-press + Room/XML/screenshot 证据，不是真机/平板/TalkBack/action-mode/EPUB link coexistence。
- 最新 READ-05 证据现包含 TXT `/tmp/readflow-read05-note-save-instrument-pass.txt` + `/tmp/readflow-read05-note-save-pass-db/readflow.db`、EPUB 非 TXT `/tmp/readflow-read-interaction-batch2/epub_annotation_db`，以及 MD grouped runtime `/tmp/readflow-md-runtime-fix4-20260624-205642`、`/tmp/readflow-md-runtime-fix4b-20260624-210955`、`/tmp/readflow-md-runtime-fix5-20260624-2115`。TXT 方面，先由手动 `ACTION_VIEW` `/tmp/readflow-read05-manual.xml` 证明 FileProvider TXT 可直达 reader，再将 phase2 `TxtRead05NoteSaveSmokeTest` 修正为按空行分段、等待 reader 根节点和 `EditText` 自身值；在严格串行 `adb shell am instrument -w -e class dev.readflow.read05.TxtRead05NoteSaveSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` 下通过，日志见 `/tmp/readflow-read05-note-save-instrument-pass.txt`。WAL 一并导出的 DB 快照 `/tmp/readflow-read05-note-save-pass-db/readflow.db` 可查询最新 active `TXT-NOTE-*` rows，`selectedText=anchor`、`note=TXT-NOTE-*`、`anchorType=text-selection-range`、`totalProgression=0.0843881890177727`；同条 smoke 还校验了保存后 selection actions dismiss，以及 reader chrome -> `标注` 面板可见 note。EPUB 方面，高亮后 Room active `text_annotations` 写入 `selectedText=Chapter`、`anchorType=text-selection-range`、`totalProgression=0.24002134799957275`，离开到 `0.3243579864501953` 后再从标注列表跳回相同锚点。MD 方面，save 后 `reading_progress` 保持 near-tail `Section(spineIndex=0, elementIndex=362, charOffset=21823)`、`totalProgression≈0.9173182`；移走到 `Section(spineIndex=0, elementIndex=304, charOffset=18311)`、`totalProgression≈0.76969314` 后，再从标注列表跳回会把 `reading_progress` 恢复到选区锚 `Section(spineIndex=0, elementIndex=366, charOffset=21936)`、`totalProgression≈0.9220681`。边界：TXT/EPUB/MD 这组仍是 AVD synthetic long-press/tap/instrumentation + Room/XML/screenshot 证据，不是真机/平板/TalkBack/action-mode/MD/EPUB 导出或真实同步验收。
- 2026-06-24 晚 MD READ-04/05 closure：root cause 已从旧的 offset 混用与 long-press refresh 问题继续收敛到“保存后清理 native selection 时不能覆写视口/locator”。实现上新增 `SelectionAwareTextView.clearNativeTextSelection()`，在移除原生 `Spannable` selection 时保留祖先 `ScrollView` scroll；`MarkdownEngine.clearTextSelection()` 则在清空 engine/native selection 时抑制内部 locator overwrite，并按当前 near-tail 视口重算 locator。Validation: RED/GREEN `SelectionAwareTextViewTest.clear native text selection preserves scroll and removes native selection`、`MarkdownEngineTest.clearTextSelection preserves markdown scroll and locator near current section`，以及 `./gradlew -Preadflow.phase=2 :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.SelectionAwareTextViewTest" :render:md:testDebugUnitTest --tests "dev.readflow.render.md.MarkdownEngineTest" :render:api:compileDebugKotlin :app:assembleDebug` GREEN。AVD 证据 `/tmp/readflow-md-runtime-fix4-20260624-205642`、`/tmp/readflow-md-runtime-fix4b-20260624-210955`、`/tmp/readflow-md-runtime-fix5-20260624-2115` 现已证明 `TOC -> Search Target -> 长按 -> 保存 -> 移走 -> 标注列表跳回` 全链路稳定，不再回到文首；仍只能记 grouped runtime `VERIFY`，不可替代真机/平板与 TalkBack/action-mode 验收。
- 最新 TXT-01/02/03/04 JVM/local corpus 证据：`TxtDocumentTest.large utf8 corpus keeps block ranges locator search and cache round trip stable` 覆盖大于 `3 * 64 KiB` 的 UTF-8 语料、48 段 paragraph、跨 block ranges、`indexForOffset()`、search `ByteOffset` 字符起点和 engine-state cache round-trip；`normalizes gb18030 four byte offsets to character starts` 覆盖 GB18030 四字节字符内部 offset 回退；`mixed encoding corpus keeps search and selection byte locators on character starts` 覆盖 UTF-8/GBK/Shift_JIS search 与 selection ByteOffset 字符起点。Validation: `./gradlew :render:txt:testDebugUnitTest --tests "dev.readflow.render.txt.TxtDocumentTest" --rerun-tasks`、`:render:txt:testDebugUnitTest`、phase2 `:render:txt:testDebugUnitTest :features:reader:testDebugUnitTest :app:assembleDebug` 通过。边界：仍缺真实手机/平板导入 TXT、PSS/首开/滚动、恢复、搜索/选择 UI smoke，来源表中 `TXT-01/02/03/04` 保持 `VERIFY`。
- 最新 PDF-02 JVM synthetic PDF 证据：`PdfOutlineParser` 在 direct `/Dest` 与 GoTo `/D` 数组之外，新增 catalog `/Names /Dests` name tree、旧式 catalog `/Dests` dictionary named destination、indirect `/Title` object ref、indirect `/A` / `/D` action-destination ref 与 name-tree value ref 解析；`PdfOutlineParserTest` 覆盖 outline `/Dest (chapter-two)`、GoTo `/D /chapter-one`、direct destination array、indirect `/D [...]` destination object，以及 indirect outline title/action/name-tree destination ref，均映射到预期 page index。Validation: dictionary named-destination 与 indirect title/action/name-tree destination RED/GREEN targeted run、`PdfOutlineParserTest` class、`:render:pdf:testDebugUnitTest`、phase2 `:render:pdf:testDebugUnitTest :render:pdf:compileDebugKotlin :features:reader:testDebugUnitTest :app:assembleDebug` 通过。2026-06-24 runtime follow-up：`/tmp/readflow-toc-batch10-pdf-real` 与 `/tmp/readflow-toc-batch11-pdf-real-late` 证明 `_id=255 react_2210_03629_outline|PDF` 的 TOC 面板出现真实 outline 条目 `1 Introduction`、`2 ReAct: Synergizing Reasoning + Acting`、`3.3 Results and Observations`，不再回退为 `第 N 页（页列表）`；点击 `3.3 Results and Observations` 后，reader 从 `第 1 页，共 33 页` 跳到 `第 5 页，共 33 页`，Room 写入 `LocatorStrategy.Page(index=4,total=33)`、`totalProgression=0.121212124824524`。边界：仍缺物理手机/平板 TOC jump、xref/object streams、超过 16 MiB scan cap 的大 PDF 和更广 named destination 形态，来源表中 `PDF-02` 保持 `VERIFY`。
- 最新 MD-01 JVM/compile 证据：`MarkdownDocument` 新增源码 offset 与 Markwon rendered text offset 的轻量映射，`MarkdownEngine` 在 scroll locator、goTo、selection 和 highlight range 中使用同一映射，避免 `#`、`**bold**`、`[link](url)` 等源码语法被渲染剥离后锚点漂移。Validation: `MarkdownDocumentTest.selection maps rendered markdown offsets back to source anchors`、`annotation source anchors map to rendered markdown highlight ranges`、`:render:md:testDebugUnitTest`、phase2 `:render:md:testDebugUnitTest :render:md:compileDebugKotlin :features:reader:testDebugUnitTest :app:assembleDebug` 通过。2026-06-24 runtime follow-up：`/tmp/readflow-toc-batch8-md` 证明 `readflow-search-long|MD` 从文首打开后，TOC 面板点击 `Search Target` 可跳到 near-tail `Search Target` / `MDSEARCH042` 区域；Room 写入 `LocatorStrategy.Section(spineIndex=0, elementIndex=362, charOffset=21820)`、`totalProgression=0.917192101478577`。边界：仍缺真实设备/平板 MD save-restore、TOC jump、长文滚动、复杂 Markdown inline/table/link 选择和布局变化恢复 smoke，来源表中 `MD-01` 保持 `VERIFY`。
- 最新 SRC-01/02 grouped AVD/runtime 证据：phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src01/CalibreGroupedRuntimeSmokeTest.kt` 现已在 `emulator-5554` 上稳定串起 Settings 连接失败提示与 guided probe。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 8081`、`/Volumes/OmubotDisk/Applications/sdk/platform-tools/adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src01.CalibreGroupedRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner`，结果 `OK (2 tests)`。运行时证据 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke/settings-probe-summary.txt` 证明：坏端口 `http://10.0.2.2:8090` 会显示 `无法连接到服务器` 与下一步 `确认手机和 Calibre 在同一局域网，并检查端口是否为 8080`；host-only hint `10.0.2.2` 经 probe 成功发现 `http://10.0.2.2:8081` 且返回 `已发现 Calibre：10.0.2.2:8081，发现 1 本书`。边界：这是 AVD + fake host server 证据，不是真实 Calibre Content Server、认证服务器、广义 LAN IP 组合或物理手机/平板。
- 最新 SRC-03/04 grouped AVD/runtime 证据：同一 `CalibreGroupedRuntimeSmokeTest` 在 Settings 保存探测到的 `10.0.2.2:8081` 后，走通 `导入书籍 -> Calibre 搜索 -> 搜索 smoke -> 下载` 的真实 HTTP 链路。运行时证据 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke/search-results.png`、`search-downloaded.png` 与 `downloaded-state-readflow.db` 证明 `Remote EPUB Smoke` 可从假服务返回到搜索 sheet，下载后 `books` 表写入 `calibre-42|Remote EPUB Smoke|DOWNLOADED|file:/data/user/0/dev.readflow/files/books/calibre-42.epub|`；这同时证明 AVD 的 host route `10.0.2.2` 不只是 URL policy/JVM 检查，而是实际可搜索、可下载、可落库。边界：仍是 AVD + fake host server，不是真实 Calibre Content Server、认证服务器、物理手机/平板或封面/大文件下载验收。
- 最新 SRC-05 JVM 证据：`DownloadedBookCachePlanner` / `DownloadedBookCache` / `LibraryRepository` 已接入远程下载书 LRU trim，默认保留最近 5 本 `calibre-*` downloaded 书，淘汰时删除 app-private `file://` 资产并清空 row 的 `localUri` / download status；补 `:core:database` 的 `libs.coroutines.test` 后，`DownloadedBookCachePlannerTest` / `DownloadedBookCacheTest` / `LibraryViewModelTest` + phase2 `:app:assembleDebug` 通过。2026-06-25 follow-up：`SRC-01~04/06/07` 的 grouped AVD/runtime 链路已稳定，`SRC-05` 将复用同一 fake server + instrumentation 扩到 6+ 远程下载和真实 LRU 淘汰。当前边界仍是尚缺该 6+ 下载后的文件删除、离线打开和 UI 解释 smoke。
- 最新 SRC-06 grouped AVD/runtime 证据：在 `CalibreGroupedRuntimeSmokeTest` 第二步中，下载 `Remote EPUB Smoke` 后关闭 fake server，再通过书架菜单 `移除下载` 执行真实移除链路。`/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke/download-offline-remove-summary.txt` 与 `removed-state-readflow.db` 证明移除前 `downloadedFileExistsBeforeRemove=true`、移除后 `removedStatusAfterRemove=NOT_DOWNLOADED`、`removedLocalUriAfterRemove=null`、`downloadedFileExistsAfterRemove=false`，书架仍保留 row。边界：仍缺真实 Calibre Content Server + 真机/平板下载后移除、重新下载、大文件进度/取消和失败态 smoke。
- 最新 SRC-06 failure-path AVD/runtime 证据：新增 `CalibreDownloadFailureRuntimeSmokeTest`，使用完整 URL `http://10.0.2.2:18081` 先连通 fake Calibre，再在搜索结果出现后关闭 server 并点击下载。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest`、`python3 android/test-tools/fake_calibre_server.py --host 127.0.0.1 --port 18081`、`/Volumes/OmubotDisk/Applications/sdk/platform-tools/adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src06.CalibreDownloadFailureRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner`，结果 `OK (1 test)`。证据 `/tmp/readflow-src06-download-failure-runtime-20260626-pass/calibre-download-failure-runtime-smoke/download-failure-summary.txt` 证明下载错误显示为 `Calibre: Failed to connect to /10.0.2.2:18081`，失败后 `bookRowAfterFailure=null`、`orphanFilesAfterFailure=`、DB `books` count 为 0，书架仍是 `还没有书`。边界：这仍是 AVD + fake server 断源模拟，不是真实 Calibre Content Server、认证服务器、物理手机/平板、大文件进度/取消或失败重试 UX。
- 最新 SRC-07 grouped AVD/runtime 证据：同一 runtime smoke 在 fake server 关闭后切换到 `离线可读`，仍能看到并打开 `Remote EPUB Smoke`；reader 首屏命中段落 `Calibre smoke paragraph proves offline reader opening after download.`，随后移除下载后离线筛选显示 `没有离线可读的书`。证据位于 `/tmp/readflow-src01-runtime-20260625-final/calibre-runtime-smoke/offline-filter.png`、`offline-reader-open.png`、`after-remove-download.png`。边界：仍是 AVD + fake host server，不是真实 Calibre Content Server + 物理手机/平板 source unavailable、TalkBack/UI 或横竖屏验收。
- 最新 SRC-08 JVM/compile 证据：`LinReadsBackupExporter` 已通过 Room DAO 导出全部 `reading_progress`、`bookmarks`、`text_annotations`，写入 ZIP `manifest.json`，包含 `format=linreads-backup`、`schema_version=1`、三类数据和空 `assets` 扩展位；Settings 通过 SAF `CreateDocument("application/zip")` 提供 `导出备份`，成功/失败会显示计数或错误。`LinReadsBackupExporterTest`、`SettingsViewModelTest`、phase2 `:app:assembleDebug`、phase1 `:app:assembleDebug` 通过。2026-06-24 runtime follow-up：新增 phase2 AndroidTest `android/app/src/phase2AndroidTest/java/dev/readflow/src08/BackupExportRestoreRuntimeSmokeTest.kt`，在 `emulator-5554` 上执行真实 ZIP 导出。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin`、`./gradlew -Preadflow.phase=2 :app:installDebug :app:installDebugAndroidTest`、`/Volumes/OmubotDisk/Applications/sdk/platform-tools/adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src08.BackupExportRestoreRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner`，结果 `OK (2 tests)`。运行时导出物 `/tmp/readflow-src08-src09-backup-runtime/linreads-backup-runtime.zip` 的 `manifest.json` 含 `format=linreads-backup`、`schema_version=1`、`reading_progress_count=2`、`bookmarks_count=2`、`text_annotations_count=1`、`assets_count=0`；配套证据还包含 `/tmp/readflow-src08-src09-backup-runtime/instrument-roundtrip.txt`、`backup-runtime-source.db`、`backup-runtime-target.db`。边界：这是 AVD/runtime + app-private file/DB 证据，不是真实手机/平板 SAF 文档选择器 UI、手动 ZIP 核验、大数据量/异常写入或跨真机恢复 round-trip。
- 最新 SRC-09 JVM/compile 证据：`LinReadsBackupRestorer` 已能读取备份 ZIP 的 `manifest.json`，校验 `format=linreads-backup` 且只接受显式 `schema_version = 1`，只恢复 `reading_progress`、`bookmarks`、`text_annotations`，按 `updatedAt` LWW 合并、保留 tombstone 且不触碰 `books` 表；Settings 通过 SAF `OpenDocument` 提供 `恢复备份`，成功/失败会显示恢复计数或错误。2026-06-24 follow-up 补入 schema hardening：`LinReadsBackupExporterTest.restoreRejectsSchemaVersionsBeforeOneWithoutMutatingLocalData` RED 先证明 `schema_version=0` 会被接受，`restoreRejectsMissingSchemaVersionWithoutMutatingLocalData` RED 先证明缺失 schema 会被默认成 v1；GREEN 后非法/缺失 schema 都会在写入任何本地进度前被拒绝；完整 `LinReadsBackupExporterTest`、`:core:database:testDebugUnitTest`、`SettingsViewModelTest`、phase2 `:app:assembleDebug`、phase1 `:app:assembleDebug`、`git diff --check` 通过。2026-06-24 runtime follow-up：同一 `BackupExportRestoreRuntimeSmokeTest` 证明 restore 会把导出的 ZIP 回灌到第二个 Room DB，且保持 `updatedAt` LWW 与 `books` 非覆盖边界。`/tmp/readflow-src08-src09-backup-runtime/backup-runtime-target.db` 查询可见 `existing-book|400|{\"type\":\"byte\",\"offset\":999}|...|local-device` 仍保留较新的本地进度，`new-book|300|{\"type\":\"byte\",\"offset\":128}|...|backup-device` 和 `new-annotation|new-book|Readflow|restored|360|0` 成功恢复，`new-bookmark|new-book|deleted remote bookmark|350|1` tombstone 被保留，`local-shelf-book|Local Shelf Book|file:///books/local-shelf-book.txt|DOWNLOADED` 证明 `books` 表未被覆盖。坏 ZIP `/tmp/readflow-src08-src09-backup-runtime/linreads-backup-invalid-missing-schema.zip` 会因缺失 `schema_version` 被拒绝，且目标 DB 不会出现 `missing-schema-book`。边界：这仍是 AVD/runtime 对 app-private DB/ZIP 的 round-trip，不是真实手机/平板 SAF 恢复、实际导出文件手动核验、导出→恢复跨设备 round-trip、大数据量/损坏 ZIP 或确认/预览 UX smoke。
- 最新 SRC-08/SRC-09 SAF UI runtime 证据：2026-06-26 新增测试 APK provider `SafDocumentProvider` 与 phase2 AndroidTest `BackupSafUiRuntimeSmokeTest`，从 Settings 可见入口点击 `导出备份` / `恢复备份`，通过 `Instrumentation.ActivityMonitor` 拦截 `ACTION_CREATE_DOCUMENT` / `ACTION_OPEN_DOCUMENT` 并返回 `content://dev.readflow.test.safdocumentprovider/src08-src09-saf-ui-backup.zip`。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest`、`/Volumes/OmubotDisk/Applications/sdk/platform-tools/adb -s emulator-5554 shell am instrument -w -e class dev.readflow.src08.BackupSafUiRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner`，结果 `OK (1 test)`。证据 `/tmp/readflow-src08-src09-saf-ui-runtime-20260626/backup-saf-ui-runtime-smoke/backup-saf-ui-summary.txt` 记录 `export_monitor_hits=1`、`restore_monitor_hits=1`、`manifest_format=linreads-backup`、`manifest_schema=1`、`manifest_counts=progress:1,bookmarks:1,annotations:1`、`exported_zip_size=503`，恢复后 Room 行回到导出侧较新的 `progress={"type":"byte","offset":128} updatedAt=300`、删除态书签和标注。边界：这是 AVD instrumentation 拦截 SAF intents + test ContentProvider Uri，不是真实手机/平板 DocumentsUI 手动选择、OEM 文件管理器、跨设备恢复或大数据/损坏 ZIP UX；`SRC-08/SRC-09` 保持 `VERIFY`。
- 最新 OPT-01/02/03 决策：新增 `docs/audit/adr-mupdf-license-2026-06-23.md`，并同步 `docs/android-architecture-v4.md` §5.7/§12.4/§12.5。裁决：base APK 保持 MuPDF-free；当前里程碑不启用 AGPL MuPDF-linked binary；未来 MuPDF 必须先满足完整 AGPL 合规或商业授权。DOCX 因当前官方资料未显示为 MuPDF Core 标准输入而延期；CBZ 虽可由 MuPDF 支持，但因 AGPL/商业授权和 native binary 成本，本轮也延期，未来优先评估无 AGPL 的 first-party ZIP image pager。来源表中 `OPT-01` 标记 `DONE`，`OPT-02`/`OPT-03` 标记 `DEFERRED`。
- 最新 PAGE-05 text-to-image repagination retirement follow-up：EPUB paged active `ComposeView` 如果在字号/行距重分页后同一 page index 映射为 image slice，会从 active text page state 退场；`rebindActiveComposeTextPage()` 检测到 `EpubPageSliceKind.Image` 后移除 weak-map entry，清空 engine selection、text surface、selection callback、gesture wiring、page-local selection/highlight 和 text semantics tags，避免旧文本页在图文分页重绑后继续参与选择或语义曝光。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime retires active compose text page when repagination maps page to image slice" --rerun-tasks` 先失败于旧 text surface visible 仍为 `true`；GREEN 后同一测试通过；相邻重分页/跨页 targeted tests 通过；EPUB 全量 `:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 通过。边界：这是 Robolectric/code-contract 证据，不是真机图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 text-to-image Compose root progress cleanup follow-up：EPUB paged active text `ComposeView` 在字号/行距重分页后映射为 image slice 时，现在会同时清空旧 root `R.id.epub_compose_page_progress_description`、正文语义委托标记和 API 30+ `stateDescription`；真实图片页仍由新建 paged `ImageView.contentDescription` 暴露 alt/page 文案。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime retires active compose text page when repagination maps page to image slice" --rerun-tasks` 先失败于旧 Compose root 仍残留 progress 语义；GREEN 后同一测试通过；相邻 progress/image targeted tests 通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、paged link tapping、外链 chooser/browser 或真实 Calibre/SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 ViewPager host pageCount refresh follow-up：`ViewPagerTransitionHost` 现在在绑定 `PagedReaderEngine` 后收集 `pageCount`，页数变化时触发 `PagedEngineAdapter.refreshPageCount()` 并 clamp `currentItem`，避免 EPUB 字号/行距重分页或 text/image slice 变化后 ViewPager2 继续保留 stale 页面。Validation: RED `./gradlew --no-daemon --no-parallel :render:animate:testDebugUnitTest --tests "dev.readflow.render.animate.ViewPagerTransitionHostTest.paged host refreshes adapter when engine page count changes" --rerun-tasks` 先失败于 refresh observer count 为 `0`；GREEN 后同一测试通过；`:render:animate:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked Kotlin no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 ViewPager host rebind lifecycle follow-up：`ViewPagerTransitionHost.unbind()` 取消宿主 scope 后，同一 host 再次 `bind()` 会检测 inactive scope 并重建，再重新启动 `PagedReaderEngine.pageCount` collection 与页回调，避免重绑后 adapter 不再刷新。Validation: RED `./gradlew --no-daemon --no-parallel :render:animate:testDebugUnitTest --tests "dev.readflow.render.animate.ViewPagerTransitionHostTest.paged host refreshes adapter after unbind and rebind" --rerun-tasks` 先失败于 rebind 后 refresh observer count 为 `0`；GREEN 后同一测试通过；`ViewPagerTransitionHostTest` class 通过；`:render:animate:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 cross-book openBook selection cleanup follow-up：同一 `EpubReflowEngine` 打开第二本 EPUB 前会清理上一本文本 selection、active paged `ComposeView` text/selection tags、active page maps、lazy cache、目录/page count/current locator 等书籍态；`updatePagedTextSelection()` 现在检查 callback 对应 `ComposeView` 仍是 active page 且 slice 未过期，避免旧 Compose selection callback 晚到后写入新书 selection。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose selection when opening another book" --rerun-tasks` 先失败于打开第二本书后旧 selection 状态未清空；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked test no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 close lifecycle Compose state cleanup follow-up：`EpubReflowEngine.close()` 现在会在清空 active page weak maps 前清理已创建 paged `ComposeView` 的 text surface、selection tags、selection callback、gesture wiring 与 text semantics tags，避免 engine 关闭后外部仍持有的旧页继续暴露 stale selection/语义状态；关闭后的旧 callback 因 active page state 已移除，晚到调用不能写入 `currentTextSelection`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose text page state when closing book" --rerun-tasks` 先失败于 `close()` 后旧 text surface visible 仍为 `true`；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked test no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 page-local link metadata follow-up：EPUB paged block/style-aware pagination 现在会把 cached `EpubDisplayBlock.Text.links` 透传到 `EpubPageSlice.links`，并裁剪为 page-local offsets，避免分页后该页 inline link metadata 丢失。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubPageMappingTest.paged layout preserves page local link metadata" --rerun-tasks` 先失败于 `links` metadata 为空；GREEN 后同一测试通过；`EpubPageMappingTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked Kotlin no-index 空白检查无空白输出。边界：这是 JVM/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、混合图文分页视觉、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose link ranges tag follow-up：EPUB paged `ComposeView` root 现在通过 `R.id.epub_compose_text_links` 暴露当前 page-local inline link ranges，并在 text page retire / book reset 清空，避免重分页、切模式或关书后 stale link metadata 残留。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose text link ranges" --rerun-tasks` 先失败于缺少 link ranges tag；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、混合图文分页视觉、Compose selection action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose link callback follow-up：EPUB paged `ComposeView` 文本页现在暴露 `R.id.epub_compose_text_link_callback` / `R.id.epub_compose_text_link_tap_wired`，可见 Compose 文本层通过 `TextLayoutResult` tap hit-test 命中 page-local link 并复用既有链接处理；内部链接在 paged 模式不再依赖 `RecyclerView.post`，会直接更新 `currentLocator` / `chapterInfo` 并请求 ViewPager 目标页。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose link callback that navigates internal links" --rerun-tasks` 先失败于 callback 为空；GREEN 后同一测试通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、混合图文分页视觉、Compose selection action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 paged link navigation selection cleanup follow-up：EPUB paged 内链跳转离开当前文本页时会在请求目标 ViewPager 页前调用 `clearTextSelection()`，清空 engine `currentTextSelection`、活跃 `ComposeView` 的 page-local selection range 与 selection highlight，避免链接跳页后旧选区残留。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose selection when link navigation leaves active page" --rerun-tasks` 先失败于内链跳转后 selection 仍保留；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal、外链 intent、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 paged external link selection cleanup follow-up：EPUB paged 外链 callback 发出 `ACTION_VIEW` intent 前会复用 `clearTextSelection()`，清空 engine `currentTextSelection`、活跃 `ComposeView` 的 page-local selection range 与 selection highlight；Robolectric 同时校验 intent action/data/`FLAG_ACTIVITY_NEW_TASK`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose selection when external link opens intent" --rerun-tasks` 先失败于外链 intent 后 selection 仍未清空；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机外链 chooser/browser smoke、paged link tapping、TalkBack link traversal、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose URL annotation follow-up：EPUB paged 可见 Compose `BasicText` 现在使用带 link URL string annotation 的 `AnnotatedString`；page-local inline links 会写入 `URL` annotation 并加 `TextDecoration.Underline`，`R.id.epub_compose_text_annotated_string` 暴露当前文本层注解，selection 更新/清理与 text page retire/book reset 会同步刷新或清空该 tag。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose url annotations for inline links" --rerun-tasks` 先失败于缺少 annotated-string 暴露；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack link traversal/speech、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose LinkAnnotation follow-up：EPUB paged 可见 Compose `BasicText` 的 `AnnotatedString` 现在除 legacy `URL` string annotation / underline span 外，也通过 `AnnotatedString.Builder.addLink(LinkAnnotation.Url(...))` 暴露 Compose 原生 link ranges；`TextLinkStyles` 中的 underline 样式与现有 visual span 保持一致。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose link annotations for inline links" --rerun-tasks` 先失败于 link annotations 为空；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack link traversal/speech、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose LinkAnnotation listener follow-up：EPUB paged `LinkAnnotation.Url` 现在带 `LinkInteractionListener`，listener 会调用既有 `handleLinkClick()`；tag 重建、selection 更新/清理和可见 `BasicText` 使用同一 link handler，Robolectric 覆盖内部链接 activation 后 locator 更新和目标页请求。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose link annotation listener for internal links" --rerun-tasks` 先失败于 listener 为空；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack link traversal/speech、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 stale Compose link activation guard follow-up：EPUB paged link handler 现在带 active-page/slice guard；退场后的旧 `ComposeView` link callback 或 `LinkAnnotation.Url` listener 即使在切到 scroll 模式后被调用，也不会再打开外链或驱动旧页链接动作。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime ignores stale compose link activation after switching to scroll mode" --rerun-tasks` 先失败于旧 listener 仍启动外链 intent；GREEN 后同一测试通过；`EpubReflowEngineTest` class、EPUB 全量 `:render:epub:testDebugUnitTest --rerun-tasks`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合、`git diff --check` 和 touched-file 空白扫描通过。边界：这是 Robolectric/code-contract 证据，不是真机 paged link tapping、TalkBack link traversal/speech、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 mode switch Compose root progress cleanup follow-up：EPUB paged 文本页切到 scroll 模式后，旧 `ComposeView` root 会清空 `R.id.epub_compose_page_progress_description`、正文语义委托标记和 API 30+ `stateDescription`，避免退场页面继续暴露 stale 页码/进度语义；同一 reset 清理路径也覆盖 close/openBook。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose page progress semantics when switching to scroll mode" --rerun-tasks` 先失败于旧 progress tag 未清；GREEN 后同一测试通过；相邻 lifecycle targeted tests、`EpubReflowEngineTest` class、EPUB 全量 `:render:epub:testDebugUnitTest --rerun-tasks`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合、`git diff --check` 和 touched-file 空白扫描通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack speech/action-mode、paged link tapping、外链 chooser/browser smoke、混合 EPUB 视觉、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 mode switch selection cleanup follow-up：EPUB paged 文本页已有 selection 时切换到 scroll 模式，现在会清理 engine selection 与旧 paged `ComposeView` page-local selection/highlight，避免模式切换后旧 ViewPager 页继续携带 stale selection；`setMode()` 在 reading mode 真实变化时、更新 `_pagingKind` 前复用 `clearTextSelection()`，重复设置同一模式不额外清理。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose selection when switching to scroll mode" --rerun-tasks` 先失败于切换 scroll 后旧 selection 未清空；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 mode switch stale Compose callback follow-up：EPUB paged 文本页切换到 scroll 模式后，现在会清空 active paged text/page container tracking；旧 `ComposeView` 捕获的 selection callback 即使晚到触发，也不再属于 active page state，不能重新写入 engine selection 或 page-local selection/highlight。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime ignores stale compose selection callback after switching to scroll mode" --rerun-tasks` 先失败于旧 callback 晚到后重新写入 selection；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 mode switch Compose page-state cleanup follow-up：EPUB paged 文本页切换到 scroll 模式后，现在会在清空 active page tracking 前清理旧 paged `ComposeView` 的 text surface、selection tags、selection callback、gesture wiring 和 text semantics tags，避免旧 ViewPager 页继续暴露正文/选择/语义状态。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose text page state when switching to scroll mode" --rerun-tasks` 先失败于切换 scroll 后旧 text surface visible 仍为 `true`；GREEN 后同一测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 mode switch image page state cleanup follow-up：EPUB paged 图片页切换到 scroll 模式后，现在会递归清理旧 paged `ImageView.contentDescription` 与 drawable，避免旧 ViewPager 图片页继续暴露 stale alt/page 语义或视觉内容；同一清理 helper 也复用于 `openBook()` 书籍态 reset 与 `close()`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears image page accessibility state when switching to scroll mode" --rerun-tasks` 先失败于切换 scroll 后旧 `ImageView.contentDescription` 仍包含 `Cover art`；RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears image page drawable state when switching to scroll mode" --rerun-tasks` 先失败于旧 drawable 未清；GREEN 后两条 targeted 测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked test no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 active image page progress rebind follow-up：EPUB paged 图片页现在也有 active image page weak-map；字号/行距重分页后若同一 page index 仍是 image slice，会刷新旧 active `ImageView.contentDescription` 的 alt/page progress 文案，避免总页数变化后 TalkBack 读到 stale 页码；若同一 index 已映射为 text slice，会清理旧图片页 contentDescription 与 drawable。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime updates active image page progress after page count changes" --rerun-tasks` 先失败于旧图片页文案仍是旧总页数；GREEN 后同一测试通过；相邻 image page targeted tests 通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked test no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 close book progress state cleanup follow-up：`EpubReflowEngine.close()` 现在把书籍级可观察状态同步清空：`_pageCount` 归零，`_currentLocator` 回到 `LocatorStrategy.Unknown`，`_chapterInfo` 回到 `ChapterInfo(0, 1, "", 0f)`，并保留既有 `_tableOfContents` 清空，避免 close 后 stale 页数、locator 或章节进度继续外泄。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears book progress state when closing book" --rerun-tasks` 先失败于 `pageCount` 仍为 `2`；GREEN 后同一测试通过；相邻 close 生命周期测试通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 scroll goTo page request boundary follow-up：`EpubReflowEngine.goTo()` 现在只在 `_pagingKind == PAGED` 时调用 `pageRequestCallback`，internal-link post 路径也复用同一模式边界；scroll 模式的 Section locator 跳转只更新滚动位置、current locator 与 chapterInfo，不再误驱动 paged ViewPager 宿主。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.scroll runtime does not request paged navigation for section goTo" --rerun-tasks` 先失败于 callback 被调用；GREEN 后同一测试通过；相邻 paged navigation targeted test 通过；`EpubReflowEngineTest` class 通过；`:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、TalkBack speech/action-mode、Compose selection action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 ViewPager stale page request callback follow-up：`ViewPagerTransitionHost.bind()` 现在会先清理旧 `PagedReaderEngine.setPageRequestCallback(null)`，再安装新 engine callback，避免直接 rebind 到新 engine 后旧 engine 仍能通过 page request 推动新 ViewPager。Validation: RED `./gradlew --no-daemon --no-parallel :render:animate:testDebugUnitTest --tests "dev.readflow.render.animate.ViewPagerTransitionHostTest.paged host clears old page request callback when rebound" --rerun-tasks` 先失败于 `ViewPagerTransitionHostTest.kt:102`，旧 engine request 后 `pager.currentItem` 变为 `2`；GREEN 后同一测试通过；`ViewPagerTransitionHostTest` class 通过；`:render:animate:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 EPUB 混合图文分页视觉、Compose selection action-mode、TalkBack speech/action-mode、性能、真实 Calibre 或 SAF smoke 验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 paged navigation selection cleanup follow-up：EPUB paged `goTo()` 现在会在 PAGED 模式比较当前 page index 与目标 page index；跨页导航时复用 `clearTextSelection()` 清空 engine `currentTextSelection`、活跃 `ComposeView` 的 `R.id.epub_compose_text_selection_range` 和 selection highlight state，避免 ViewPager 翻到下一页后上一页选区残留。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime clears compose selection when page navigation leaves active page" --rerun-tasks` 先失败；GREEN 后同一测试通过；串行 targeted `EpubReflowEngineTest...page navigation...` + `EpubSelectionTest` 通过；EPUB 全量 `:render:epub:testDebugUnitTest --rerun-tasks` 通过；phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 与 touched untracked Kotlin no-index 空白检查无空白输出。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose outside-text initial selection follow-up：EPUB paged Compose 长按 hit-test offset 位于文本范围外时不再被 `epubCodePointStartAt()` clamp 到首/末字符并误选边缘词；`epubComposeInitialSelectionRange()` 对 `offset !in 0 until text.length` 返回 `null`，`epubComposeInitialSelectionAt()` 继续产出 collapsed range 与 `anchor=null`，让后续 callback 清理旧 selection。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection event reports collapsed range outside text" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose typographic apostrophe initial selection follow-up：EPUB paged Compose 初始词选区现在把常见排版右单引号 `\u2019` 纳入拉丁词连接符；`reader\u2019s` 这类正文长按不会被拆成 `reader` / `s`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection expands typographic apostrophe word" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose standalone connector punctuation follow-up：EPUB paged Compose 初始词选区现在只把 `'` / `\u2019` / `-` 作为词内部连接符保留；连接符独立出现或被 trim 到词边缘时返回 `null`，避免长按分隔符生成无意义 selectedText。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection ignores standalone word connector punctuation" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose Unicode hyphen initial selection follow-up：EPUB paged Compose 初始词选区现在把 Unicode hyphen `\u2010` 和 non-breaking hyphen `\u2011` 纳入词内部连接符；`well\u2010being` / `well\u2011being` 这类正文长按不会被拆成半个词，standalone/edge connector trim 语义保持不变，en/em dash 不纳入词连接。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection expands unicode hyphen word" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 selection edge trim follow-up：EPUB selection 的共同出口 `epubTextSelection()` 现在会在 code point/text-element 边界规整后，继续清理 selection 两端的空白、non-breaking space 和 standalone word connector；Compose 拖选/callback 落在分隔符边缘时不再生成 `- beta` 这类带边缘噪声的 selectedText，start/end locator 会落到 trim 后正文锚点。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.selection trims edge whitespace and standalone word connectors" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose callback trim mirror follow-up：EPUB paged Compose selection callback 现在使用 shared EPUB selection 的 trimmed 有效范围同步 `R.id.epub_compose_text_selection_range` 与 `R.id.epub_compose_text_selection_highlight_range`；当 callback 原始 range 覆盖 `- beta` 但 `epubTextSelection()` 清理为 `beta` 时，Compose 可观察 range/highlight 也落在 `beta`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime mirrors trimmed compose callback selection range into compose state" --rerun-tasks` 先失败于 tag range 未 trim；GREEN 后同一测试通过；相邻 `EpubSelectionTest`、callback code point、callback combining mark、blank selection targeted 回归通过；EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose emoji/ZWJ initial selection follow-up：EPUB paged Compose 长按初始 selection 现在会把 emoji symbol 与 ZWJ 序列作为单个可见 text element 选中；`epubComposeInitialSelectionRange()` 对非词 symbol path 会扩展 variation selector、肤色 modifier 与 zero-width-joiner 串，并要求 range 内包含 selectable symbol，标点分隔符仍返回 `null`。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection keeps emoji zwj sequence to one text element" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 emoji Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose drag emoji/text-element focus boundary follow-up：EPUB paged Compose 拖选 focus 现在会先规整到完整可见 text element 后再扩展 range；`EpubComposeInitialSelection` 携带原文，`epubComposeDragSelectionRange()` 通过 code point / `BreakIterator` text element 边界和 emoji symbol sequence 扩展，避免 focus 落在 `👩‍💻` 内部时产生半个 surrogate 或半个 ZWJ 序列。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose drag selection keeps emoji focus on text element boundary" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、`EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring`、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 和 touched untracked Kotlin 文件 no-index whitespace check 无空白输出。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose drag standalone connector focus cleanup follow-up：EPUB paged Compose 拖选 focus 落在独立 word connector 时不再把连接符和边缘空白扩进 page-local range；`epubComposeDragSelectionRange()` 复用 selection edge trim 清理候选 range，`epubComposeDragFocusRange()` 只把词内部连接符继续当作可扩展 focus。Validation: emoji modifier guard `EpubSelectionTest.compose drag selection keeps emoji modifier focus on text element boundary` 直接通过；RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose drag selection ignores standalone word connector focus" --rerun-tasks` 先失败于 actual `0..6`；GREEN 后同一测试通过；`EpubSelectionTest` class、`EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring`、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过；`git diff --check` 和 touched untracked Kotlin 文件 no-index whitespace check 无空白输出。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose drag selection follow-up：EPUB paged Compose 长按拖选现在用 `EpubComposeInitialSelection` 保存初始词 range/anchor，`epubComposeDragSelectionRange()` 在向左拖时返回 `focus..initialWordEnd`，向右拖时返回 `initialWordStart..focus`，focus 仍在初始词内时保留完整初始词；空白/collapsed 初始 selection 不生成拖选 range。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose drag selection keeps initial word when dragging before it" --rerun-tasks` 先失败；GREEN 后同一测试通过；`EpubSelectionTest` class、相邻 gesture/callback targeted regressions、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose initial combining-mark boundary follow-up：EPUB paged Compose 长按初始 selection 现在会先把 hit-test offset 扩到用户可见 text-element/grapheme，再用 text-element range 做词边界扫描；`Cafe\u0301` 这类 decomposed accent 序列在长按 base `e` 或 combining mark offset 时都会返回完整词选区。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection keeps combining mark with word range" --rerun-tasks` 先失败于只选中 base letter / mark offset 无 range；GREEN 后同一测试通过；相邻 initial-selection / callback boundary targeted tests 通过；EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- PAGE-05 Compose combining-mark selection boundary follow-up：EPUB paged Compose selection callback 现在在 code point 边界之外，会用 `BreakIterator.getCharacterInstance(Locale.ROOT)` 把 page-local start/end 扩到用户可见 text-element/grapheme 边界；decomposed accent / combining mark 不再被选成半个可见字形。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime keeps compose callback selection on combining mark boundaries" --rerun-tasks` 先失败于 callback 只写入 base letter range；GREEN 后同一测试通过；相邻 selection/callback targeted tests 通过；EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- PAGE-05 Compose callback code point boundary follow-up：EPUB paged Compose selection callback 现在会通过 `epubSelectionRangeOnCodePointBoundaries()` 把 page-local start/end 规整到 Unicode code point 边界，再同步 engine selection、`R.id.epub_compose_text_selection_range` 和 selection highlight；拖选 focus 落在 CJK Extension-B surrogate pair 低代理项时不会产生半个 Unicode 字符 range。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime keeps compose callback selection on code point boundaries" --rerun-tasks` 先失败于 callback 写入 `0..1` 半 surrogate range；GREEN 后同一测试通过；相邻 selection/callback targeted tests 通过；EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 CJK Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- PAGE-05 Compose CJK initial selection follow-up：`epubComposeInitialSelectionRange()` 现在改为 code point aware，会把中文/日文假名/韩文等无空格 CJK/Hangul 长按起点限制为单个 Unicode code point range，避免整句/整段连续文字被初始选中，并覆盖 CJK Extension-B surrogate pair；ASCII/拉丁字母、数字、下划线、撇号和连字符仍按词边界扩展，空白 offset 的 collapsed 清理行为不变。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection keeps cjk long press to one character" --rerun-tasks` 先失败于当前 helper 过度扩展；RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubSelectionTest.compose initial selection keeps cjk surrogate pair to one code point" --rerun-tasks` 先失败于 surrogate pair 返回非完整 code point range；GREEN 后同一测试与相邻 initial selection targeted tests 通过；EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/code-contract 证据，不是真机中日韩文本 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose long-press initial selection follow-up：EPUB paged Compose 文本层现在会在长按起点先调用 `epubComposeInitialSelectionRange()`，把 hit-test offset 扩展到连续字母/数字/下划线/撇号/连字符区间并立即调用 Compose selection callback；空白 offset 不扩展为词选区，但 `epubComposeInitialSelectionAt()` 会返回 collapsed range 并走同一 callback，借由 `epubTextSelection(...)` 的空/blank 清理路径清空旧 engine selection、Compose selection range 和 selection highlight，避免 stale 高亮残留。Validation: RED/GREEN `EpubSelectionTest.compose initial selection expands long press offset to word range`、`EpubSelectionTest.compose initial selection ignores whitespace offsets`、`EpubSelectionTest.compose initial selection event reports word range with anchor`、`EpubSelectionTest.compose initial selection event reports collapsed range for whitespace`、`EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring`、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 JVM/Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 active Compose progress semantics follow-up：EPUB paged active page rebind 现在会按重建后的 `pagedSlices.size` 刷新 `R.id.epub_compose_page_progress_description` 与 API 30+ `stateDescription`，并 clamp active page index，避免行距/字号改变 page count 后 TalkBack/无障碍进度语义仍停在旧总页数。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime updates compose page progress after line spacing rebuilds page count`、相邻重分页 targeted tests、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 active Compose repagination follow-up：EPUB paged 文本页的 active page state 现在记录 page index；`setLineSpacing()` / `setFontSize()` 重建 `pagedSlices` 后，已创建的 `ComposeView` 会在 rebind 时换到当前 page index 对应的新 `EpubPageSlice`，同步根 tag/page text/highlight，并清空旧 page-local Compose selection range/highlight 与 engine `currentTextSelection`，避免重分页后沿用 stale slice 或 stale selection。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime rebinds active compose page to rebuilt line spacing slice`、RED/GREEN `EpubReflowEngineTest.paged runtime clears page local compose selection when line spacing rebuilds slices`、EPUB 全量 `:render:epub:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 transparent overlay native selection off follow-up：`SelectionAwareTextView.nativeTextSelectionEnabled` 默认 `true`，保持普通 TXT/MD 文本视图原生选择；EPUB paged 透明 overlay 在创建和 Compose rebind 时关闭 native selection，使兼容桥保持 `isTextSelectable=false`，selection 更新继续走 Compose callback bridge 到 engine `currentTextSelection` 与 Compose selection range tag。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay out of native selection while compose callback selects text`、TXT 默认行为守护、TXT/EPUB 单测和 phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 transparent overlay empty bridge follow-up：EPUB paged 透明 overlay 不再复制整页正文或高亮 spans，创建与标注 rebind 后都保持空文本；可见正文与高亮由 Compose surface/tag 承担，Compose selection callback 仍可写入 engine `currentTextSelection` 与 Compose selection range tag。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay text empty while compose surface owns content after rebind`、TXT/EPUB 单测和 phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机视觉、TalkBack speech/action-mode、Compose selection action-mode 或性能验收，PAGE-05 保持 `PARTIAL`。
- PAGE-05 detached selection bridge follow-up：`EpubComposeTextPage` 不再用 Compose `AndroidView` 承载空 `SelectionAwareTextView`，Compose UI tree 只保留前景 `SelectionContainer` / `BasicText` 文本与 gesture path；当时 bridge 对象仍保留在 `ComposeView` tag 和 active weak-map 中，用于复用现有 selection callback/state 更新路径但不挂载到 UI 树，后续 2026-06-24 view-free follow-up 已把该 View bridge 移除。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps selection bridge outside compose tree while compose gesture text owns foreground`、相邻 native-selection/empty-bridge 回归、TXT/EPUB 单测和 phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 view-free selection state follow-up：EPUB paged 文本页不再创建或暴露 `SelectionAwareTextView` bridge；`R.id.epub_selection_overlay_view` 保持 `null`，active page rebind/clear/selection state 改由 `WeakHashMap<ComposeView, EpubPagedTextPageState>` 维护，Compose callback 直接写入 engine `currentTextSelection`、`R.id.epub_compose_text_selection_range` 和 `R.id.epub_compose_text_selection_highlight_range`。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime updates compose selection without selection aware text view bridge`、EPUB 全量 `:render:epub:testDebugUnitTest`、TXT 全量 `:render:txt:testDebugUnitTest`、phase2 `:render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection action-mode、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 JVM/compile 证据：EPUB paged mode 已从 paragraph-per-page 推进到 StaticLayout/measured-line 辅助的 `EpubPageSlice` 段内 TextView 页切片；`EpubPageMapping` 新增 `EpubPageMetrics` / `epubViewportPagedLayout`，会按视口宽高、padding、平均字宽和 line height 估算 page size，并在有实际文本时优先靠近词/空白边界切分。`EpubReflowEngine` paged 模式使用 slice count，按 page substring 渲染，高亮/文字选择 offset 会转换回原段落 offset；字号/行距变化会重建切片；`PagedReaderEngine.pageIndexForLocator()` 让 ViewPager2 可从 Section locator 恢复正确切片页。Follow-up: `EpubLazyBook.cachedParagraphAt()` 让 runtime paged slice builder 使用已缓存 lazy spine 正文作为 `textProvider`，因此已 warm 的 spine 可启用词/空白边界切分，同时不会为了分页加载冷 spine。StaticLayout follow-up: `EpubReflowEngine.buildPagedSlices()` 改为调用 `epubStaticLayoutPagedLayout()`，在有缓存正文时通过 Android `StaticLayout` 测量 visual line ranges，并按 viewport-derived `linesPerPage` 合并为 page slices；`epubMeasuredPagedLayout()` 提供可注入 line breaker 的 JVM/Robolectric 测试入口，空/冷文本仍回退到 indexed/viewport slicing。图片 follow-up: `EpubPageSliceKind.Image` 与 `epubPagedLayoutWithBlocks()` 会把已缓存 lazy spine 中的 `EpubDisplayBlock.Image` 插入独立 page slice，`EpubReflowEngine.createPageView()` 对图片页渲染 `ImageView` 并复用安全 decode / alt text；`EpubLazyBook.cachedBlocks()` 只读取已缓存 spine，避免为分页加载冷 spine。图片 locator follow-up: image slice 现在锚到关联段落末尾，`epubPageIndexFromLocator()` 会先匹配零宽图片页锚点，避免从图片页恢复/导航时落回前一个文本页。文本样式 follow-up: `EpubPageTextStyle` 会把 cached `EpubDisplayBlock.Text` 的 heading、pre/table、list/blockquote 和 indent 元数据透传到 paged text slice，`EpubReflowEngine.createPageView()` 复用标题加粗/字号、等宽、缩进和 blockquote padding。样式感知测量 follow-up: `epubMeasuredPagedLayout()` / `epubPagedLayoutWithBlocks()` 现在会把 `EpubPageTextStyle` 传给 line breaker，runtime `StaticLayout` 使用标题字号/粗体和 pre/table 等宽字体对应的 `TextPaint` 测量 visual line ranges。Compose/TextLayoutResult follow-up: `EpubPageMeasurement`、`EpubTextLayoutLineRange` 与 `epubComposeMeasuredPagedLayout()` 让分页核心可消费 TextLayoutResult 风格 visual line ranges，并把 page slice 标记为 `ComposeTextLayoutResult` 来源；fallback follow-up 让 TextLayoutResult-style line ranges 为空时生成的完整文本页也保留 `ComposeTextLayoutResult` measurement 与 `EpubPageTextStyle`。Validation: targeted lazy/page-mapping、EPUB 全量单测和 phase2 `:app:assembleDebug` 组合均通过；measured-line follow-up 的 targeted `EpubPageMappingTest.static layout paged layout follows measured visual line breaks`、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过；图片 follow-up 的 `EpubPageMappingTest.paged layout inserts cached image blocks as standalone slices`、`EpubLazyBookTest.cached block lookup does not load cold spines for paged image slices`、图片 locator follow-up 的 RED/GREEN `EpubPageMappingTest.image page slice maps to a distinct section offset after preceding text`、文本样式 follow-up 的 `EpubPageMappingTest.paged layout preserves cached text block style metadata`、样式感知测量 follow-up 的 RED/GREEN `EpubPageMappingTest.paged layout measures cached text blocks with their style metadata`、Compose/TextLayoutResult follow-up 的 RED/GREEN `EpubPageMappingTest.compose text layout result line ranges are preserved as compose measured page slices`、fallback follow-up 的 RED/GREEN `EpubPageMappingTest.compose measured pagination keeps measurement and style when line ranges are empty`、EPUB 全量单测和同一 phase2 组合通过。边界：仍不是 runtime `ComposeView` 真分页；空/冷 spine 仍按 indexed/viewport slicing 且不显示冷 spine 图片；真机视觉、帧率/内存预算和完整图文分页证据仍缺，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 real-corpus paged gate：`EpubCorpusSmokeTest` 新增 `real epub corpus preserves paged layout coverage and locator round trips`，直接对 `/tmp/readflow-epub-corpus` 5 本公开 EPUB 跑 `epubPagedLayoutWithBlocks()` 的真实分页契约。该用例验证：每个 page slice 都能经 `epubLocatorForPageSlice()` -> `epubPageIndexFromLocator()` round-trip；text page 的 `totalProgression` 单调；image page 保留 `href/alt`；page-local links 不越界且保留 targeted internal-link 命中；styled text blocks 保留 `EpubPageTextStyle`；measurement 来源维持 `ComposeTextLayoutResult`。Validation: 先 RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubCorpusSmokeTest.real epub corpus preserves paged layout coverage and locator round trips" -Dreadflow.epubCorpusDir=/tmp/readflow-epub-corpus --rerun-tasks`，在 `idpf-childrens-literature.epub` 的 image-only `cover.xhtml` 上暴露“不能要求所有视觉页 `totalProgression` 全局单调”的测试假设错误；校正 gate 为“所有页 round-trip + text page progression 单调”后 GREEN。最终 targeted 结果 XML `android/render/epub/build/test-results/testDebugUnitTest/TEST-dev.readflow.render.epub.EpubCorpusSmokeTest.xml` 记录 `EPUB_PAGED_CORPUS|gutenberg-23950-san-guo.epub|pages=9793|imagePages=0|linkPages=6|styledPages=29|targetedInternalLinkPages=0`、`gutenberg-4094-chinese-classics.epub|pages=5967|imagePages=0|linkPages=6|styledPages=5720|targetedInternalLinkPages=0`、`idpf-childrens-literature.epub|pages=4329|imagePages=1|linkPages=119|styledPages=220|targetedInternalLinkPages=92`、`idpf-cole-voyage.epub|pages=128|imagePages=9|linkPages=0|styledPages=7|targetedInternalLinkPages=0`、`idpf-indexing.epub|pages=5311|imagePages=11|linkPages=862|styledPages=1260|targetedInternalLinkPages=959`。后续 `./gradlew :render:epub:testDebugUnitTest -Dreadflow.epubCorpusDir=/tmp/readflow-epub-corpus --rerun-tasks` 与 phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 也均通过。边界：这是 JVM/real-corpus 契约证据，不是真机 mixed text/image 视觉分页、Compose selection action-mode、TalkBack speech/link traversal、帧率/内存预算或真实用户手势验收，所以 PAGE-05 仍保持 `PARTIAL`。
- 最新 PAGE-05 same-paragraph multi-image locator follow-up：在 real-corpus gate 之外，又用纯本地 RED/GREEN 把一个真实 paged locator 冲突钉住了。新增 `EpubPageMappingTest.multiple image page slices in one paragraph round trip to distinct pages` 后发现：同一段里如果存在两张以上图片，旧实现会让它们共享“段尾”图片锚点，第二张图 `epubLocatorForPageSlice()` 生成的 locator 经 `epubPageIndexFromLocator()` 回跳时会落回第一页图片。修复后，`epubPagedLayoutWithBlocks()` 会为同段 successive image slice 分配稳定且彼此不同的 synthetic paragraph-local image anchors（段尾、段尾+1、…）；`epubLocatorForOffset()` 允许图片页保留这种超出正文长度的 `Section.charOffset`，但 `totalProgression` 仍按该段正文末尾计算，不把图片页伪装成新的文本进度。Validation: RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubPageMappingTest.multiple image page slices in one paragraph round trip to distinct pages" --rerun-tasks` 先失败于第二张图回到第一页图片；GREEN 后该 targeted test 与既有 `EpubPageMappingTest.image page slice maps to a distinct section offset after preceding text` 都通过；随后 `./gradlew :render:epub:testDebugUnitTest --rerun-tasks` 与 phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 JVM/code-contract 修复，不是真机 mixed image gallery、TalkBack speech、Compose selection action-mode、视觉或性能验收，PAGE-05 仍保持 `PARTIAL`。
- 最新 PAGE-05 image-fragment internal-link follow-up：这次继续沿着同一套 synthetic image anchor 语义把 fragment target 链路补齐。旧实现里 `fragmentTargetIndexes` / `epubInternalLinkTargetIndexes(...)` 只存段落索引，导致 `<img id="scene">` 这类 fragment 在 TOC 和 runtime internal link 里都会丢掉图片页 offset，paged 跳转只能落回前一个文本页。修复后新增 `EpubTargetPosition(paragraphIndex, paragraphOffset)`：`epubFragmentTargetIndexes()` 会为同段图片 fragment 分配段尾、段尾+1…的 synthetic paragraph-local image anchors；`EpubParser`/`EpubLazyBook` 会把该结构保留到 `fragmentTargetIndexes`；`buildEpubToc(...)` 与 `EpubReflowEngine.goToInternalLink()` 都改为通过 `epubLocatorForTarget(...)` 生成带 offset 的 `Section` locator。这样 nav/ncx TOC 和 paged runtime internal link 在命中图片 fragment 时，都能落到真正的图片页，而不是前一张文本页。Validation: 先 RED `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime internal links can target image fragments" --rerun-tasks`，补入 `EpubLinkTargetsTest.fragment targets preserve synthetic image anchors within one paragraph` 与 `EpubParserTocTest.parseBook and lazy parse map toc image fragments to synthetic image anchors` 后 GREEN；随后 `./gradlew :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubLinkTargetsTest" --tests "dev.readflow.render.epub.EpubParserTocTest" --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime internal links can target image fragments" --rerun-tasks`、`./gradlew :render:epub:testDebugUnitTest --rerun-tasks`、phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 和 `git diff --check` 通过。边界：这仍是 JVM/Robolectric/code-contract 证据，不是真机 mixed text/image 内链点击、TalkBack link traversal/speech、Compose selection action-mode、视觉或性能验收，PAGE-05 仍保持 `PARTIAL`。
- 最新 PAGE-05 image-only spine synthetic paragraph closure：这一轮继续把 synthetic image anchor 语义收口到 image-only cover spine 本身。`epubParagraphPlan()` 现在会为 image-only spine 保留稳定 synthetic paragraph slot，把 spine 起始 `Break` 延后绑定到该 spine 首个真实 text/image paragraph，只在纯 trailing-break spine 上落到 synthetic paragraph；与之配套，`epubPagedLayoutWithBlocks()` 不再让 `EpubDisplayBlock.Break` 提前把 paragraph 标成 emitted，避免 image-only cover / leading-break contract 引入后，同段 `Break -> Text` 把首个正文页吞掉。这样 image-only cover spine 的 TOC / fragment target / runtime open-book / paged page mapping 都能共用同一 synthetic paragraph 语义，同时 paged 模式既不会在 cover 前插空白文本页，也不会因为 leading break 把正文页丢失。Validation: 新增 RED/GREEN `EpubPageMappingTest.paged layout keeps text page when leading break shares the first paragraph`，并回归 `EpubLinkTargetsTest.fragment targets in image only spine use synthetic spine paragraph`、`EpubPageMappingTest.paged layout does not emit blank synthetic text page before image only spine`、`EpubParserTocTest.parseBook keeps toc entry for image only cover spine before later text spine`、`EpubReflowEngineTest.paged runtime starts on image only cover spine without blank text page`、`EpubReaderItemParserTest.text and heading reader items can feed existing paragraph renderer`；随后 `./gradlew :render:epub:testDebugUnitTest --rerun-tasks`、phase2 `./gradlew -Preadflow.phase=2 :render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 与 `git diff --check -- android/render/epub/src/main/kotlin/dev/readflow/render/epub/EpubPageMapping.kt android/render/epub/src/test/kotlin/dev/readflow/render/epub/EpubPageMappingTest.kt docs/tracking/ACTIVE.md docs/tracking/linreads-architecture-docs-2026-06-18.md` 全部通过。边界：这仍是 JVM/Robolectric/code-contract 证据，不是真机 mixed cover/text 视觉分页、TalkBack speech/link traversal、Compose selection action-mode 或性能验收，因此 PAGE-05 继续保持 `PARTIAL`。
- 最新 A-02 performance follow-up：本轮把当前环境里还能直接落地的 TXT/PDF 性能热点收了一次。TXT 侧 root cause 先落在“reader 打开 app-private `file://` 已导入书籍时还会再复制到 `readflow-txt-*` temp file”，因此 `TxtVirtualPagerEngine.openBook()` 现在会优先识别 `filesDir/cacheDir` 下的 canonical `file://` 资产并直接索引；只有外部 `content://`/非 app-private 文件才继续走 copy-to-temp，且仅在需要恢复 engine-state 时才顺手计算 fingerprint。Validation: 新增 `TxtVirtualPagerEngineTest.openBook reuses app private file uri without creating a temp txt copy`，配合 `./gradlew :render:txt:testDebugUnitTest --tests "dev.readflow.render.txt.TxtVirtualPagerEngineTest" --rerun-tasks` 与 phase2 `./gradlew -Preadflow.phase=2 :render:txt:testDebugUnitTest :features:reader:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:txt:compileDebugKotlin :app:assembleDebug` 通过。PDF 侧 root cause 则先落在“paged `createPageView()` 同步 `PdfRenderer.openPage + render` 把整页位图渲染压在 UI 路径上”，因此新增 `PdfPageBitmapStore`：串行异步 render、同页 request coalescing、首屏预热、邻页 prefetch、close/cancel 清理都由 store 接管；`PdfRendererEngine` 的 paged `ImageView` 改为优先吃缓存，未命中时先展示空页宿主再异步回填 bitmap，并保留旧的 `PdfBitmapAttachmentRegistry` 防止回收中位图仍被 `ImageView` 引用。Validation: 新增 `PdfPageBitmapStoreTest` 守住 coalescing/cancel/release 语义，并跑通 `./gradlew :render:pdf:testDebugUnitTest --tests "dev.readflow.render.pdf.PdfPageBitmapCacheTest" --tests "dev.readflow.render.pdf.PdfPageBitmapStoreTest" --tests "dev.readflow.render.pdf.PdfBitmapAttachmentRegistryTest" --rerun-tasks`、phase2 `./gradlew -Preadflow.phase=2 :render:txt:testDebugUnitTest :render:pdf:testDebugUnitTest :features:reader:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:pdf:compileDebugKotlin :render:txt:compileDebugKotlin :app:assembleDebug`、`./gradlew -Preadflow.phase=2 :app:assembleOta` 和 touched-file `git diff --check`。AVD spot-check 证据统一落在 `/tmp/readflow-a02-stage-20260625`：TXT `ACTION_VIEW` 冷启代理首轮 `2089ms`，重复两次仍在 `1047ms` / `1353ms`，说明 duplicate-copy removal 尚未把 A-02 拉进 `<500ms`；PDF 若在 reader 稳定进入 `第 1 页，共 50 页` 后再 `dumpsys gfxinfo ... reset` 并做两次翻页，则 `pdf-10mb-50p-ota-aligned2-gesture-gfxinfo.txt` 记录 `69 frames / 28.99% jank / p90 61ms / p95 73ms / p99 97ms / Slow UI thread 8 / Slow issue draw 20`，相较 2026-06-22 的 `87 frames / 64.37% jank / p90 65ms / Slow UI thread 24 / Slow issue draw 56` 有阶段改善，但仍未过 `<50ms` frame-paced gate。结论：这一轮确认了“TXT duplicate copy 不是 A-02 唯一瓶颈”，同时 PDF async page-bitmap 路径对 AVD 翻页卡顿有帮助但未收口预算；A-02 继续保持 `PARTIAL`，下一阶段仍应优先盯真实手机/平板、真实语料和 TXT open/import 链路剩余热段，而不是再回头做零散文档回填。
- 最新 A-03 accessibility follow-up：reader/library 的 TalkBack 语义已经先在代码契约层收口，并在 2026-06-26 补成 repo-owned AVD runtime smoke。此前实现侧新增 `ReaderAccessibilityLabels.kt` 与 `ReaderAccessibilityLabelsTest`，把进度、章节、搜索结果、书签、标注、TOC 的中文朗读文案固定成可单测契约；`ReaderScreen` 给全书进度条补 `contentDescription="全书进度"` + percent `stateDescription`，章节中区改为整块朗读，底部快捷按钮合并容器语义，`字号` / `行距` slider 独立暴露语义；`BookGrid.kt` 把书卡/菜单细化为 `打开 <书名>` 与 `<书名> 的菜单`。最新 runtime 侧新增 `android/app/src/phase2AndroidTest/java/dev/readflow/a03/A03AccessibilityRuntimeSmokeTest.kt`，用真实 `MainActivity` 走 ACTION_VIEW 导入 EPUB、返回书库、从书卡打开、reader chrome 与 TOC/search/bookmark/annotation/font/theme 面板检查。Validation: `./gradlew -Preadflow.phase=2 :app:compileDebugAndroidTestKotlin :app:installDebugAndroidTest`；`adb -s emulator-5554 shell am instrument -w -e class dev.readflow.a03.A03AccessibilityRuntimeSmokeTest dev.readflow.test/androidx.test.runner.AndroidJUnitRunner` = `OK (1 test)`。证据 `/tmp/readflow-a03-accessibility-runtime-20260626/a03-accessibility-runtime-smoke/a03-accessibility-summary.txt` 记录书卡 `打开 a03-accessibility-*`、菜单 `<title> 的菜单`、reader surface `阅读内容，捏合调整字号`、action `显示或隐藏阅读工具栏`、TOC `1 级目录，A03 Start`、搜索 actions、空书签/标注、`字号/16sp`、`行距/1.75x` 和主题选项；同目录拉取 22 个截图/XML/DB 文件，run-window logcat 关键崩溃/OOM/权限 grep 为空。当前 AVD settings 记录 TalkBack bound、`accessibility_enabled=1`、`touch_exploration_enabled=1`。边界：这仍是 AVD instrumentation + UIAutomator/XML/screenshot 证据，不是物理手机/平板、真人 TalkBack speech/TTS 或完整手动焦点顺序遍历；A-03 继续保持 `PARTIAL`。
- 最新 UX-01 tap-zone follow-up：这轮确认 UX-01 之前不只是“差真机”，还存在一个代码层语义缺口。来源表要求 `scroll formats do not lose natural scrolling`，但连续滚动宿主 `NoTransitionHost.next()/previous()` 实际会按 locator 推进；旧实现里 `ReaderTapContainer` 无论 paged/continuous 都把左右点击映射到 `PreviousPage/NextPage`，因此 TXT/MD/EPUB scroll 模式存在“左右点偷偷跳段”的风险。修复后，`readerTapZoneForTap()` 新增 `pagedTapZonesEnabled` 边界：连续滚动格式只保留中间区 `ToggleChrome`，左右区返回 `null`；`ReaderTapContainer` 会根据 `pagingKind == PAGED` 才开启左右 tap-zone 与 `上一页/下一页` 无障碍 action，scroll 模式只保留点击显示/隐藏工具栏。Validation: `./gradlew :features:reader:testDebugUnitTest --tests "dev.readflow.features.reader.ReaderTapZoneTest"`、`./gradlew :features:reader:compileDebugKotlin -Preadflow.phase=2`、`./gradlew -Preadflow.phase=2 :app:installDebug` 全绿；新增 AVD smoke `/tmp/readflow-ux01-stage-20260625` 先把 1MB TXT 滚到中段 `baseline_first_line=000045`，随后左点、右点、中点连续验证，`summary.txt` 记录 `left_first_line=000045`、`right_first_line=000045`、`center_first_line=000045`、`center_has_chrome=True`，说明连续滚动 TXT 已收口为“左右点不跳段，中间点开 chrome”。结论：UX-01 这轮已经把代码层缺口收掉，剩余边界主要是分页格式的人手 ergonomics、平板布局和真实手指容错；因此 UX-01 仍保持 `PARTIAL`，但后续应把重心放到真机/平板验收，而不是继续改 tap-zone 逻辑本身。
- 最新 PAGE-05 Compose/block measurement follow-up：`epubPagedLayoutWithBlocks()` 新增 `measurement` 参数并传入 `epubMeasuredPagedLayout()`，图文/样式感知分页路径也可保留 `ComposeTextLayoutResult` 来源。Validation: RED/GREEN `EpubPageMappingTest.block paged layout can preserve compose measurement source`、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这仍不是 runtime `ComposeView` 真分页，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose/block cold-cache fallback follow-up：当 block/style-aware 分页路径的 cached text 为空、回退 indexed/viewport slicing 时，fallback page slices 现在也保留调用方传入的 `ComposeTextLayoutResult` measurement 和 cached `EpubPageTextStyle`。Validation: RED/GREEN `EpubPageMappingTest.block paged layout preserves compose measurement and style when cached text is empty`、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这仍不是 runtime `ComposeView` 真分页，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 runtime Compose measurement source follow-up：`EpubReflowEngine` 新增 internal `EpubPageLineMeasurer` 注入点，公开 `EpubReflowEngine(context)` 构造保持不变；runtime `buildPagedSlices()` 通过当前 measurer 把 `ComposeTextLayoutResult` measurement 传入 `epubPagedLayoutWithBlocks()`。Robolectric 覆盖 `openBook()` → `setMode(PAGED)` → `createPageView(0)` 路径，并从 tagged `EpubPageSlice` 断言 measurement 保持 `ComposeTextLayoutResult`。Validation: `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime can use compose text layout measurement source" --rerun-tasks`、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这只是 runtime measurement 来源预接线，不是实际 `ComposeView` / 真实 `TextLayoutResult` 排版分页，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 default runtime Compose TextMeasurer follow-up：公开 `EpubReflowEngine(context)` 默认 paged measurement 现在使用 Compose `TextMeasurer.measure()` 生成真实 `TextLayoutResult` line ranges，并把 `getLineStart()` / `getLineEnd(visibleEnd = true)` 转为 `EpubTextLayoutLineRange`；`TextStyle` 会按当前字号、行高、标题加粗/字号、Serif/Monospace 构建。Robolectric 覆盖默认构造 `openBook()` → `setMode(PAGED)` → `createPageView(0)` 路径，并从 tagged `EpubPageSlice` 断言 measurement 为 `ComposeTextLayoutResult`。Validation: `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime uses real compose text layout measurement by default" --rerun-tasks`、`./gradlew :render:epub:testDebugUnitTest`、phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 组合通过。边界：这仍不是 per-page `ComposeView` 视觉分页，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 ComposeView host follow-up：`:render:epub` 启用 Compose 编译插件和 `buildFeatures.compose`，EPUB paged 文本页 `createPageView()` 现在返回包含 `ComposeView` 的页面宿主，并在 `ComposeView.setContent { AndroidView(...) }` 中临时承载既有 `SelectionAwareTextView`，保留 selection/highlight callback 与 slice tag。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime renders text pages with compose view host" --rerun-tasks` 失败于 `assertNotNull(ComposeView)`；GREEN 后同一 targeted test、`./gradlew :render:epub:testDebugUnitTest`、phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：文本内容仍由 Android `SelectionAwareTextView` 渲染，不是纯 Compose `Text` / selection，且没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 ComposeView root follow-up：EPUB paged 文本页 `createPageView()` 现在直接返回 `ComposeView` 根节点，根节点持有 page slice tag、页码 `contentDescription` 和页面背景；`setContent { AndroidView(...) }` 仍临时承载既有 `SelectionAwareTextView`，保留 selection/highlight callback。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime returns compose view as text page root" --rerun-tasks` 失败于 `assertTrue(page is ComposeView)`；GREEN 后同一 targeted test、`./gradlew :render:epub:testDebugUnitTest`、phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：文本仍不是纯 Compose `Text` / selection，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose text surface follow-up：`:render:epub` 增加 Compose foundation，EPUB paged 文本页 `ComposeView.setContent` 现在包含透明 `BasicText` 文本面，并继续用 `AndroidView(SelectionAwareTextView)` 覆盖选择/高亮兼容路径；`R.id.epub_compose_text_surface` keyed tag 暴露 page text，避免 Robolectric 依赖脆弱的 Compose 内部树反射。Validation: RED `./gradlew --no-daemon --no-parallel :render:epub:testDebugUnitTest --tests "dev.readflow.render.epub.EpubReflowEngineTest.paged runtime exposes compose text surface state" --rerun-tasks` 失败于 tag 为空；GREEN 后同一 targeted test、`./gradlew :render:epub:testDebugUnitTest`、phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：Compose 文本面目前非交互且透明，真实可见文本/selection/highlight 仍由 Android `SelectionAwareTextView` 承担，没有真机视觉/性能证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose visible text/highlight follow-up：EPUB paged 文本页从透明 Compose 文本面推进到可见 Compose `BasicText` 渲染层，分页高亮 ranges 迁到 Compose `AnnotatedString` 背景样式；`SelectionAwareTextView` 继续作为透明 `AndroidView` 覆盖层保留长按选择 callback，字号、行距、主题和标注刷新会重新绑定活跃分页页的 Compose 文本层。Validation: RED `EpubReflowEngineTest.paged runtime makes compose text visible while selection overlay stays non visual` 失败于可见状态 tag 为空；RED `EpubReflowEngineTest.paged runtime exposes compose text highlight ranges` 失败于高亮 ranges tag 为空；GREEN 后两个 targeted tests、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：可见文本和高亮已由 Compose 承担，但 selection 仍不是纯 Compose selection；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose selection state follow-up：EPUB paged 文本页现在会把透明 `SelectionAwareTextView` overlay 产生的 page-local selection range 同步到 `ComposeView` keyed tag，并在 `clearTextSelection()` 时清空活跃 Compose 页 selection range。Validation: RED `EpubReflowEngineTest.paged runtime mirrors overlay selection range into compose state` 失败于 overlay view tag 为空；RED `EpubReflowEngineTest.paged runtime clears compose selection range with engine selection` 失败于 stale selection range 未清理；GREEN 后两个 targeted tests、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：selection 状态已有 Compose 可观察落点，但手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose text style/layout follow-up：EPUB paged 文本页现在把 slice `EpubPageTextStyle` 与 `EpubComposeTextLayout` 暴露到 `ComposeView` keyed tags，并让可见 Compose `BasicText` 使用与既有 overlay TextView 等价的正文 padding、indent、blockquote 额外缩进和 preformatted 横向滚动。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose text style and layout state` 在补最小 layout 类型后失败于 style tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：可见文本/高亮/样式布局已由 Compose 承担，但 selection 手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose selection highlight follow-up：EPUB paged 文本页现在会把透明 `SelectionAwareTextView` overlay 产生的 page-local selection range 转为 `ReaderTextHighlightRange`，写入 `R.id.epub_compose_text_selection_highlight_range` 并同步到 Compose state；`EpubComposeTextPage` 会把该 selection highlight 追加到 Compose `AnnotatedString` 背景 ranges。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose selection highlight range` 失败于 selection highlight tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：selection 高亮状态已有 Compose 渲染落点，但手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose selection container follow-up：EPUB paged 文本页现在把可见 Compose `BasicText` 包入 `SelectionContainer`，并通过 `R.id.epub_compose_text_selection_enabled` 暴露 Compose selection container 已启用状态。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose text selection container state` 失败于 selection-enabled tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：Compose 文本层已有 selection container 可测落点，但真实手势/action-mode 仍依赖透明 Android overlay，不是纯 Compose selection；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose selection callback bridge follow-up：EPUB paged `ComposeView` 现在通过 `R.id.epub_compose_text_selection_callback` 暴露 Compose-side selection 回调桥；该回调复用透明 `SelectionAwareTextView` overlay 的 selection 更新逻辑，会同步 engine `currentTextSelection`、`R.id.epub_compose_text_selection_range` 和 `R.id.epub_compose_text_selection_highlight_range`，并归一化 page-local range。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose text selection callback bridge` 失败于 callback tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是未来纯 Compose selection 手势的回调落点，真实手势/action-mode 仍依赖透明 Android overlay；仍缺真机/平板视觉、TalkBack action-mode、帧率/内存预算和完整图文分页验收，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose gesture selection wiring follow-up：EPUB paged Compose 文本层现在保存 `BasicText(onTextLayout)` 的 `TextLayoutResult`，并通过 `Modifier.pointerInput` / `detectDragGesturesAfterLongPress` 将长按拖选坐标映射为 page-local text offset，再调用 `R.id.epub_compose_text_selection_callback`；`R.id.epub_compose_text_gesture_selection_wired` 暴露该接线状态。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose text gesture selection wiring` 失败于 gesture wiring tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 Compose hit-test/long-press-drag wiring，不是真机已验证的 gesture/action-mode 迁移；透明 Android overlay 仍保留，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose selection overlay z-order follow-up：透明 `AndroidView(SelectionAwareTextView)` 仍保留为兼容/状态桥，但在 `EpubComposeTextPage` 的 `Box` 中先声明 AndroidView、再声明 `SelectionContainer { BasicText(...) }`，让可见 Compose 文本和 `pointerInput` gesture modifier 成为前景交互层；`R.id.epub_selection_overlay_behind_compose_text` 暴露该契约。Validation: RED `EpubReflowEngineTest.paged runtime keeps selection overlay behind compose gesture text` 失败于 overlay-behind tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是避免 overlay 在代码层级挡住 Compose gesture 的契约，不是真机已验证的纯 Compose selection/action-mode；无真机/平板视觉、TalkBack action-mode、帧率/内存预算或完整图文分页证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose text semantics / overlay accessibility follow-up：EPUB paged 可见 Compose `BasicText` 不再使用 `clearAndSetSemantics {}` 清空语义；透明 `SelectionAwareTextView` overlay 设为 `View.IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS`，避免 TalkBack 聚焦不可见兼容层；`R.id.epub_compose_text_semantics_exposed` / `R.id.epub_selection_overlay_accessibility_hidden` 暴露该契约。Validation: RED `EpubReflowEngineTest.paged runtime exposes compose text semantics instead of transparent overlay accessibility` 失败于语义/overlay accessibility tag 为空；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是代码层语义职责切换，不是真机 TalkBack speech/action-mode 验收；无真机/平板视觉、帧率/内存预算或完整图文分页证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 transparent overlay accessibility persistence follow-up：`SelectionAwareTextView` 新增默认关闭的 `hideFromAccessibility` 模式，EPUB paged overlay 在创建和 Compose rebind 时启用该模式，确保 active page 因标注/文本重绑触发 `setText()` 后仍保持 `IMPORTANT_FOR_ACCESSIBILITY_NO_HIDE_DESCENDANTS`，不把透明兼容层重新暴露给 TalkBack。Validation: RED `EpubReflowEngineTest.paged runtime keeps transparent overlay accessibility hidden after compose text rebind` 失败于 rebind 后 overlay accessibility 从 `4` 变回 `1`；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 Robolectric overlay 稳定性契约，不是真机 TalkBack speech/action-mode 验收；无真机/平板视觉、帧率/内存预算或完整图文分页证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose blank-selection cleanup follow-up：EPUB paged Compose/overlay 共用 selection 更新路径现在以 `epubTextSelection(...)` 的实际结果为准；当 page-local selection 只覆盖空白时，同步清空 engine `currentTextSelection`、`R.id.epub_compose_text_selection_range` 和 `R.id.epub_compose_text_selection_highlight_range`，避免 Compose 层残留 stale selection/highlight。Validation: RED `EpubReflowEngineTest.paged runtime clears compose selection state for blank compose selection` 先因测试文本多空格被 parser 合并而暴露为错误断言，校准为单空格空白选区后 GREEN；EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 Robolectric 状态契约，不是真机手势/action-mode 或 TalkBack 验收；无真机/平板视觉、帧率/内存预算或完整图文分页证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 Compose root accessibility progress follow-up：EPUB paged 文本页根 `ComposeView` 不再把页码写进 `contentDescription`，避免根节点抢占内部可见 Compose `BasicText` 正文语义；页码改由 `R.id.epub_compose_page_progress_description` 和 API 30+ `stateDescription` 暴露，`R.id.epub_compose_page_root_delegates_accessibility_to_text` 标记根节点把可读正文语义交给文本层。Validation: RED `EpubReflowEngineTest.paged runtime returns compose view as text page root` 失败于根节点仍有页码 `contentDescription`；GREEN 后同一 targeted test、EPUB 全量单测和 phase2 `:render:epub:testDebugUnitTest :render:api:compileDebugKotlin :render:animate:compileDebugKotlin :render:epub:compileDebugKotlin :features:reader:compileDebugKotlin :app:assembleDebug` 通过。边界：这是 Robolectric 语义边界契约，不是真机 TalkBack speech/action-mode 验收；无真机/平板视觉、帧率/内存预算或完整图文分页证据，PAGE-05 保持 `PARTIAL`。
- 最新 PAGE-05 transparent overlay touch/focus bridge follow-up：`SelectionAwareTextView.touchSelectionEnabled` 默认 `true`，保持普通 TXT/MD 文本视图可点击、可长按、可聚焦选择；EPUB paged 透明 overlay 在创建和 Compose rebind 时设为 `false`，关闭 `isClickable` / `isLongClickable` / `isFocusable` / `isFocusableInTouchMode`，确保它只作为 selection 状态/兼容桥，不再抢触摸或焦点。Validation: RED/GREEN `EpubReflowEngineTest.paged runtime keeps transparent overlay out of touch and focus after compose text rebind`、TXT 默认行为守护、TXT/EPUB 单测和 phase2 组合构建通过。边界：这是 Robolectric/code-contract 证据，不是真机 Compose selection、TalkBack speech/action-mode、视觉或性能验收，PAGE-05 保持 `PARTIAL`。
- 最新 EPUB-01/02/03 证据：`EpubCorpusSmokeTest` 已扩展为验证真实 EPUB 语料的字符级 progression、`epubLocatorForIndex()`、typed ReaderItem locators、parsed TOC Section locators 和有效 `totalProgression`；`fragmentIds` / `EpubBook.fragmentTargetIndexes` / `EpubLazyBook.fragmentTargetIndexes` / `epubResolveHref()` / `buildEpubToc(..., fragmentTargets)` 让 nav/ncx fragment/id 落到匹配段落，runtime internal link 也通过 `epubInternalLinkTargetKey()` 保留 `#fragment`。Validation: `EpubParserTocTest` 覆盖 full/lazy fragment 落点，`EpubLinkTargetsTest` 覆盖 runtime key，`EpubCorpusSmokeTest` 在 `/tmp/readflow-epub-corpus` 5 本公开 EPUB 上通过，EPUB 全量单测和 phase2 build 通过。来源表中 `EPUB-01/02/03` 标记 `DONE`；PAGE-05 继续保持 `PARTIAL`。
- 最新 EPUB-04/05/06 证据：`EpubCorpusSmokeTest` 继续增强为真实语料级图片/链接/降级矩阵验证：图片校验 safe zip-root href、package-local entry、encoded-size budget 和 alt text；链接校验 range、linked text、external 分类、display-block 保留和 internal target 命中；降级矩阵校验 style span 范围、list indent、table/list/blockquote/pre、fixed-layout fallback、lazy/full `isFixedLayout` 与 `spinePaths` parity。5 本公开 EPUB 通过，报告覆盖 `packageLocalImages=21`、`altImages=21`、`links=1208`、`targetedInternalLinks=1048`、`styles=787`、`tables=6`、`lists=1092`、`blockquotes=2`、`pre=2`、`idpf-cole-voyage.epub fixed=true`。Validation: corpus run、EPUB 全量单测、phase2 build 均通过。边界：仍缺真机/平板视觉 smoke、TalkBack link traversal、外链 intent、pre 横向滚动和 paged-mode image-aware pagination，来源表中 `EPUB-04/05/06` 保持 `VERIFY`。
- 最新 EPUB TOC AVD runtime smoke：`/tmp/readflow-toc-batch12-epub-real` 证明 `readflow-search|EPUB` 从 baseline `Chapter 1` 进入 TOC 后点击 `Chapter 4`，正文切到 `Chapter 4 paragraph 000..007`；Room 写入 `LocatorStrategy.Section(spineIndex=3, elementIndex=243, charOffset=0)`、`totalProgression=0.748087286949158`。边界：仍是 AVD synthetic tap/XML/screenshot/Room 证据，不是真机/平板、TalkBack speech traversal、或真人 clean tap 验收。
- 最新 EPUB-05/06 AVD runtime link smoke：MediaStore `_id=189` 的 `rfbatch3-indexing-2|EPUB` 在当前 AVD 上补齐了外链/内链运行时链路。外链方面，使用 `am start -W -a android.intent.action.VIEW -d content://media/external/file/189 -t application/epub+zip --grant-read-uri-permission dev.readflow/.MainActivity` 启书后，搜索 `competitiveautoparts` 并落到 ACDelco 段落；`/tmp/readflow-epub-runtime-batch6/external-firstline-scan-20260624-135713` 证明旧历史探针 `x=430,y=250` 在当前布局下不再命中，`/tmp/readflow-epub-runtime-batch6/external-y-scan-20260624-140352/x_430_y_270` 则成功跳到 `com.android.chrome/org.chromium.chrome.browser.firstrun.FirstRunActivity`，说明外链 runtime 已通，但验收探点需要改到新的有效 hit band。内链方面，`/tmp/readflow-epub-runtime-batch7/ch4-open-centered-20260624-142754` 先通过 TOC 打开 `Chapter 4 / Index Style and Format`，随后 `/tmp/readflow-epub-runtime-batch7/internal-link-tap-20260624-143027` 从 chapter 4 首屏 note 中点击 `Chapter 3`，页面仍停留 `dev.readflow`，标题切到 `Chapter 3 / Characteristics of a Good Index`；配套 Room 快照 `/tmp/readflow-epub-runtime-batch7/db-snapshot-20260624-143232` 记录最新 `reading_progress.totalProgression=0.171250447630882`、`LocatorStrategy.Section(spineIndex=8, elementIndex=444, charOffset=0)`，与解包语料 `package.opf` 中 `chapter003.xhtml` 的 spine 顺序一致。边界：同次 injected tap 会额外带出 `笔记` / `取消` / `高亮` / `保存` 浮层，因此这组证据只能证明 AVD synthetic runtime navigation 已通，不能替代真机 clean tap、TalkBack link traversal/speech、或 selection/link coexistence 的 UX 验收；来源表中 `EPUB-05/06` 仍保持 `VERIFY`。
- 最新 EPUB-07/08 证据：lazy/LRU 侧新增 `EpubLazyBookTest` prefetch 与 close cleanup 覆盖，`EpubCorpusSmokeTest.validateLazyBookIndex()` 在 5 本语料上校验 lazy/full paragraph count、spine paths、display blocks、fixed-layout parity、lazy index text omission、char-offset parity、sampled `paragraphAt()` 正文加载、block index range、cache warm 与 close cleanup。parser hardening 侧新增 XHTML entity rejection、lazy oversized spine skip、zip entry-count cap、corrupt ZIP fallback、安全路径拒绝覆盖，并让 `epubParserGuard()` 捕获 `Exception`。Validation: targeted lazy/hardening/corpus run、EPUB 全量单测、phase2 build 均通过。边界：仍缺真实大 EPUB 设备 PSS/首开/滚动性能、恶意 corpus/fuzz、encrypted ZIP 和真机/平板内存压力，来源表中 `EPUB-07/08` 保持 `VERIFY`。

## Source Of Truth

优先顺序：

1. `docs/android-architecture-v4.md`
2. `docs/android-p1-foundation-plan.md`
3. `docs/android-p2-txt-minimal-slice.md`
4. `docs/audit/root-cause-resolution-2026-06-18.md`
5. `docs/audit/decisions-2026-06-18.md`
6. `docs/android-architecture-v3.md`（历史/审计溯源）
7. `docs/tracking/BACKLOG.md`
8. `maintenance-log.md`

当前 Android 权威规范是 v4，P1/P2 执行状态以 P1/P2 plan 和 `maintenance-log.md` 2026-06-19 条目为准。不要从已废弃的 v1/v2 架构文档重新推导结论，除非它们与 v4 或 root-cause 决议发生矛盾时需要定位历史来源。

## Live Todo

- [x] 将 `docs/tracking/ACTIVE.md` 收短为恢复入口。
- [x] 创建 active tracker，承接 live todo、决策、验证、回滚、分区进度。
- [x] 在 tracker 中记录当前恢复证据和缺失文件。
- [x] 历史项：核验并统一 v3 阶段入口/追踪文档中的 `19` / `20` / `21` 模块说法；当时权威口径为 `21` 模块，历史审计/研究材料保留原文。
- [x] 历史项：收口架构入口；v4 发布后 Android 当前权威规范已升级为 `docs/android-architecture-v4.md`。
- [x] 刷新 `docs/architecture.md`、wiki 入口和平台页中旧 `epubjs`/`epublib`/Android 渲染状态描述；当前 Web 仍使用 epubjs 的事实保留，目标架构标注为 epub-ts。
- [x] 完成 Android v3 框架架构重审，按“用户使用轻量”而非“开发轻量/少模块”校准结论，记录在 `docs/audit/android-v3-framework-audit-2026-06-18.md`。
- [x] 用户已放行 Android Phase 1 地基 + TXT 最小阅读链路实装（2026-06-19）。
- [x] Phase 1 地基框架落地：`build-logic`、9 个模块、`:app`，`-Preadflow.phase=1 :app:assembleDebug` SUCCESSFUL。
- [x] Phase 2 TXT 垂直切片落地：`render:api`/`render:txt`/`render:animate`/`features:reader` + phase2 app wiring，`-Preadflow.phase=2 :app:assembleDebug` SUCCESSFUL。
- [ ] 刷新 `docs/wiki/Active-Work.md` / `docs/wiki/Home.md` / `docs/wiki/Development-Guide.md` 的 2026-06-18 旧状态与命令口径。
- [ ] 真机验证 TXT 滚动 + progression 回报（AVD runtime smoke 已完成）。
- [ ] 沿 P1 §6 边界推进业务：Calibre 拉取、Room 查询、本地导入、书库数据绑定、进度持久化。
- [ ] Web/HarmonyOS 实现仍需用户另行许可后再动。

## Decisions

- Active work 模式为 `task`，不是 `bug`；因此没有 Test Ledger。
- `ACTIVE.md` 只保留恢复所需的最短状态，详细信息放入本 tracker。
- 当前默认权威架构为 `docs/android-architecture-v4.md`；v1/v2/v3 仅作为历史背景或审计溯源。
- P0 构建阻塞项按 `BACKLOG.md` 现状视为已处理，但实现阶段仍需用真实构建验证。
- 模块数当前权威口径为 v4 的 22 模块；历史审计/研究文档中的 19/20/21 作为当时状态保留。
- EPUB 口径区分“当前实现”和“目标架构”：Web 当前仍是 epubjs；Android v4 已裁决为原生重排（jsoup→AnnotatedString，去 WebView/epub-ts/CFI）。
- 用户侧轻量已进入 v4 `User-Light Architecture Contract`；当前实现应继续避免把引擎选择、账号、Calibre 配置、危险权限等复杂度泄露给用户。
- `:app` 使用 phase 条件 sourceSet：`src/phase1` 零 render 依赖，`src/phase2` 绑定 TXT slice，避免 phase1 解析 phase2-only 模块失败。

## Per-Section Progress

| Section | Status | Evidence |
| --- | --- | --- |
| Project rename | Done | `ACTIVE.md` and `maintenance-log.md` record LinReads rename. |
| Architecture audit | Done | v1/v2/Round 3 audit docs exist under `docs/audit/`. |
| v3 architecture | Done | `docs/android-architecture-v3.md` exists and `docs/architecture.md` points to it. |
| Backlog consolidation | Done | `BACKLOG.md` module-count wording now points to 21 modules. |
| Doc drift cleanup | Done | `README.md`, `CLAUDE.md`, `docs/architecture.md`, and current wiki pages distinguish current epubjs from target epub-ts and remove old epublib/MuPDF-for-EPUB target wording. |
| Android v3 framework audit | Done | `docs/audit/android-v3-framework-audit-2026-06-18.md` redefines lightness as user-facing lightness, not reduced developer complexity; it recommends a `User-Light Architecture Contract` before implementation. |
| Continuity tracking | In progress | `ACTIVE.md` now names this active tracker. |
| Android v4 architecture | Done | `docs/android-architecture-v4.md` is current authority and records native EPUB reflow, 22 modules, phase includes, and User-Light contract. |
| Phase 1 foundation | Done | `android/build-logic/`, 9 modules, `:app`; maintenance log records `-Preadflow.phase=1 :app:assembleDebug` SUCCESSFUL. |
| Phase 2 TXT slice | Done | `:render:api`, `:render:txt`, `:render:animate`, `:features:reader`, phase2 app wiring; maintenance log records `-Preadflow.phase=2 :app:assembleDebug` SUCCESSFUL. |
| Wiki current-state sync | Pending | `docs/wiki/Active-Work.md` still says 2026-06-18 and Android TXT ❌. |
| Device verification | In progress | AVD runtime smoke 已完成；仍需物理手机/平板 TXT scrolling/progression 验证。 |

## Verification

Commands already run during tracker normalization:

```bash
sed -n '1,220p' /Users/kragcola/.codex/skills/omubot-continuity/SKILL.md
sed -n '1,220p' docs/tracking/ACTIVE.md
sed -n '1,280p' docs/tracking/BACKLOG.md
test -f AGENTS.md && sed -n '1,180p' AGENTS.md || printf 'AGENTS.md: MISSING\n'
test -f .workspace/agent-session-state.md && sed -n '1,180p' .workspace/agent-session-state.md || printf '.workspace/agent-session-state.md: MISSING\n'
git status --short docs/tracking maintenance-log.md CLAUDE.md
rg -n "19 个模块|20 模块|20个模块|21 个模块|21个模块|模块数|模块计数" docs README.md CLAUDE.md .claude/skills -g '!references/**'
```

Observed results at normalization time:

- `omubot-continuity` requires `ACTIVE.md` plus an active tracker for long-running/context-continuity work.
- `AGENTS.md` is missing.
- `.workspace/agent-session-state.md` is missing.
- `docs/tracking/ACTIVE.md` and `docs/tracking/BACKLOG.md` exist.
- Module-count references still drifted across historical/tracking docs; this was resolved for current entry/tracking docs during drift cleanup.

Commands run during drift cleanup:

```bash
rg -n "epubjs|epub-ts|epublib|19 个模块|20 个模块|21 个模块|尚未实现渲染|空文件/stub|全原生零 WebView|自研动画引擎|MuPDF \\(JNI\\)|硬编码为|BASE_URL" README.md CLAUDE.md docs/wiki docs/tracking docs/architecture.md -g '!docs/wiki/Android-Architecture-Audit.md' -g '!docs/wiki/Architecture-Comparison.md' -g '!docs/wiki/Ink-Architecture-Gap.md' -g '!docs/wiki/Research-MoonReader.md'
rg -n "尚未实现渲染|空文件/stub|全原生零 WebView|自研动画引擎|epublib 版本|MuPDF \\(JNI\\) \\| 待定|统一为 19|20 个模块|20 模块" README.md CLAUDE.md docs/architecture.md docs/wiki/Home.md docs/wiki/Architecture.md docs/wiki/Platform-Web.md docs/wiki/Platform-HarmonyOS.md docs/wiki/Rendering-Engine.md docs/wiki/Development-Guide.md docs/wiki/Active-Work.md docs/wiki/Calibre-API.md docs/tracking docs/wiki/_Sidebar.md maintenance-log.md
rg -n "epubjs|epub-ts|epublib|19 个模块|20 个模块|21 个模块|BASE_URL|Readflow|readflow-web|scaffold" README.md CLAUDE.md docs/architecture.md docs/wiki/Home.md docs/wiki/Architecture.md docs/wiki/Platform-Web.md docs/wiki/Platform-HarmonyOS.md docs/wiki/Rendering-Engine.md docs/wiki/Development-Guide.md docs/wiki/Active-Work.md docs/wiki/Calibre-API.md docs/wiki/_Sidebar.md docs/tracking maintenance-log.md
```

Observed cleanup result:

- Current docs now intentionally distinguish `epubjs` current Web implementation from `epub-ts` target architecture.
- `BACKLOG.md` no longer claims module count unified to 19.
- HarmonyOS docs no longer claim `BookList.ets` is an empty stub.
- Current entry/wiki/tracking docs no longer contain misleading target-state claims for `epublib`, Android MuPDF-for-EPUB, full-native-zero-WebView, self-built AnimationEngine, or “BookList empty stub”.
- README/wiki positioning now distinguishes current Web EPUB/PDF capability from target multi-format support.
- Remaining `epubjs`, `Readflow`, `BASE_URL`, and `scaffold` hits are either current implementation facts, explicit target-vs-current notes, or historical maintenance-log entries.

Commands run during user-light re-audit:

```bash
rg -n "v3-lite|直接按 21|少人团队实施|21 物理模块|物理模块.*过重|轻量性.*团队|开发轻量|少建模块|User-Light|用户轻量|用户使用轻量" docs/audit/android-v3-framework-audit-2026-06-18.md docs/tracking/linreads-architecture-docs-2026-06-18.md maintenance-log.md
git status --short docs/audit/android-v3-framework-audit-2026-06-18.md docs/tracking/linreads-architecture-docs-2026-06-18.md maintenance-log.md
wc -l docs/audit/android-v3-framework-audit-2026-06-18.md docs/tracking/linreads-architecture-docs-2026-06-18.md maintenance-log.md
```

Observed re-audit result:

- Current audit/tracker/log no longer recommend `v3-lite` or reduced physical modules as the primary lightness strategy.
- Current audit defines lightness as user-facing installation, first-run, reading, configuration, data portability, and permissions burden.
- `docs/audit/android-v3-framework-audit-2026-06-18.md`, `docs/tracking/linreads-architecture-docs-2026-06-18.md`, and `maintenance-log.md` are still untracked in current git status.

Commands run during 2026-06-19 current-change tracking:

```bash
git status --short --branch
sed -n '1,220p' docs/tracking/ACTIVE.md
sed -n '1,320p' maintenance-log.md
sed -n '1,260p' docs/android-p1-foundation-plan.md
sed -n '1,280p' docs/android-p2-txt-minimal-slice.md
git diff --stat
git diff --name-status
git ls-files --others --exclude-standard | sed -n '1,240p'
sed -n '1,260p' android/render/api/src/main/kotlin/dev/readflow/render/api/ReaderEngine.kt
sed -n '1,320p' android/render/txt/src/main/kotlin/dev/readflow/render/txt/TxtVirtualPagerEngine.kt
sed -n '1,260p' android/features/reader/src/main/kotlin/dev/readflow/features/reader/ReaderViewModel.kt
```

Observed 2026-06-19 current state:

- `docs/tracking/ACTIVE.md` and `maintenance-log.md` agree that the implementation gate has been opened for Android Phase 1 + TXT minimal slice.
- Tracked diff only shows a small part of the work; most implementation/doc files are untracked and must be included intentionally in any handoff/commit.
- Actual new code includes phase-specific app source sets, `ReaderEngine` contract, `ReaderEngineRegistry`, TXT `RecyclerView` engine, no-transition host, reader ViewModel/screen, core model/data/ui modules, Room schemas, build-logic plugins, and Gradle wrapper/settings/catalog.
- `docs/wiki/Active-Work.md`, `Home.md`, and `Development-Guide.md` still lag behind the 2026-06-19 implementation state.

## Risks

- Web code still uses `epubjs`; Web migration remains separate work.
- Android EPUB target is now v4 native reflow; do not reintroduce Android WebView/epub-ts/nanohttpd/CFI unless a new ADR explicitly overturns v4.
- Current TXT slice is deliberately minimal: UTF-8 whole-file read, paragraph split, continuous `RecyclerView`; FileChannel 64KB streaming, ICU charset detection, real import, chrome, search/TOC/selection, persistent progress are not done.
- Historical audit/research/deprecated pages still contain old terms by design; do not bulk rewrite history unless explicitly requested.
- A large portion of Android implementation and docs is untracked (`docs/`, `android/build-logic/`, `android/core/`, `android/render/`, `android/features/`, Gradle wrapper/settings/catalog, `maintenance-log.md`, `CLAUDE.md`, MoonReader references). A handoff or commit must include/exclude files intentionally.
- Generated/local files such as `.codegraph/`, `.reasonix/`, `.vscode/`, `.DS_Store`, and decompiled APK artifacts need explicit commit policy before staging.

## Rollback

This normalization is documentation-only. To roll back:

1. Restore the previous verbose `docs/tracking/ACTIVE.md` from git or backup.
2. Delete `docs/tracking/linreads-architecture-docs-2026-06-18.md`.
3. Remove the `Continuity Tracker Normalization` entry from `maintenance-log.md` if that entry has already been added.

## Do Not Re-Investigate

- Do not redo broad architecture audits from scratch; use v4 + P1/P2 execution docs + maintenance log for current state.
- Do not re-open the Android EPUB engine decision unless new evidence contradicts v4 native reflow.
- Do not re-add phase2 render dependencies to phase1 app wiring; keep phase-specific source sets unless v4 phase include design changes.
